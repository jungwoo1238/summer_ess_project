# ESS 최적 배치·용량 산정 프로젝트 — 명세 (Phase 1)

> IEEE 33-bus 배전계통 / L-SHADE 기반 이중수준 최적화 / 배전사업자 관점
> (상위 최적화기는 12차에서 PSO → L-SHADE로 교체됨. 문서 내 "PSO"는 대부분 상위 최적화기의
>  구실행/구서술이며, 현행은 L-SHADE다. 7-A절·7절·2절이 현행 기준이고, 나머지 절의 "PSO"
>  표현은 최적화기 일반을 뜻하거나 구실행 기록이다.)
> 이 문서는 방법론·파라미터의 단일 진실 원천(single source of truth)이다.
> 코드 작성 시 이 문서를 우선하며, 상충하는 이전 자료보다 이 문서가 우선한다.
>
> **개정 이력**
> - 2026-07 (1차): `benefits.py` 설계 확정 반영 — 2절 `pk_s` 구조적 배제 /
>   3절 자료구조·단위·슬랙수지 검증 규약 신설 및 B_defer 클리핑 금지 /
>   7절 실행환경 MKL 주의 / 8절 음의 B_defer 카운터 / 부록 A·B 갱신.
> - 2026-07 (2차): `evaluate.py` 완료 반영 — **3-A절 신설**(부하 규약·warm start 정책·
>   발산 처리·구조·페널티 위치) / 7절 환경 이원화 해소(데스크탑 주 실행, Colab 백업 격하)
>   및 머신 간 재현성·수치 잡음의 성격 신설 / 7절 테스트 설계 원칙 4·5 추가
>   (허용오차 규약, 테스트 삭제 금지) / 부록 A 진행상황 갱신 및 main.py 미정사항 정리 /
>   부록 B `PENALTY_DIVERGE` 추가. `test_lp.py` 미반영 권고는 처리 완료되어 삭제.
> - 2026-07 (3차): `main.py` 완료 및 **첫 dev 실행 진단** 반영 —
>   **1절 슬랙 전압 1.0 → 1.02 pu 변경**(기저 전압위반 해소, 검증값 4개 전면 재산출) /
>   **3-B절 신설**(전압 페널티 지배 문제 — 진단 실측과 미결 논점) /
>   7절 병렬화 실측 확정(워커 16 / 입자 32) 및 `bench_workers.py` 결과 /
>   **7-A절 신설**(main.py 설계 확정: 실행 단위·3n 차원·시드·로깅) /
>   부록 A 진행상황 갱신(미정사항 3건 전부 해소) / 부록 B 상수 갱신.
> - 2026-07 (4차): 슬랙 1.02 코드 반영 후 **dev 재실행 + 진단 스크립트 3종** 결과 반영 —
>   **3-B절 해소**(λ_V는 현 조건에서 조정 불필요 — 경제 영역과 위반 영역이 분리.
>   영구 확정이 아니며 재검토 트리거를 함께 규정) /
>   **6-A절 신설**(최적 규모 결정 메커니즘: 한계 b_defer vs 한계 cost 교차, 피크시각 이동 포화) /
>   **8절-5 전면 수정**(★ "위치 무차별" 예상이 실측으로 **반증**됨 — 폭 34.9%, 근원은 b_loss,
>   소용량/대용량 선호 역전) / 3절 음수 클리핑 금지가 n=2에서 실제 작동(−2.4e7원) /
>   7-A절 n=2 결과(최적 기수 1) 및 **분산 설치 미확인** 기록 / 부록 A scripts 3종 추가.
> - 2026-07 (5차): **Phase 1 뼈대 full 실행 완료(n=1, 100세대·30 run)** + 후처리·분산·스윕 확정 —
>   **8절-1 편익 share 분모를 순편익(j_net)→총편익(B_gross)으로 정정**(순편익 대비는 무의미;
>   실측 b_defer 84.4% / b_arb 14.2% / b_loss 1.42%. 기존 "손실 6%"는 순편익 대비 오류였음) /
>   **8절-5 근거 교체**(위치 스윕은 용량고정 편향 단면 → PSO 결과 내부의 b* 분포로 교체.
>   full 실측: 4버스 1.08% 이내, 버스15가 자기최적·고정용량 전수스윕 양쪽 모두 1위/32) /
>   **2절 분리 LP 유효범위 신설**(시나리오 축=정확 / 기수 축은 solve_avg만 정확, solve_peak은
>   근사 — 분산실험서 실측된 d* 중복적용. 최적기수 1에서 무해, 부록 C 편입 시 통합으로 해소) /
>   **6-A절 보강**(한계표는 E/S 고정 단면임을 명시 + full 실측: 최적 E/S≈2.342가 위치 무관 상수) /
>   **7-A절 갱신**(분산 설치 "미확인" 해소 — 현 분리LP에서는 측정 불가로 판명; 3-B 트리거 통과;
>   최적기수 1이 full·스윕으로 확정) / 부록 A scripts 2종 추가(probe_split, probe_bus_sweep_full)
>   및 진행상황·다음작업 갱신(예고했던 다음 단계 = LinDistFlow 편입은 6차에서 실현).

- 2026-07 (6차): **하위 문제 LP→QP 편입 + 물리 파라미터 확정** — 20여 세션의 프로토타입 검증 완료 반영 —
  **2-B절 신설**(하위 QP 구성: 손실 5계수 QP·LinDistFlow 전압 μ(표준 Baran–Wu)·다각형 PCS POLY_N=128·계수 측정/진단 재산출·캐시(b,S)) /
  **3-C절 신설**(손실 편익의 목적함수 편입: 부호 규약·AVG 2차항·PEAK pk 1차항·피크일 이중기여 회계변경 예정) /
  **2절 재작성**(q_ratio 제거 3n / 하위 QP / Q 사후계산→LP 내부변수 / 손실 "LP가 모른다"→"QP가 본다" / solve_avg 다수기 분리깨짐 정정 / pk_s 손실 1차항 반영) /
  **1절 부하 재스케일링**(역률 0.85→0.95, S=10MVA 고정, K_P=2.557201·K_Q=1.357609; 피더 종합역률 논거; 기저 손실·전압 개선) /
  **4절 파라미터**(RTE 90% 근거 SUNSYS+MDPI / **PCS 효율 ETA_PCS=0.975 신설**, 근거 Power Electronics HEM CEC 97.53%; 무효전력 구조 견고성) /
  **8절 신설 소절**(무효전력·손실·QP 진단 14종: b_loss P/Q분해·PCS손실·다각형바인딩·재산출효과·캐시통계·LinDistFlow전압오차 등) /
  **부록 C 회고록화**(계획서→결정 여정: 손실무시→QP편입 경위·PWL vs QP·MISOCP 등 기각·부호버그·괴리 A(LinDistFlow 뿌리)/B/C·교차항 B행렬·POLY_N 12→128) /
  **부록 B 상수**(ETA_PCS·K_P/K_Q·POLY_N·MU_VOLT 추가, Q_RATIO_BOUNDS 삭제) /
  **부록 A**(QP 편입 진행상황·다음작업 갱신, `test_qp_regression.py` 회귀기준 신설) /
  **10절**(LinDistFlow 편입 완료 반영). **5차 full 결과(j_net 3.089e6)는 Q 미반영 뼈대라 6차 재실행 시 갱신.**

- 2026-07 (7차): **6차 설계의 본코드 구현 완료 + 구현 세부 결정(Q1~Q4) 확정 + 진단 계측 추가** — dev 실행 직전 상태 —
  **부록 A 진행상황 전면 갱신**(lower_lp/loss_coeffs/evaluate/benefits/postprocess/main/테스트 전부 구현·정합성 검증 완료. `loss_coeffs.py` 신설) /
  **부록 A에 "구현 세부 결정(Q1~Q4)" 소절 신설**(3층 분업·계수 상수 굽기+SHA256 캐시·재산출 흐름 1차공유/2차분기·assert P항 한정) /
  **3-C.2 부호 assert 정정**(방전 시각 `loss_reduction>0`이 아니라 **`−a_P·P>0`(P항만)** — Q<0 흡수 시 loss_reduction 정상 음수라 전체 검증은 오탐. Q항 부호는 측정 직후 `a_Q<0`이 담당) /
  **3-C.5 회계 확정**(피크일=ess 슬랙피크 최대 시나리오 1일 통째 평가, 대응 계절 −1일; b_defer 불변(통합 max); 검산은 **b_arb_total+b_loss_total=b_energy_total** 총편익 기준으로 (나) 확정) /
  **8절 그룹 C 계측 경로 확정**(재산출 delta·캐시 통계·LinDistFlow 전압오차·μ 활성은 evaluate/main 저장 선행 필요 → 구현됨. 멀티코어 캐시는 반환값 편승+pid baseline 차분, 세대별 히트율 generations.csv) /
  **S 양자화 미결 명시**(계수 캐시 (b,S)가 S 연속이면 히트율 저하 우려 — dev 세대별 추이로 판정, 필요 시 8차 도입) /
  **1절 검증값 미결**(당시 구 슬랙 1.0 전압위반 0.9169는 구 부하 기준이라 새 부하 회귀값이 필요했음 — 2026-08-03 현 부하 실측 0.3781163378로 해소). **dev/full 실측값(캐시 히트율·b_loss_q·recompute 수렴·전압위반)은 8차에서 채운다.**

- 2026-07 (8차): **dev 첫 실행 MemoryError → 캐시 LRU 상한 + S 양자화 도입 (Q2 미결 해소)** —
  **[사건] dev 첫 실행이 MemoryError로 크래시**(solve_avg QP가 CLARABEL/SCS 양쪽 2~4 MiB 할당 실패 = 시스템 메모리 고갈). 원인: `lower_lp._PROBLEM_CACHE`가 계수 해시를 키로 무거운 cvxpy Problem(POLY_N=128 QP, 개당 수~20 MiB)을 무한 저장 + S 연속이라 계수해시가 매 평가마다 달라 히트 없이 무한 증식 + 16워커 곱. **Q2에서 "S 양자화 미도입, 히트율 보고 판정"으로 미뤘던 것이 크래시로 현실화** — dev 첫 산출물이 크래시 형태로 양자화 필요성을 증명. /
  **[해결 1] LRU 상한:** `_PROBLEM_CACHE`·`_measured_cache`를 OrderedDict LRU로. `PROBLEM_CACHE_MAXSIZE=16`(params), `MEASURED_CACHE_MAXSIZE=8192`(params). 안전판 — 메모리 상한 보장(64는 4~16 GiB로 이 데스크탑 가용 ~7.85 GiB 초과 위험이라 16으로; 워커당 ~16개×~15 MiB, 16워커 ~3.8 GiB). /
  **[해결 2] S 양자화(방식 B):** 계수 캐시 키의 S만 격자(`S_CACHE_GRID_MVA=0.001`)에 반올림, **solve·PSO 탐색은 연속 S 유지**(계수만 가장 가까운 격자에서 측정·재사용; 계수는 S의 매끄러운 함수라 근사 안전). **검증 확정: j_net 영향 0.02원(게이트 1,000원의 5만분의 1), 인접 S 고유 키 21→3(85.7%↓). 0.0001은 감소 0%라 기각.** → **Q2의 "S 양자화 미도입"은 8차에서 "격자 0.001 도입"으로 개정.** /
  **[미결 → 9차 해소]** LRU+양자화 적용 후에도 8·4워커에서 메모리 부족 재발 → **워커당 피크 실측(약 650 MiB, 기저 ~150 MiB)** 후 **8워커로 완주 확정**. 캐시 히트율은 활성 run에서 3.2~3.5%로 낮음(입자가 (bus,S)를 흩어 탐색). b_loss_q 등 실측은 9차 참조.

- 2026-08 (12차): **상위 최적화기 PSO → L-SHADE 확정·본코드 편입 + 통합 다수기 실측 반영 + 후처리 3종 개정** — 이번 세션 확정사항 —
  **[알고리즘 교체] PSO → L-SHADE 확정.** 실제 목적함수 벤치(`probe_optimizer_real.py`, N_RUNS=8)에서 L-SHADE가 우수 골짜기 포착률 100%(PSO 50%·ABC 87.5%), 재시작분산 0.13%(PSO 4.8%), wall 최소로 전 축 지배. 대리 벤치(twin_valley/rastrigin/rosenbrock) 4알고리즘 비교에서 예측된 ABC 이봉우위는 재현됐으나 **L-SHADE가 실제 3n 지형에서 오히려 최강**(대리 저차원서 희석됐던 강점이 실지형서 발현). IABC(ABC+DE) 대비 L-SHADE 우위 판정(우리 지형은 이미 포착률 천장이라 IABC 탐색력 우위 발현 여지 없음 + 무튜닝·문헌근거·구현완료가 방어·실행 양면 우위). → **2절 상위 알고리즘·7절 하이퍼파라미터·7-A절 pso_core 확인/프로파일 전면 개정.** /
  **[L-SHADE 구조·파라미터] 표준 L-SHADE**(current-to-pbest/1 + 아카이브 + 성공이력 F/CR 자기적응 Lehmer평균 + LPSR 선형인구감소). `lshade_core.py` 신설(numpy만 의존, 벤치서 검증된 구현 이식). **정할 파라미터는 사실상 eval_budget 하나** — F/CR은 실행 중 자기적응. 확정 상수: 초기인구 `N_init=12·n_dims`(n=1→36, n=2→72), full 예산 `100·N_init`·run 20, dev 예산 `30·N_init`·run 3, P_BEST_RATE=0.11·MEMORY_SIZE=6·MIN_POPULATION=4(Tanabe-Fukunaga 2014 표준값, 무튜닝). 종료가 세대수→**평가예산** 기준으로 바뀜. /
  **[기수별 입자수 원칙 반전] 7-A절 "사전 비례 금지" 개정.** 종전 "고정 입자수+사후조정"에서 **차원비례 `12·D` 채택**으로. 통제변수가 "동일 예산"이 아니라 "동일 탐색충분성"임을 명시(최적기수 비교의 공정성은 예산이 아니라 각 기수에서 최적 확보로 담보; 재시작분산·탐색실패로 사후 검증). /
  **[main 알고리즘중립 리팩터] `len(history)==len(gen_rows)` 계약 해제.** L-SHADE는 LPSR로 objective 호출당 배치크기 가변 → 세대계약 불가. generations.csv를 **배치 단위**로 재정의(신규 컬럼 `batch_size`·`eval_cumulative`, `gen`은 배치순번). gbest_f는 history 비의존·배치별 best 누적최소로 자체계산(길이불일치 원천제거). std_f를 수렴판정에 쓰지 않음(원래도 재시작분산이 판정근거라 무해). CLI `--n-particles`/`--n-iters` 제거, `--budget-coef` 추가. /
  **[통합 solve 실측 — 2절·7-A절 "미래형" 서술 대거 정정] solve_avg·solve_peak 통합 및 다수기 확장은 이미 구현·실행 완료.** 2절의 "다수기 통합은 확장단계 안건"·"최적기수1은 분리LP 산물이라 폐기"는 낡은 서술 → 통합 구현됨. 통합 n=2 실행(PSO, 세대50·run10) 실측: **1기 붕괴(한 기 S=E=0 소멸) 재현**, 최적해 b=32, j_net≈3.957e6, b_defer 붕괴 없음(통합 LP 정상작동 실증, probe_split 0.53배 붕괴 해소 확인), 대각근사 오차 median 0.03%(AVG)/0.07%(PEAK)·연간영향 2.4만원. **최적기수 1은 여전히 유력하나 "PSO 결과라 L-SHADE 재실행으로 재확정 대기"**(1기붕괴는 물리적 논증(이연지배→집중최적)+음의 b_defer 369회 간접증거로 지지되나, "16+32 분산 강제배치 j_net<1기" 직접측정은 미실시). /
  **[통합 solve_peak assert 수정] `_assert_peak_p_loss_sign`을 "시스템 순방전 시각"→"모든 기 동시방전 시각"으로 한정.** n≥2에서 한 기 방전+다른 기 충전(정상적 역방향 운전)이 "시스템합 기준 시각선택 vs 기별 가중합 검증"의 기준 엇갈림을 유발해 오탐(n=2 실행서 t=13, p_loss_term=−4.3e-4로 assert 터짐). `np.all(p_net>tol, axis=0)`로 한정 — 그 시각엔 a_P<0·전 기 p_net>0이라 부호 수학적 보장, 계수부호오류 조기탐지는 유지, n=1 동작 불변. 전수점검 결과 이것이 유일한 "n=1 가정에 묶인 assert"였고 나머지(기별루프 물리assert·시스템합 slack balance·shape assert)는 n-불변 확인. /
  **[후처리 3개정] (1) cost 3분해 추가**(`capex_power_annual`=CRF_20·C_KW·S / `capex_energy_annual`=CRF_20·C_KWH·E / `opex`; 합=cost 검산). **(2) b_loss→b_loss_avg 명칭**(본문 분해는 avg만이라 오해방지; 단 비중섹션 분모는 총편익이라 `b_loss_total`·`b_arb_total` 유지 — avg만 넣으면 peak분 빠져 합≠100%. CSV 컬럼·benefits 함수명 불변, 표시계층만). **(3) EFC 회계정합**: 연간EFC를 회계(`_accounting_values`)와 동일하게 `select_peak_day`가 고른 **단일 peak 계절만 −1일+peak 1일 대체, 다른 계절 peak은 연간합서 제외**(종전 247일 순가중 + peak 별도보고에서 정정). /
  **[음의 b_defer 서술 정정 — 3절·8절] "n=1은 1e-5 잡음, 실질은 n≥2에서만"은 낡은 서술 → 폐기.** PSO 시절에도 n=1 일부 run에서 백만원 단위 음의 b_defer 발생, L-SHADE dev도 −539만원 실측. 이는 **최적해가 아니라 탐색경로의 열등해(bus16 지역해)가 만드는 실질 음편익**(최적해 b=31은 b_defer 양수 유지). min_bdefer는 전 run 합산 최솟값이라 열등해 통과 시 큰 음수. /
  **[기타] RUN_CONSISTENCY_ATOL 10→15원**(n=2 우수해서 Q가 PCS한계 binding·poly_binding 0.53일 때 QP solver 재현오차 10.07원으로 임계 미세초과 — 물리량 아닌 solver 재현잔차라 버그탐지력 무손상). **`_sample_f` overflow 무해 판정**(Cauchy 샘플의 tan(π/2±ε) 특이점 → numpy reduce overflow 경고, 그러나 `value>0` 재샘플·`min(,1.0)` 클리핑이 방어해 F값 정상·결과 무영향, 미수정). **시드 SeedSequence 복원**(편입 시 실수로 `base_seed+run_idx`(42,43,44 연속정수)로 격하됐던 것을 원래 `SeedSequence(entropy,spawn_key=(n_ess,)).spawn(n_runs)`로 복원 — 기수간 독립·run간 무상관 보장, default_rng가 SeedSequence 직접 수용). **POLY_N=128 유지 확정**(256은 편익 +300원·wall +24%·메모리 +8%로 회수 안 됨; 64는 다수기·Phase2 비상수단으로 문서보존). **계산시간 실측**(n=2 통합 run당 ~1540s = n=1의 2배 미만, LPSR+캐시 완충으로 다수기 부담 예상보다 가벼움). /
  **[미결 → 다음 세션]** ① **L-SHADE 실제 포착률**(probe·저품질시드 100%였으나 SeedSequence 복원 후 dev 3run중 1개 bus16 함몰 관측 — full run20으로 실측 필요; 낮으면 인구·예산 증액 검토) / ② **예산 절반 실험**(full 예산 100·N_init는 PSO등가 안전상한 — 50·N_init로 포착률·j_net 유지되면 다수기·Phase2 부담 절감) / ③ **n=2 최적기수 L-SHADE 재확정**(편입후 재실행; 1기붕괴 재현되면 알고리즘무관 결론 확정) / ④ test_qp_regression·test_evaluate·test_lp·test_benefits 전면 갱신(최신 최적해 b=31/32·통합solve·L-SHADE 기준; L-SHADE 편입후 일괄) / ⑤ "16+32 분산 강제배치" 직접측정으로 1기최적 확정 여부 / ⑥ 그룹C 결측(`qp_loss_model_err` — LP 시각별 예측손실 미저장) / ⑦ 중간발표 PPT.

- 2026-08 (11차): **메타휴리스틱 대리 벤치(10-F) + 통합 solve_peak·evaluate 다수기 확장 구현** — L-SHADE 편입 직전 —
  대리함수(twin_valley 이봉/rastrigin/rosenbrock) 4알고리즘(PSO/ABC/L-SHADE/GWO)×30run 벤치: 이봉탈출률 ABC 100%/L-SHADE 36.7%/PSO 20%/GWO 13.3%, rastrigin 전부 100%, rosenbrock 정밀수렴 L-SHADE 100%/ABC 80%. GWO 탈락, L-SHADE는 대리 저차원(2D)서 강점희석이라 실지형 벤치까지 보류(→12차서 실지형 최강 확인). PCS 무효전력 변환손실 목적함수 편입(접선하한, POLY_N=128 각도재사용; b_loss_q −444,114→+474,078 부호반전). PEAK 목적함수 원단위 재작성(pk-only→c_cap·pk+arb+loss+eps+volt+pcs; 이연이 에너지의 ~1,018배). Q 순가치 dj_net=+743,004원=j_net의 18.78%, boundary 공급이 진짜 최적(무차별구간 없음) → 9절-Q "Q 가치가 전압·용량 채널" 해석은 PCS 미편입 인공물이라 폐기, **"무효전력이 배전망에서 실질가치·한계공급이 최적"으로 확정**. evaluate n기 확장(조인트(n,T) QP·active mask·대각손실근사·(bus,S)공유측정 dedup·QP대각예측 vs AC 진단). 교차항 측정(위상의존: 같은분기 최대 8.2%/다른분기 0.1~0.2% → 대각근사 채택). 캐시개선(S_CACHE_GRID 0.05·MAXSIZE 262144·N_WORKERS 8, full 20run 히트율 91.5%). full(20run) 지형 이봉(bus31/32 우수 vs bus16 열등, 4.8% 갭)·다중재시작으로 최우수 포착 → full 완주 가능. 최우수해 b=31·S≈0.2107·E≈0.4408·j_net≈3.96e6.

- 2026-08 (10차): **QP 채택 확정 + 물리 파라미터 최종 확정** — 9세션 프로토타입 실험 종합 —
  probe_lp_loss_proto 9세션으로 QP가 PWL 대비 우위 확인(PWL의 ±0.4% 정확도는 세그먼트 경계 측정의 인위적 결과, 경계 사이 M=4 최대 −32.5% 오차). Qe_base 보정(1/α grossup)으로 오차 −8.4%→−0.7%. POLY_N=128 확정(N=64 대비 수렴차 500원 이내이나 방어가능성 우선). PSO 차원 3n 확정(q_ratio 상위변수 제거, LinDistFlow LP가 전압페널티·Q운영 최적화 담당). 전력률 0.95 기준선(한국 요금제 0.92–0.97 관리밴드 집합적 피더 역률, 특정 규제의무 아님; pf=0.85 Baran–Wu 기본값서는 Q 순기여 양수, pf=0.95서 음수 전환). PCS 손실 Loss_pcs=(1−ETA_PCS)·[√(P²+Q²)−|P|](Q전용 증분, P경로는 RTE 90%에 포함이라 이중계산 방지). LinDistFlow 정확도 게이트 최대전압오차 0.001421 pu<0.005 통과("sweep wide, judge narrow"). 테스트 허용오차 정책(물리량 atol=1e-7/비영금액 rtol=1e-9/기댓값0 금액 atol=10.0). Envelope theorem(Q 7.5% 이동에도 j_net 0.006–0.18%).

- 2026-08 (9차): **측정 병목 3.7배·재산출 제거 2배 = 계수측정 약 4배 가속 + dev 완주·무해성 검증** — 활성 run 4,780초 → 1,169초 —
  **[9-A] 측정 그리드 min7 도입(3-C.3·2-B.5 반영됨):** `scripts/probe_coef_grid_reduction.py`로 96조건×81점 조밀격자 참값 대조. min7 7점(baseline 포함 8회)이 종전 full25 26회 대비 손실면 오차 동급(중앙 0.002%, 최악 0.054%)이면서 측정 3.25배 감소. `b_PQ` 상대오차(최악 23%)는 절대크기 1/11에 따른 착시로 손실면 무해. `LOSS_GRID_DESIGN`으로 full25 복원 가능. **dev 검증: 최적해 bus16·S·E·편익구조 불변, run2 4,780→2,222초(2.15배).** /
  **[9-B] PCS 경계 pull(2-B.5·3-C.3 반영됨):** min7이 정격 경계(`|P_center|/S=1.0`)에서 rank=1·`a_Q=0`으로 축퇴(dev 크래시). `scripts/probe_boundary_degeneracy.py` 180조건서 축퇴는 frac=1.0의 36건에만 발생. 해법 3종 비교(pull 0.036% / full25 fallback 안전하나 비용증가 / qzero 8.4% 폭발) → **pull 채택**(`LOSS_PULL_THRESH=0.95`, `LOSS_PULL_TARGET=0.85`, 측정 그리드 중심만 이동, solve·운영점 불변, 7점 비용 유지). /
  **[9-C] 최적화 재산출 완전 제거(2-B.5 반영됨):** dev 후처리로 재산출 전후 지표 측정 — 재산출의 연 j_net 영향 약 100원(j_net 0.003%), p_shift-coef_delta 상관 0.975. j_net으로 자르는 것은 재산출을 해야 알 수 있어 순환이므로 불가. 조건부·차등 임계는 연 100원 규모라 실익 없어 **완전 제거**(`RECOMPUTE_ENABLED=False` 기본, 진단 경로는 C1/X1 유지해 무해성 지속 관측, True로 복원). **dev 검증: j_net 2,993,945→2,991,905(−0.068%), 최적해 bus16 유지·편익구조 불변, run2 2,222→1,169초(1.90배).** 주의: 재산출 없이 PSO를 처음부터 재탐색해 **최적해가 S 0.15353→0.15018(−2.18%)로 미세 이동** — 오류가 아니라 C0 일관 최적해(최적화·평가가 같은 계수 사용). /
  **[9-D] 후보4(캐시 공유) 기각:** 히트율 3.5%로 세대 내 중복이 원천적으로 적음((bus,S) 동시일치 희박). min7로 측정이 싸진 뒤 공유 이득은 전체의 6.5%뿐인데 pso_core+evaluate 3모듈 수정 필요 → 투자 대비 회수 나빠 취소. /
  **[9-E] dev 실측값 확정(8차 [미결] 해소):** 8워커 완주(run0/1 256/255초, run2 1,169초). v_violation=penalty_v=0(3-B 트리거 통과 유지). b_loss_q=**−444,114원**(음수 유지 — 단 해석은 "Q 무가치"가 아니라 "Q의 가치가 손실이 아닌 전압·용량 채널에 있음"으로 정정, 9절-Q 참조). pcs_loss_annual=2,492,264원. q_to_s_ratio=0.787, poly_binding_frac=0.633, s_util_median≈1.0. **최적해 갱신: b=16, S=0.15018, E=0.43580, j_net=2,991,905원/년**(5차 뼈대 3.089e6은 pf=0.85·Q미반영이라 폐기). 편익구조 b_defer 88.5%/b_arb 12.9%/b_loss −1.5%. /
  **[미결 → 다음 세션]** ① **PSO degeneration 탈출**(3 run 중 2가 S=E=0 비활성 평지에 빠짐 — full의 통계 판정 선결조건, 9절-D 참조) / ② test_evaluate·test_qp_regression을 이번 최적해 값으로 확정 / ③ S 양자화(나아가 캐시 전체) 제거 검토(히트율 3.5%면 속도 무관 — 단 degeneration 수정 후 히트율 재측정하고 판정) / ④ overflow 경고(초기 세대 발산입자 집계) 원인 확인.
---

## 0. 연구 목적과 산출물

**연구 질문:** "이 배전망에 ESS를 설치하는 것이 경제성이 있는가? 임계조건은 무엇인가?"
(→ "어디에 얼마나"에서 "경제성 갖는가"로 이동. 이연 위치무차별·손실~1%·손익분기 아슬아슬이 근거.)
> ★ **2026-07 4~5차 정정:** 위 근거 중 **"이연 위치무차별"은 실측으로 반증됐다**(8절-5).
> 위치는 유의하며 그 근원은 이연이 아니라 **손실 편익**이다. 다만 연구 질문의 이동 자체는
> 유효하다 — **최적권이 넓다**(full 30 run: PSO가 4개 버스로 갈렸으나 j_net이 **1.08% 이내**,
> 버스15가 자기최적·전수스윕 양쪽 1위)라 실용적 선택 자유도가 크고, 손익분기가 아슬아슬하다는
> 판단은 오히려 강화됐다(최적 S가 상한의 7%인 0.176 MVA).
> ★ **2026-08 9차:** 위 0.176 MVA는 5차 뼈대(pf=0.85·Q미반영) 값으로 폐기. QP·pf=0.95
> 반영 후 **최적해 b=16, S=0.15018 MVA, E=0.43580 MWh, j_net≈2.99e6 원/년**(부록 A 9차).
> pf=0.95(Q 여유 제약)에서도 순편익 약 299만원이 나온 것은 예상보다 높은 값 — ESS 경제성이
> Q 여유에 크게 의존하지 않는다는 (연구 질문에 직접 답하는) 신호일 수 있다. n=1 full·다수기서 확인.

**최종 의사결정 산출물: (b, S, E)** — 위치·전력용량·에너지용량.
- CAPEX가 (S, E)의 함수이며, 이 셋이 실제로 돈을 들여 결정하는 물리적 설비 사양.
- **q_ratio, P[t], Q[t] 스케줄은 최종 산출물이 아니라 "편익 평가의 산물"**이다.
  PCS는 정격 S 안에서 P·Q를 실시간 조절 가능하므로 q_ratio는 설비가 아니라 운영 파라미터.
  q*는 "이런 P/Q 배분이 편익을 최대화한다"는 운영 특성의 발견(설치 사양 아님).

**편익은 완전예지(perfect foresight) 상한이다.** 과거 데이터로 24시간을 미리 다 알고 최적화.
실제 운영자는 예측값(불완전)으로 운영하므로 실제 편익은 이 상한보다 다소 낮을 수 있다.
논문에는 "완전예지 상한"임을 명시만 하고 예측오차는 별도 모델링하지 않는다.
사이징 판단에는 상한으로 충분. Phase 1·Phase 2를 **동일한 완전예지 기준**으로 평가하므로
두 Phase의 비교는 공정(예측오차 편의가 양쪽에 동일하게 작용).

**Phase 구분:**
- Phase 1 (본 명세): 손실 + 이연 + 조달비, 유효+무효전력. "현재조건 경제성·임계조건".
- Phase 2 (나중): 태양광 추가(침투율 스윕). 시나리오 5→10개(맑음/흐림 분기).
  데이터 미확보, Phase 1 완료 후 착수. **코드는 시나리오 리스트 길이에 무관하게 설계**할 것.

---

## 1. 계통 설정 (0번 검증 완료)

- IEEE 33-bus (pandapower `case33bw`), 버스 0-based, 슬랙=bus 0, 총 33버스.
- 선로 37개(방사형 32 + tie 5). **tie 선로 5개는 in_service=False (제외)**.
- `max_i_ka = 99999`(더미) → 정격 주입 필수.

**전압·스케일:**
- `net.bus.vn_kv = 22.9`, **`net.ext_grid.vm_pu = 1.02`** (아래 ★ 참조)
**부하 스케일 (6차 개정 — 역률 0.95).** S=10 MVA를 고정하고 종합 역률을 0.95로 맞춘다. P·Q에 **각각 다른 계수**를 적용한다:
- 원본 case33bw 부하 합: P=3.715 MW, Q=2.300 Mvar (역률 0.850).
- 목표: S=10 MVA, PF=0.95 → P_target = 9.500 MW, Q_target = 3.122499 Mvar.
- `K_P = 9.500 / 3.715 = 2.557201`, `K_Q = 3.122499 / 2.300 = 1.357609`.
- 버스별: `p_mw *= K_P`, `q_mvar *= K_Q`. 버스별 역률은 원본대로 다르되 **총 S=10 MVA, 총 역률 0.95**.

**왜 역률 0.95인가 (5차까지의 0.85에서 변경):**
- **현실 정합성.** case33bw 원본 역률 0.85는 IEEE 벤치마크의 임의값이지 한국 배전망 근거가 아니다. 실제 배전 피더의 종합 역률에 맞춘 값이 0.95다.
- **★ 요금제 역률(0.92~0.97)과 피더 종합 역률의 관계.** 한전 역률 요금제는 **기준역률 0.92**로, 0.92~0.97 구간을 관리한다(2025 개정). 그러나 이 요금제 기준은 **개별 수용가 말단 부하의 역률**을 규율하는 것이지, 배전 피더 전체의 종합 역률이 아니다. 실제 배전망의 피더 종합 역률은 요금제 기준보다 **높게 형성**된다. 이유:
  1. **말단 역률 규제의 상향 압력.** 개별 수용가가 요금 유인으로 콘덴서를 설치해 역률을 0.92 이상으로 개선하면, 그 부하들이 모인 피더의 종합 역률도 그만큼 끌어올려진다.
  2. **계통 측 무효전력 보상 설비.** 배전 계통에는 변전소·피더의 전력용 콘덴서 뱅크, 자동 역률 조정 장치가 설치되어 잔여 지상 무효전력을 추가 보상한다. 말단 부하 역률에는 반영되지 않는 이 보상이 피더 종합 역률을 더 높인다.
  3. **선로·변압기 충전용량(진상 무효전력).** 배전 선로와 변압기의 정전용량이 진상 무효전력을 공급해 부하의 지상 무효전력 일부를 상쇄한다.
  → 이 세 요인의 결과로, 변전소에서 본 피더 종합 역률은 요금제 하한(0.92)보다 높은 **0.95 내외**에 형성된다. 본 연구는 이를 일반적 배전망의 대표값으로 채택한다.
- **무효전력 편익 현실화.** 역률 0.85에서는 부하 무효전력이 과대해 ESS 무효전력의 손실저감 편익이 과대계상된다. 잘 보상된 현실 배전망(PF 0.95)을 모사해 이를 억제한다.
- **정당한 값을 고정.** S=10 MVA는 산업부 고시 논거가 있으나, P·Q 개별값이나 역률 0.85에는 독립 논거가 없다. 정당한 것(S)을 고정하고 물리 논거가 있는 역률(0.95)로 P·Q를 재산출한다.

**부수 효과 (실측):** P 증가(8.50→9.50)에도 Q 감소(5.26→3.12)가 전압강하를 상쇄하고 남아, 기저 손실 296.86→281.25 kW, Vmin 0.9620→0.9641로 **개선**된다.

**민감도 (예정):** 역률은 무효전력 편익 크기를 통해 결론에 영향을 줄 수 있는 유일한 파라미터다. 역률 0.92/0.95/0.97 민감도 스윕으로 결론 강건성을 확인하는 것은 Phase 2 이후 여유 시 수행한다.
- 근거: 한전 「분산형전원 배전계통 연계 기술기준」(HO-배전-기준-0015, 2025.12.02)
  제23조 — 22.9kV 일반 배전선로 상시운전용량 10,000 kVA.

### ★ 슬랙 전압 1.02 pu (2026-07 3차 개정, 확정)

**변경 이유: 기존 `vm_pu = 1.0`에서는 기저상태(ESS 없음)가 이미 전압 하한을 위반한다.**
`Vmin = 0.9407 < 0.95`이며, 7절 정의(ALL_DAYS 5시나리오 × 24시각 × 전 버스 L1 합)로 집계하면
**기저 전압위반 총량 = 0.9169 pu**다. 이것이 목적함수를 오염시켰다(상세는 3-B절).

**실측 (`scripts/probe_voltage.py`, 2026-07-20 확정):**

| `vm_pu` | Vmin (bus 17, summer_peak t=18) | 총 위반량 | 총손실(kW) | line0(A) | 최대 선로이용률 |
|---|---|---|---|---|---|
| 1.00 | 0.94070 | **0.9169** | 310.06 | 261.51 | 0.6378 |
| 1.01 | 0.95135 | 0.0 | 303.35 | 258.72 | 0.6310 |
| **1.02** | **0.96199** | **0.0** | **296.86** | **255.99** | **0.6244** |
| 1.03 | 0.97261 | 0.0 | 290.57 | 253.32 | 0.6178 |
| 1.05 | 0.99381 | 0.0 | 278.62 | 248.15 | 0.6052 |

- 상한 위반은 전 구간 0. **슬랙(bus 0)이 최대 전압 버스**이므로 `Vmax = vm_pu`이고,
  1.02에서 상한(1.05)까지 0.03 pu 여유가 있다. 33버스 방사형 배전망의 전체 강하폭이
  0.059 pu이므로 ESS 방전이 이를 역전시켜 상한을 넘길 수 없다.

**왜 1.01이 아니라 1.02인가 (여유의 문제):**
- 1.01의 여유는 `0.95135 − 0.95 = 0.00135 pu`뿐이다. 이 폭은 **부록 C.8이 지적한
  "연속오차의 이산증폭"에 그대로 노출된다** — 페널티 판정이 0.95라는 날카로운 경계에서
  일어나므로, 조류계산 수렴오차나 시나리오 미세 변경만으로 위반이 생겼다 없어졌다 하며
  **결과의 재현성 자체가 흔들린다.**
- 1.02의 여유 0.012 pu는 기저 강하폭(0.059)의 20%로, 해석 오차보다 충분히 크다.
- 1.03 이상은 상한 여유가 0.02로 줄어 반대쪽이 조여진다.
- 1.02는 실무 변전소 OLTC(부하시 탭절환기)의 통상적 탭 설정에 해당해 물리적 정당성도 있다.
- **★ 6차 재확인:** 부하 재스케일링(역률 0.95, P=9.5) 후에도 vm_pu=1.02에서 기저 전압위반 0을 재확인했다(Vmin=0.9641). 역률 개선이 전압 여유를 늘려 1.02 논거가 그대로 유지된다.

**★ 이 조정이 흡수하는 것과 흡수하지 않는 것 (경계를 명확히 할 것):**
- **흡수하는 것: 기저 위반뿐이다.** 기저 위반은 case33bw 원본계통의 성질이지 ESS의 책임이
  아니며, 연구 질문(0절 "일반적인 배전망에서 ESS가 경제성이 있는가")과 무관한 상수다.
- **흡수하지 않는 것: ESS 충전으로 발생하는 위반.** 이것은 **ESS가 만든 위반**이므로
  페널티가 벌해야 할 대상이다. 이를 미리 흡수하려고 슬랙을 더 올리면
  "기저 위반은 ESS 책임이 아니다"라는 명분을 넘어 **"ESS 책임인 것도 봐준다"**가 되어
  페널티의 존재 이유가 사라진다. → **1.02에서 충전 시 위반이 나오는 것은 정상 작동이다.**

**심사 대비 서술:** "슬랙 전압 1.02 pu는 변전소 OLTC의 통상적 탭 설정에 해당하며,
이를 통해 ESS 미설치 기저상태에서 전 시나리오·시각·버스가 0.95~1.05 pu를 만족한다.
이는 ESS의 편익을 '이미 위반 중인 계통의 구제'가 아니라 **'정상 운영 계통에서의 경제성'**으로
평가하기 위한 설정이다."

**※ 전압 범위 0.95~1.05 pu의 출처는 ANSI C84.1 / IEEE 계열 표준이다.**
한국 전기사업법 시행규칙 [별표 3]의 표준전압 허용오차는 **저압 수용가 단자전압 기준**
(220V ±10% 등, 2025.6.13 개정)이고 22.9kV 배전선로의 pu 유지범위가 아니다.
심사에서 "0.95 근거"를 물으면 **전기사업법이라고 답하면 틀린다.**

**검증값 (반드시 이 값이 재현되어야 함 — 3차 개정으로 전면 재산출):**
| 항목 | 원본 12.66kV | 스케일 22.9kV/10MVA<br>`vm_pu=1.0` (구, 참고) | **스케일 + `vm_pu=1.02` (현행)** |
|---|---|---|---|
| 총손실 | 202.68 kW | 310.06 kW | **296.86 kW** |
| Vmin | 0.9131 pu (bus 17) | 0.9407 pu (bus 17) | **0.9620 pu (bus 17)** |
| 주간선 전류(line 0) | — | 261.51 A | **255.99 A** |
| 최대 선로이용률 | — | 0.6378 | **0.6244** |
| 기저 전압위반 총량 | — | 0.9169 pu | **0.0 pu** |
- Vmin 발생 지점은 두 경우 모두 **summer_peak, bus 17, t=18**로 동일하다.
- `vm_pu=1.0` 열은 **폐기된 설정이 아니라 회귀 검증용 참고값**이다. `build_net`이
  슬랙 전압을 인자로 받는다면 옛 값도 재현되어야 한다(변경이 슬랙 전압에만 기인함의 확인).
- 위 표의 `vm_pu=1.0`, 위반 0.9169는 **구 부하 PF=0.850241까지 포함한 역사 비교값**이다.
  현 확정 부하(P=9.5MW, PF=0.95)에서 슬랙만 1.0으로 내린 회귀값은
  **0.3781163378009831 pu**이며 `VALIDATION_CURRENT_LOAD_SLACK_1P0`와
  `test_base_violation_at_legacy_slack`가 이 조건을 검증한다.
- 슬랙 유입 8.8125 MW는 **부하 프로파일·슬랙 전압에 따라 달라지므로** 단일 검증값으로
  쓰지 말 것(위 표에서 제외했다). 대신 수지식 `P_slack = ΣLoad + Loss − P_ESS`가
  atol=1e-7로 성립하는지로 검증한다(3절).

**선로 정격 (KS C 3113, 40°C). from/to 버스쌍으로 매칭:**
| 선로(bus pair) | 규격 | 정격 |
|---|---|---|
| 0 (0→1), 1 (1→2) | ACSR-OC 160㎟ | 410 A |
| 2 (2→3), 3 (3→4), 4 (4→5) | ACSR-OC 95㎟ | 296 A |
| 5~31 (27개) | ACSR-OC 58㎟ | 222 A |
- 여유율 계수 안 곱함(부하 설정에 반영됨). 제약조건 전용.
- 실측: 최대충전(2.4MVA) 시 선로 이용률 최대 61% → **전압이 지배 제약**.
  (슬랙 1.02 기저에서는 이용률이 더 낮다 — 0.6244. 결론 동일.)

---

## 2. 방법론 — 이중수준 최적화 (Nested)

### 상위 (L-SHADE): 배치·용량 탐색 (12차 — PSO에서 교체)
- **★ 최적화기 = L-SHADE (12차 확정).** 12차까지 PSO였으나, 실제 목적함수 벤치에서 L-SHADE가 우수 골짜기 포착률·재시작분산·wall 전 축에서 PSO를 지배해 교체됐다(개정이력 12차, 7-A절). L-SHADE = DE + 성공이력 F/CR 자기적응(SHADE) + 선형인구감소(LPSR). 구조·파라미터는 7-A절, 구현은 `lshade_core.py`.
- **이질적 다수기.** n기 = **3n 차원**: 각 기 `(b_i, S_i, E_i)` 독립. (5차까지의 `q_ratio_i`는 6차에서 제거됐다 — 아래 ★.)
- **★ q_ratio 폐기 (6차).** 5차까지 무효전력비 `q_ratio`를 상위변수로 두고 `Q[t] = q_ratio·√(S²−P²)`로 사후 계산했다. 6차에서 **Q는 하위 QP의 시각별 결정변수**가 되어(2-B), 상위가 q_ratio를 정할 필요가 없어졌다. 하위 QP가 손실·전압·PCS를 보고 각 시각의 Q를 직접 최적화하므로, 상위는 배치·용량 `(b,S,E)`만 탐색한다. 상위 차원은 **3n**이며(코드 전면 3n 기반, 4n 어댑터는 제거됨), 8절-6의 q_ratio 스윕은 폐기된다.
- 1기부터 늘리며 순편익이 꺾이는 지점 탐색(기수 자체가 결과). 기수별 독립 최적화.
- 중복 배치 허용 (a)방식, 경계 `b∈[1,32]`, `S∈[0,2.4]`, `E∈[0,10.2]` — 유지. **q_ratio 경계 삭제.**
- 상위는 `(b,S,E)`만 던지고 fitness 스칼라를 받음. 편익 계산은 전부 하위 QP + 조류계산.
- **종료 조건 = 평가예산(eval_budget).** PSO는 세대수로 종료했으나 L-SHADE는 LPSR이 인구감소를 예산에 맞추므로 **평가예산 기준 종료**다(7-A 프로파일). full=100·N_init, dev=30·N_init.

### 하위 (QP, 시나리오별) — 6차 재작성
> **6차 변경 요지.** 하위 문제가 (a) LP→QP(손실 편입), (b) Q 사후계산→LP 내부변수, (c) solve_peak 다수기 통합으로 바뀌었다. 하위 QP의 상세 구성(손실 5계수·전압·다각형·계수 재산출)은 **2-B절**, 손실항의 편익 편입(부호·AVG/PEAK)은 **3-C절**에 있다. 이 소절은 방법론 골격만 다룬다.

**시나리오 축 분리 — 유효(불변).** 운영변수 `P_s[t], Q_s[t]`가 시나리오마다 별개이고 결합 제약이 없어 시나리오별 분리 최적화 = 통합 최적화(수학적 동일). 6차에서도 유지된다.

**★ 기수 축 — solve_avg의 분리가능성이 깨진다 (6차 정정).** 5차는 *"solve_avg 목적 `min Σ SMP·(P_ch−P_dis)`가 기별 분리가능이라 정확"*이라 했다. **6차에서 손실항이 목적함수에 들어오면서 이 서술이 깨진다.** 다수기 손실은 `Q^T B Q` 꼴로 **기 간 교차항(비대각 `b_PQ` 성분)**을 갖는다 — 방사형 배전망에서 두 기가 상류 선로를 공유하면 `2·r·I_i·I_j` 교차 손실이 실재하기 때문이다. 이 비대각항이 곧 기 간 결합이므로 **solve_avg도 분리 불가능**해진다.
- **함의:** QP 손실 편입은 solve_peak뿐 아니라 solve_avg도 **다수기 통합 대상**으로 만든다. 5차는 통합을 solve_peak에만 걸었으나, 6차에서는 solve_avg에도 걸린다.
- **★ 구현 완료 (12차 정정).** 위 통합은 **이미 구현·실행 완료**됐다(종전 "다수기 확장 단계 안건"은 낡은 서술). `lower_lp.solve_avg`는 조인트 (n,T) QP로 교차항 `b_PQ`(비대각 `H[p,p+1]`)를 담고, `solve_peak`은 시스템 전체 단일 스칼라 `pk`로 통합됐다. 통합 n=2 실측(12차): 교차항은 위상의존(같은 분기 최대 8.2%/다른 분기 0.1~0.2%)이라 **대각근사 채택**(최종 편익은 AC로 정확, 대각은 QP 스케줄 선택에만 영향; n=2 대각근사 오차 median 0.03%(AVG)/0.07%(PEAK)·연간영향 2.4만원). **최적 기수는 다수기 실험이 결정한다** — n=2 통합 실측에서 1기 붕괴가 재현됐고(아래·7-A), 이는 유력하나 L-SHADE 재실행으로 재확정 대기다.

**★ 기수 축 — solve_peak (통합 구현 완료, 12차).** `min max_t(load−Σ P_i)`의 max가 기수에 분리 불가능한 문제(되갚음 상한 d* 중복 적용, 분할 시 b_defer 0.53배)는 5차 실측대로였고, **통합 solve_peak(n기를 하나의 peak LP, 시스템 단일 pk)로 이미 해결됐다.** 통합 n=2 실측에서 분산 설치 시 b_defer 붕괴가 **없음**을 확인(probe_split이 경고한 0.53배 붕괴 해소) — 통합 LP가 다수기에서 정상 작동한다는 실증이다.

**AVG_DAYS 3개 / PEAK_DAYS 2개** — 목적함수는 3-C절.
- **★ pk_s의 성격 변화 (6차 정정).** 5차의 `pk_s = max_t(load − P_net)`는 **부하단 피크로 손실을 전혀 모르는** 대리값이었다. 6차에서는 pk 제약에 손실 1차항이 들어가(3-C.4) `pk_s ≥ load − P_net − (−a_Q·Q − a_P·P)`가 되어, **부하단과 실제 슬랙 사이의 1차 근사**로 개선됐다. 즉 대리값이 실제 슬랙에 더 가까워졌다.
- **★ 불변인 것:** 그럼에도 B_defer는 LP의 `pk_s`가 아니라 **사후 AC 조류계산 슬랙피크**로 계산한다("정직한 단서" 구조). pk_s는 여전히 스케줄을 정하기 위한 내부 대리값일 뿐이고, 최종 이연은 그 스케줄을 조류계산에 넣은 슬랙피크가 결정한다. pk_s가 1차항으로 개선된 것은 "더 나은 스케줄을 유도"할 뿐, 편익 산정 경로를 바꾸지 않는다.
- **★ b_defer 시그니처 강제 (불변):** `benefits.py`의 `b_defer`는 `pk_s`를 인자로 받지 않는다. 조류계산 슬랙 배열(`p_slack_base`/`p_slack_ess`)만 받는다. (오용 구조적 차단.)

**충·방전 변수** (P_ch/P_dis 비음 분리, 상보성 자동) — 6차 불변.

**★ 무효전력 — 사후계산에서 LP 내부변수로 (6차 정정).** 5차는 *"Q = q_ratio·√(S²−P²)로 LP 밖 사후계산, LP는 |P|≤S만"*이었다. **6차에서 Q는 하위 QP의 시각별 결정변수**다. Q가 손실·전압을 통해 목적함수에 들어가므로(2-B), 사후계산으로는 최적화할 수 없다. PCS 제약은 **다각형 근사**(2-B.4, POLY_N=128)로 `(P,Q)` 원을 표현한다. 5차의 "|P|≤S 동치" 논리는 q_ratio 고정을 전제했으므로 6차에는 적용되지 않는다.

**★ 손실 — "LP가 모른다"에서 "QP가 본다"로 (6차 정정).** 5차는 *"손실은 비선형이라 LP에 못 넣고, 조달비 대리로 스케줄 후 사후 측정"*이었다. **6차에서 손실은 QP 5계수(2-B.2)로 목적함수에 들어간다.** 비선형 손실을 2차 근사로 표현할 수 있게 됐기 때문이다. 단 **최종 편익 값은 여전히 사후 AC로 측정**하며(3-C.1), 목적함수의 손실항은 "더 나은 스케줄을 유도"하는 역할이다. 근거였던 "손실 편익 ~1%"는 무효전력을 못 보던 시절의 값이고, Q를 최적화하면 손실 편익이 유의미해진다.

시간해상도 Δt=1시간(24스텝) — 불변.

---

## 2-B. 하위 LP 구성 — QP 손실 + LinDistFlow 전압 + PCS 다각형 (6차 확정)


**6차 개정 요지.** 부록 C의 "LinDistFlow 편입" 계획이 실측을 거쳐 확정·확장됐다. 계획은 *"전압만 LP가 보게 하고 손실은 계속 버린다(2차 효과)"*였으나, 20여 세션의 프로토타입 검증 결과 **손실도 QP(2차)로 목적함수에 편입**하는 것으로 확장됐다. 하위 LP는 이제 세 가지를 동시에 본다: **(a) 계통 손실(QP 5계수), (b) 전압(LinDistFlow μ 페널티), (c) PCS 겉보기전력 한계(다각형 근사).** 방식 선택(PWL vs QP)·기각 결정의 여정은 부록 C에 남긴다.

### 2-B.1 하위 LP의 정체 — LP가 아니라 QP다

이전 뼈대의 하위 문제는 조달비 대리(슬랙 유효전력)만 보는 **LP**였고, 손실은 목적함수에 없이 사후 조류계산으로만 측정했다(3절 원 설계). 6차부터 하위 문제는 **손실을 2차식으로 목적함수에 담는 QP**다. 그 근거:

- **Q는 손실을 통해서만 편익을 만든다.** Q가 슬랙 유효전력에 영향을 주는 유일한 경로가 손실인데, 손실이 목적함수에 없으면 Q를 최적화할 구동력이 없어 해가 degenerate가 된다(부록 C.1이 지적한 그 문제). 손실을 목적함수에 넣어야 Q가 의미 있게 결정된다.
- **손실은 (P,Q)의 2차 함수다.** 실제 선로손실 `I²R`은 주입 P·Q의 2차식으로 근사되며, 이 2차 곡면을 5계수로 적합하면 실측 대비 상대오차 0.02~0.07%로 재현된다(프로토타입 검증). 1차(선형) 근사로는 Q의 내부 최적해가 생기지 않고 코너로 붕괴하지만(옛 LHS 방식의 취약점), 2차항이 있으면 연속적인 내부 최적해가 존재한다.

### 2-B.2 손실 모형 — 5계수 QP

각 (버스 b, 시나리오 s, 시각 t)에서 계통 손실저감의 **비용 형태**를 다음 2차식으로 표현한다:

```
L_cost(P, Q) = a_P·P + a_Q·Q + b_PP·P² + b_QQ·Q² + b_PQ·P·Q
```

- `L_cost = −ΔL_reduction`. 즉 **손실비용**(손실저감의 음수)이며, 최소화 목적함수에 그대로 더한다.
- 부호 규약: `ΔL_reduction`(손실저감, 양수=좋음)이 오목(한계저감 체감)이므로 `L_cost`는 **볼록**이다. 1차계수 `a_P, a_Q`는 **음수**(P·Q를 늘리면 손실비용이 준다).
- **P·Q 모두 편입한다.** 초기에는 "P는 조달비가 이미 최적화하니 사후 측정, Q만 QP"를 검토했으나(부록 C 참조), 부호 규약을 바로잡은 재실험에서 완전한 (P·Q 5계수) 편입이 정상 운영점에서 j_net을 개선함이 확인됐다. `a_P·P + b_PP·P² + b_PQ·PQ`가 모두 목적함수에 들어간다.

**볼록성 (PSD).** 2차항 계수행렬 `H_cost = [[b_PP, b_PQ/2], [b_PQ/2, b_QQ]]`의 최소 고유값이 전 케이스(216개 실측)에서 0.012~0.024로 **PSD가 구조적으로 보장**된다. 따라서 `cp.quad_form([P_net, Q], H_cost)`가 볼록이고, 사영(psd_wrap)이 불필요하다.

**DPP·구현.** `H_cost`를 Parameter로 두면 DPP가 깨지므로(교차항 bilinear), **계수를 상수로 굽고** `quad_form`을 직접 사용한다. 계수가 (b, s, t)마다 다르므로 문제를 (b, S) 단위로 캐시한다(2-B.5).

### 2-B.3 전압 — LinDistFlow μ 페널티

전압은 부록 C.4-(1)의 결정을 그대로 채택한다. **표준 Baran–Wu 선형 모델**(`v = V_slack² − 2·Dᵀ(R∘P_e + X∘Q_e)`; 분모 `V²≈1`, 2차 손실항 무시)로 각 버스 전압(제곱)을 P·Q의 affine 표현으로 얻고, 하한·상한 초과분을 **목적함수 페널티**로 넣는다:

```
+ μ · Σ_{t,bus} [ max(0, v − 1.05) + max(0, 0.95 − v) ]
```

- 하드 제약이 아닌 페널티인 이유(infeasible 위험 회피, 근사오차가 가부를 뒤집지 않음)는 부록 C.4-(1)과 동일하다.
- `max(0, ·)`는 부분선형(볼록)이라 **문제를 QP 수준으로 유지**한다(2차로 만들지 않음). 즉 하위 문제 전체는 "2차 목적(손실) + 부분선형 페널티(전압) + 선형·다각형 제약"인 QP다.
- **최종 전압 판정은 여전히 사후 AC 조류계산 + 7절 λ_V 페널티**가 담당한다. μ는 "전압을 배려하는 스케줄을 유도"하는 항이지 제약 판정이 아니다.

### 2-B.4 PCS 겉보기전력 — 다각형 내접 근사, POLY_N=128

PCS 원 제약 `‖(P, Q)‖ ≤ S`를 다각형 내접으로 근사한다(부록 C.4-2와 동일 구조, 값만 갱신):

```
(P_dis − P_ch)·cos θ_k + Q·sin θ_k ≤ S·cos(π/N),   θ_k = 2πk/N,  k = 0..N−1
```

- **POLY_N = 128** (부록 C의 초기값 12에서 상향). 근거: POLY_N 스윕 실측(부록 C).
  - 반경오차 `1 − cos(π/128) = 0.030%`, PCS 비용 과소계상 0.27% — 오차가 `1/N²`을 따르며 잡음 하한에 도달.
  - j_net 수렴: N=64에서 이미 참값 대비 <500원, N=128에서 완전 수렴.
  - 16코어 full 환산 비용은 N=32 대비 +12초 수준으로 무시할 만하다(제약이 극도로 희소).
  - 다각형은 **전 각도 N면을 생성**하므로 Phase 2의 Q<0 확장에도 그대로 유효하다.
- QCP(`norm ≤ S`)로 두면 솔버가 SOCP로 분기해 느려지므로 다각형으로 LP/QP를 유지한다.

### 2-B.5 손실 계수의 생성과 재산출 — 최적화는 C0/X0, 재산출은 진단 전용

계수 `(a_P, a_Q, b_PP, b_QQ, b_PQ)`는 조류계산으로 측정한다. 문제는 **어느 운영점에서 측정하느냐**다. 계수는 국소 2차 근사이므로, 측정 중심점과 LP 해의 운영점이 다르면 오차가 생긴다(부록 C의 "괴리 B/C").

**측정 방법.** 특정 (버스 b, 중심 운영점 P_center, 시각 t, 시나리오 s)에서:
1. 기본 측정 그리드는 **min7 7점**(`LOSS_GRID_DESIGN="min7"`): 중심 `(P_center,0)`, P축 2점, Q축 2점, PCS 원 내부의 반대 대각 2점. 회귀 대조 시 `"full25"`로 바꾸면 종전 P 5점×적응형 Q 5점을 즉시 복원한다.
2. 각 격자점을 sgen으로 버스 b에 주입해 AC 조류계산 → 실제 손실 측정.
3. `ΔL(P,Q) = Loss(P=0,Q=0) − Loss(P,Q)`를 5계수로 최소제곱 적합 → `L_cost = −ΔL`.

**min7 채택 근거.** `scripts/probe_coef_grid_reduction.py`로 4버스×3용량×2시나리오×2시각×2중심점 = 96조건을 81점 조밀격자 참값과 대조했다. 손실면 baseline 정규화 오차는 중앙값 0.002%, 최악 0.054%로 종전 25점과 동급이고, PSD·`a_P/a_Q` 부호 게이트는 전 조건에서 통과했다. `b_PQ` 상대오차는 최악 23%이나 해당 계수의 절대값이 다른 2차계수의 약 1/11이라 손실면 영향은 무시 가능한 수준이다. 그리드 표본은 25→7점, 별도 baseline까지 포함한 실제 측정은 26→8회로 약 3.25배 줄어든다. dev gbest에서 C0/X0와 운영점 재산출 C1/X1의 연간 j_net 차이가 약 100원뿐임도 확인돼, p_center=0의 C0가 실제 방문 운영점까지 충분히 정확하다고 판정했다.

**PCS 경계 pull (확정).** 재산출 운영점이 정격에 걸려 `|P_center|/S=1.0`이면 min7의 P·Q 반경이 모두 0이 되어 rank=1, `a_Q=0`으로 축퇴한다. `scripts/probe_boundary_degeneracy.py`의 180조건 검증에서 축퇴는 frac=1.0의 36건에만 발생했고 frac≤0.98에서는 없었다. 따라서 S-grid 클립 후 `frac>LOSS_PULL_THRESH=0.95`이면 **측정 그리드 중심만** `sign(P_center)·LOSS_PULL_TARGET·S`, `LOSS_PULL_TARGET=0.85`로 당긴다. solve에 전달되는 연속 S, 실제 P_net과 재산출 참조 운영점은 바꾸지 않는다. 채택 조합은 7점 비용을 유지하면서 경계 손실면 오차 최악 약 0.036%(경계 p95 0.0362%)였다. Q계수를 0으로 두는 방식은 경계오차가 8.4%로 폭발해 기각했고, full25 fallback은 안전하지만 경계 측정비용이 증가해 차선으로 두었다. pull 후 중심을 계수 캐시 키에도 사용해 측정값과 키를 일치시킨다.

**최적화 경로 — 재산출 완전 제거 (확정).** `RECOMPUTE_ENABLED=False`가 기본이며 각 PSO 평가는 다음 한 번의 측정·solve만 사용한다:

```
(b, S, E) 입력
  → [측정 1차] P=0 기준 계수 측정 (캐시 키 (b,S)로 저장/재사용)
  → [solve 1차] QP 풀기 → 스케줄 X0
  → X0를 편익·전압·이연 평가의 최종 해로 전달
```

- **제거 근거:** dev(run2 gbest)에서 재산출의 연간 j_net 영향은 AVG N_WD 가중 약 84원과 PEAK 소액을 합쳐 약 100원, j_net 약 299만원의 0.003%였다. 이 크기에서는 조건부·시각별 임계 튜닝의 실익도 없어 완전 제거가 더 단순하고 방어 가능하다.
- **복원 스위치:** `RECOMPUTE_ENABLED=True`로 바꾸면 종전 C0→X0→C1→X1 최적화 경로를 즉시 복원한다.
- **진단 경로 유지:** run당 gbest의 `return_intermediates=True`에서는 스위치와 무관하게 C1/X1/C2/X2를 계속 계산한다. 최종 편익·schedule은 스위치에 따라 X0/X1을 선택하지만, `recompute_time_diag`와 X0/X1/X2 j_net 차이로 제거의 무해성을 지속 관측한다.

**캐시 전략.** 계수는 **E에 무관**하다(실측: 같은 (b,S)에서 E를 3배 범위로 바꿔도 5계수 전부 상대변동 0). 따라서 최적화는 p_center=0의 C0를 (b,S) 캐시로 공유하며, PSO가 E를 바꾸거나 같은 (b,S)를 재방문하면 재사용한다. 운영점 중심 C1/C2 캐시는 gbest 진단 경로에서만 사용한다.

### 2-B.6 하위 문제 전체 요약

하위 문제는 각 (버스, 시나리오)에서 다음 QP를 24시각에 대해 푼다:

```
minimize  Σ_t SMP[t]·(P_ch[t] − P_dis[t])·Δt              # 조달비 대리
        + Σ_t SMP[t]·L_cost(P_net[t], Q[t])·Δt            # QP 손실 (2-B.2)
        + μ·Σ_{t,bus} [max(0,v−1.05) + max(0,0.95−v)]     # 전압 (2-B.3)
        + (degeneracy 정규화)
subject to  SOC 재귀식, P_ch/P_dis ≤ S,
            다각형 PCS 제약 (2-B.4, POLY_N=128),
            LinDistFlow 전압 재귀식
```

- 결정변수: `P_ch[t], P_dis[t], Q[t]`, SOC, 전압 보조변수. **q_ratio 상위변수는 제거**됐다(PSO 차원 4n → 3n, 2절·부록 C.4-3 참조).
- PEAK 시나리오는 목적함수가 다르다(피크저감 + 이연). 3-C절에서 다룬다.

---

## 3. 목적함수

```
maximize J_net = B_energy + B_defer − Cost    [원/년]
fitness = −J_net + λ_V·Σ전압위반 + λ_line·Σ선로위반    (ALL_DAYS 검증)
```

### B_energy (손실 + 조달비 통합, SMP 기반) — AVG_DAYS만
```
B_energy = Σ_{s∈AVG} N_WD[s] × Σ_t [P_slack^base[s,t] − P_slack^ESS[s,t]] × SMP[s][t] × Δt
```
- 슬랙 유효전력 차이 × 시간별 SMP → **손실 편익 + 조달비 자동 합산(이중계상 없음)**.
- 항등식 `P_slack = Σ부하 + Loss − P_ESS` 에 의해 손실 감소분과 ESS 주입분이 모두 포착됨.
- **AVG_DAYS 3개만.** 최대일은 이연·제약 전용(최대일 데이터는 평균일 통계에 이미 녹아 있음 →
  최대일로 에너지를 또 세면 이중계상).

### B_defer (설비 이연, SMP 무관) — PEAK_DAYS, 단일 max
```
B_defer = c_cap × [max_{s∈PEAK,t} P_slack^base − max_{s∈PEAK,t} P_slack^ESS]
```
- **연중 최대 슬랙피크 1점**이 이연을 결정(여름최대일·겨울최대일 통합에서 단일 max).
- 일수가중 없음.
- **★ 음수 클리핑 금지 (확정).** ESS가 피크시각에 충전해 슬랙 피크를 키우면 `B_defer < 0`이
  된다. 이를 0으로 클립하지 말 것. 근거 둘:
  (1) **물리적 정당성** — 피크를 키우면 배전설비 투자를 앞당기는 실질 손해이므로 음의 편익이 옳다.
  (2) **탐색 지형** — 클립하면 나쁜 해가 PSO에게 "무해"로 보여 지형이 평탄해진다.
      PSO는 gradient가 아니라 **fitness 차이**로 움직이므로 평탄 영역은 방향을 잃는 구간이 된다.
- **후처리 기록:** 최적화 과정에서 음의 `B_defer`가 실제로 발생했는지 카운터로 기록한다(8절).
  한 번도 안 나왔다면 클리핑 여부가 결과에 무관했다는 뜻이고, 자주 나왔다면
  "PSO가 피크 증가를 회피하는 방향으로 수렴했다"는 서사가 된다. 어느 쪽이든 발표 재료.
- **★ 실측·해석 (12차 정정 — "n=1은 잡음" 서술 폐기):**
  | 기수 | `n_negative_bdefer` | `min_bdefer` | 해석 |
  |---|---|---|---|
  | n=1 | 24~86건/run | **최대 −539만원(12차 L-SHADE dev)** | ★ **실질 음편익.** 탐색경로의 열등해(bus16 지역해)가 만든다. 최적해(b=31)는 b_defer 양수 |
  | n=2 | 24~47건/run | **−272만원(12차 통합)** | ★ 실질 음편익. 2기 활성 시도 시 시스템 피크 기여로 b_defer 손실 |
  - **★ 12차 정정 (중요).** 종전 서술 "n=1의 음의 b_defer는 −1e-5 부동소수 잡음이라 실질은 n≥2에서만"은
    **낡은 값에 기반한 오류였다.** 실제로는 PSO 시절에도 n=1 일부 run에서 백만원 단위가 나왔고, 12차 L-SHADE
    dev에서 −539만원이 실측됐다. 이는 **최적해가 아니라 탐색경로가 통과한 열등해(bus16 등 지역해)가 만드는
    실질 음편익**이다. `min_bdefer`는 전 run 합산 최솟값이라, 어떤 run이 지역해를 거치면 큰 음수가 남는다.
  - **올바른 서사:** "최적해는 b_defer 양수(피크 저감)이나, 탐색 중 열등 배치는 피크를 키워 실질 음편익을
    만든다 → 클리핑 금지 결정이 그 열등해를 최적화기에게 '무해'로 위장하지 않게 한다"가 맞다. "n=1은 잡음"
    표현은 어디에서도 쓰지 말 것.

### Cost
```
CAPEX = 219,528.17 × E_rated[kWh] + 36,541.18 × S_rated[kVA]
OPEX  = 5,714.70 × E_rated[kWh] + 0.005564 × CAPEX
Cost  = CRF_20 × CAPEX + OPEX
```

### ★ 집계 방식이 항마다 다름 (이중계상 주의)
| 항 | 시나리오 | 가중 |
|---|---|---|
| 에너지 | AVG_DAYS | 일수가중 N_WD |
| 이연 | PEAK_DAYS | 슬랙피크 단일 max, 가중 없음 |
| 제약(페널티) | ALL_DAYS 전부 | — |

### ★ 자료구조 규약 (확정)
- **시나리오 데이터는 `dict[str, np.ndarray(shape=(24,))]`.** 키 = 시나리오명.
  배열 `(n_scen, 24)` + 인덱스 매핑을 쓰지 **않는다**.
- **근거는 확장성이 아니라 안전성이다.** (확장성은 배열도 동일 — `n_scen`만 늘리면 되므로
  dict의 차별점이 아니다.) 위 표대로 집계 대상이 AVG/PEAK/ALL 셋으로 갈리는데,
  **인덱스 방식은 어긋나도 조용히 잘못된 답을 낸다.** dict는 키가 틀리면 `KeyError`로 즉시 터진다.
  시나리오별 가중치 `N_WEEKDAYS`를 붙일 때도 dict가 안전하다.
- 성능은 무시 가능(시나리오 10개 순회 vs 조류계산 120회).
- **각 함수 진입부에 키 검증:** `assert set(data.keys()) >= set(AVG_DAYS)`
  (해당 함수가 쓰는 그룹 기준). 시나리오 확장 시 누락을 즉시 잡는다.
- 하드코딩된 `5`나 `3`을 쓰지 말고 `AVG_DAYS`/`PEAK_DAYS`/`ALL_DAYS`를 순회할 것.

### ★ 단위 규약 (확정) — benefits.py 내부는 MW/MWh 단일 단위
- **`benefits.py` 안에서 `×1000`이 단 한 번도 나오면 안 된다.**
- 근거: "경계에서 MW로 받고 함수 진입부에서 ×1000" 방식은 **변환 지점이 함수마다 반복**되어
  하나만 빠뜨려도 1000배 오차, 두 번 곱하면 검출이 어려운 오차가 난다.
- **대신 `params.py`에 MW 기준 상수를 미리 만들어 두고 `benefits.py`는 그것만 쓴다.**
  기존 kW/kWh 상수는 **출처 추적용으로 남긴다**(4절 파라미터 표와 대조 가능).
  ```python
  C_CAP_PER_KW_YR       = 76082.2                    # 원/kW-yr  (출처값, 감사추적용)
  C_CAP_PER_MW_YR       = C_CAP_PER_KW_YR * 1000     # 원/MW-yr  ← benefits.py는 이것만 씀
  C_KWH_CAPEX_PER_MWH   = c_kWh_capex * 1000         # 원/MWh
  C_KW_CAPEX_PER_MVA    = c_kW_capex  * 1000         # 원/MVA
  C_KWH_OPEX_PER_MWH_YR = c_kWh_opex  * 1000         # 원/MWh-yr
  SMP_PER_MWH = {s: np.array(v) * 1000 for s, v in SMP.items()}   # 원/MWh
  ```
  **새 상수는 반드시 기존 상수를 곱해서 정의**하고 숫자를 다시 타이핑하지 말 것(오타 시 두 값이 갈림).
- **변수명에 단위 접미사:** `p_slack_mw`, `c_cap_won_per_mw_yr`, `e_mwh`, `s_mva` 등.
- CAPEX 함수만 kW를 받게 하는 식으로 **함수마다 단위가 다른 구조는 금지**(호출부가 최악이 됨).

### ★ 슬랙 수지 검증 (확정) — atol 실측 유도
`benefits.py`는 항등식 `P_slack = ΣLoad + Loss − P_ESS`에 의존한다(B_energy가 손실과 조달비를
이중계상 없이 함께 잡는 근거이며, 8절 검산 `B_arb + B_loss ≈ B_energy`도 여기서 나온다).

**실측 결과 (`scripts/check_balance.py`, 확정 — 재확인 불필요):**
| 항목 | 결과 |
|---|---|
| `net.line.g_us_per_km` | 전 선로 0 |
| `net.shunt` | 빈 테이블 |
| `net.line.c_nf_per_km` | 0 (참고용) |
| 기저 잔차 | −1.78e-15 MW |
| `p_from+p_to` vs `pl_mw` | 5.00e-16 MW 차 |
| ESS 주입 −2.0~+2.0 MW 스윕 | 잔차 절대값 ≤ 7.1e-15 MW, **주입 크기 의존성 없음** |
| summer_peak 24h | 최대 절대잔차 4.44e-15 MW, 평균 1.81e-15 MW |

→ **shunt·컨덕턴스 성분이 없으므로 수지식에 추가할 항이 없다.** 잔차는 machine epsilon
  (~2.2e-16)의 수 배로 **순수 부동소수점 반올림 잡음**이며, 뉴턴법 수렴오차나 누락항이 아니다.
  (구조적 이유: pandapower의 `res_ext_grid.p_mw`와 `res_line.pl_mw`가 **같은 조류해에서
   대수적으로 파생**되므로 반올림 외에 어긋날 여지가 없다. shunt가 0이라는 사실이 이 관계의 전제.)

**확정: `np.allclose(..., atol=1e-7, rtol=0)`**
- **`rtol=0`을 반드시 명시**할 것. 기본값 `rtol=1e-5`를 그대로 두면 8.8 MW에서 허용오차가
  8.8e-5로 커져 의도한 절대기준이 무너진다.
- atol 선정 논리(두 방향 모두 봐야 함):
  - **위양성 없음:** 관측 잔차(~1e-15)보다 8자리 여유.
  - **위음성 없음:** 이 assert가 잡으려는 것은 **주입 불일치**이고, 그 실제 규모는
    kW~MW(≥1e-3)다. 1e-7이든 1e-5든 **검출력은 동일**하다. 즉 이 구간에서 atol을
    조이는 것은 검출력을 늘리지 않고 위양성 위험만 늘린다. (1e-9까지 조일 이유 없음.)
- **주입 불일치의 대표 경로 3가지** (assert가 잡으려는 대상):
  1. LP가 준 `P_net`의 **부호 규약**을 뒤집어 주입 (sgen은 발전 기준 양수, LP는 방전 양수)
  2. 다수기 설치 시 **버스별 분배** 오류
  3. pandapower 주입 시점의 **단위 변환**(MW vs kW) 오류
- **검증 위치: 결과 수준이 아니라 시간별·시나리오별.** `B_arb+B_loss≈B_energy`(결과 수준)가
  어긋나면 어느 시나리오 어느 시각인지 모르지만, 시간별로 보면 **어디서 깨졌는지 바로 나온다.**
- `enabled=False`로 끌 수 있게 할 것(`lower_lp._assert_physics`와 동일 패턴, 본실험 시 속도).
- **★ 한계 인식:** `test_benefits.py`의 검산 테스트는 **인위적 정합 데이터**로 산술만 검증한다.
  위 세 경로는 **실제 조류계산이 붙어야** 드러나므로, `evaluate.py` 단계에서 **실데이터로
  한 번 더** 이 검산을 돌려야 한다.
  → **✅ 완료.** 실데이터 검산 통과: `15,553,951 + (−2,992,772) ≈ 12,561,179`.
    세 경로(부호 규약·버스별 분배·단위 변환)가 모두 정상임이 확인됐다.
    (`b_loss`가 음수인 것은 정상 — LP가 조달비를 좇아 심야에 대량 충전하면 그 시각 손실이 는다.)

---

## 3-A. evaluate.py 설계 확정 사항

`evaluate.py`는 lower_lp + 조류계산 + benefits + 페널티를 엮는 **통합점**이다.
지금까지 순수함수로 격리해온 것들이 실제 계통과 처음 부딪히는 곳이라 버그가 나기 쉽다.

### 부하 규약
- **LP 입력 부하 = 손실 제외 총 유효부하** `ΣP_load[t]` (MW). 2절의 "부하단 피크" 서술과 정합.
- **부하 프로파일은 전 버스 동일 배율, P·Q 동일 배율**(1절 K 스케일링과 동일 논리, 역률 보존).
- **★ 이것은 가정이다.** 실제 배전망은 버스별 부하 형상(산업 vs 주거)과 역률이 다르다.
  이 가정 덕에 **`b*` 분포 해석이 전기적 거리·임피던스만의 함수로 깨끗해진다**
  (버스별 형상이 다르면 최적 위치가 형상 차이에 끌려간다). 코드 주석에도 명시.

### warm start 정책
- `pp.runpp(net, numba=True, init='results')`. **시나리오 경계에서도 리셋하지 않는다.**
- 근거(5절 실측 프로파일): 시나리오 경계 최악 변화는 winter_peak t=23(0.8213) → summer t=0(0.7405)로
  0.081인데, 이는 **한 시나리오 내 시각 간 변화보다 크지 않다**
  (summer_peak t=0→1이 0.081, t=8→9가 0.082). 즉 경계가 특별히 나쁜 지점이 아니다.
  또 flat start는 전압을 전부 1.0으로 놓는데 실제 해는 0.94까지 내려가므로 **오히려 더 멀다.**

### ★ 발산 처리 (PSO가 죽지 않게)
PSO는 경계 근처의 극단적 입자를 던지므로 `runpp`가 수렴 실패할 수 있다. 예외가 위로 튀면
평가가 죽고 PSO 전체가 멈춘다(7절: 입자 평가는 항상 스칼라를 반환해야 함).

- **재시도:** 1차 `init='results'` → 실패 시 2차 `init='flat'` → 둘 다 실패하면 발산 확정.
  - **★ `init='auto'`를 쓰지 마라.** 재시도 시점에는 방금 발산한 계산의 **오염된 `res_bus`**가
    남아 있고(전압이 튀거나 NaN), `auto`가 그걸 다시 집으면 **재시도가 무의미해진다.**
    `flat`은 그 오염을 명시적으로 끊는다. flat의 단점(수렴 느림)은 재시도 경로에서만
    발생하므로 비용이 사실상 없다.
- **발산 페널티: `PENALTY_DIVERGE = 1e15`** (params.py, 잠정값).
  - 근거: **정상 평가가 도달 가능한 최대 페널티보다 확실히 커야 한다.** 전압 위반량은
    ALL_DAYS 5시나리오 × 24시각 × 32버스 = **3,840항의 L1 합**이므로 버스당 평균 0.0003 pu만
    위반해도 1.0을 넘는다. 물리적 최대(전압붕괴 직전 버스당 ~0.5 pu)를 잡으면
    ~1,900 pu × λ_V(1e10) ≈ **1.9e13**. 1e15는 그 50배 이상이고 float64 한계에서 멀어 안전하다.
  - **`np.inf`를 쓰지 마라** — PSO 통계(평균·수렴곡선)가 오염된다.
  - ⚠️ 초기 검토에서 `λ_V × 1.0`(전압 1 pu 위반 상당)을 제안했으나 **기각**했다.
    위 계산대로 **정상적인 나쁜 해가 도달 가능한 범위**라 발산과 구분되지 않는다.
- **발산 로깅 (단순 횟수로는 진단에 부족):**
  1. 발산 입자의 `(b, S, E)` — 경계 근처에 몰리는지, 특정 버스에서만 나는지
  2. 발산이 난 시나리오와 시각 — 최대부하일 저녁에만 나는지
  3. 재시도(flat)로 살아난 비율 — 재시도 로직의 실효성 판단용
  - 해석: 발산이 **특정 영역에 몰리면** 탐색 경계가 물리적으로 무리한 구간을 포함한다는 신호,
    **산발적이면** 수치적 문제다. 첫 실행 후 `PENALTY_DIVERGE` 조정 근거로 쓴다.

### 구조
- **다수기 일반형.** (★ 6차: 아래는 현행 뼈대 구조다. 6차 QP 편입으로 q_ratio가 제거되어
  입자는 **3n 차원 `(b_i, S_i, E_i) × n기`**가 된다 — 2절·2-B. 본코드 구현 시 반영.)
  현행 뼈대 입자 `x`는 4n 차원 `(b_i, S_i, E_i, q_ratio_i) × n기`. **n은 x 길이에서 유도**
  (하드코딩 금지). 나중에 고치면 x 파싱·sgen 생성·버스 분배가 전부 바뀐다.
  각 기가 `solve_avg`/`solve_peak`를 독립적으로 풀고(10절 "독립최적화"), **조류계산에서만 만난다.**
  중복 배치는 (a)방식 — 같은 버스면 sgen을 2개 별도 생성(병합 안 함). 첫 실험은 n=1.
- **무효전력 (★ 6차 폐기 예정):** 현행 뼈대는 `Q[t] = q_ratio × sqrt(S_rated² − P[t]²)`를
  한 줄로 구현해 둔다. **6차 QP 편입에서 이 경로는 사라진다** — Q가 하위 QP의 시각별 결정변수가
  되어 다각형 PCS 제약으로 표현되므로(2-B.4), q_ratio·사후계산·4n 어댑터·8절-6 스윕이 모두 폐기된다.
  (아래 3차 갱신 서술은 뼈대 이력으로 보존.)
  - **★ 2026-07 3차 갱신 (뼈대 이력):** 뼈대 PSO는 q_ratio 차원을 탐색하지 않는다(3n, 7-A절).
    `evaluate`의 4n 시그니처를 유지하고 `main.py`가 `q=0.0` 열을 붙여 넘긴다.
- **★ 페널티는 `evaluate.py`에 둔다** (`benefits.py` 아님). 근거: 페널티는 편익이 아니라
  제약 위반 벌점이고(3절 fitness 식에서 별개 항), 전압·전류 배열이 필요한데 그것은
  조류계산 결과물이라 `benefits.py`의 "pandapower를 import하지 않는다" 원칙과 어긋난다.
- **반환:** `evaluate_particle(x) -> float` 스칼라 하나.
  `fitness = −J_net + λ_V·전압위반 + λ_line·선로위반`.
  **★ PSO는 최소화 엔진이므로 부호에 주의**(J_net은 최대화 대상).
  후처리·디버깅용 상세 결과(편익 분해·위반량·발산 로그·스케줄)는 별도 경로로 얻되,
  PSO 경로에서는 스칼라만 반환해 오버헤드를 피한다.

---

## 3-B. ★ 전압 페널티 지배 문제 (2026-07 3차 진단 → 4차 해소, λ는 조건부 유보)

`main.py` 첫 dev 실행(n_ess=1, 32입자 × 30세대 × 3run, 슬랙 `vm_pu=1.0` 시점)에서
**페널티가 순편익의 21배로 fitness를 지배**하는 것이 확인됐다. 이 절은 그 진단 기록이며,
**λ 재조정은 4차에서 "현 조건에서는 불필요"로 정리됐다**(아래 참조). 영구 확정이 아니며
재검토 트리거가 함께 규정돼 있다.

### 실측 (3 run 전부 사실상 동일)
```
j_net       = −1.6087e8 원   (b_energy=2.36e7, b_defer=6.08e7, cost=2.45e8)
v_violation = 0.334063 pu  → penalty_v = 3.3406e9 원   (λ_V = 1e10)
i_violation = 0.000000     → penalty_line = 0
fitness     = −j_net + penalty = 3.5015e9              ← 페널티가 95%
최적해       = b=9, S≈0.781 MVA, E=10.2 MWh (E는 상한에 붙음)
```

### 진단
1. **ESS는 위반을 줄이고 있었다.** 기저 위반 0.9169 → 잔여 0.3341로 **63.6% 저감.**
   즉 PSO 탐색은 정상 작동했고, 문제는 탐색이 아니라 목적함수 구조였다.
2. **잔여 0.334는 ESS가 제거할 수 없는 구조적 하한이다.** 3 run이 0.33406~0.33408로
   **소수 5자리까지 일치**했다 — 서로 다른 초기배치에서 출발해 같은 바닥에 닿았다는 뜻.
3. **결과: PSO가 경제성이 아니라 위반 최소화를 최적화했다.** 편익·비용 항(1.6e8)이
   페널티(3.34e9) 앞에서 노이즈가 되어, `E`가 상한 10.2에 눌어붙고
   `E/S ≈ 13시간`이라는 **비경제적 해**가 나왔다(6절 손익분기 한계 3.81h의 3배 이상).
   7절이 경고한 "λ가 너무 크면 경계에 과도하게 눌어붙어 탐색 경직"이 그대로 발현.
4. **λ를 낮추는 것만으로는 해결되지 않는다.** 제거 불가능한 상수 오프셋과 경제성 신호가
   **같은 비율로 줄어들** 뿐이라 신호 대 잡음비가 그대로다.

### 1차 조치 (완료): 슬랙 전압 1.02로 기저 위반 제거
위 오프셋의 근원이 기저 위반(0.9169 pu)이므로, 1절대로 슬랙을 1.02로 올려
**기저 위반을 0으로 만들었다.** 이로써 ESS가 위반을 "이미 위반 중인 계통에서 줄이는" 문제가
아니라 "정상 계통을 위반시키지 않는" 문제가 되어, 페널티가 원래 의도대로 작동한다.

**검토했으나 채택하지 않은 대안:**
- **(A) 기저 대비 증분 페널티** — `max(0, 위반 − 기저위반)`으로 ESS가 악화시킨 분만 벌한다.
  논리적으로 타당하고 오프셋도 사라지지만, **"기저가 이미 위반이라 봐줬다"는 설명을
  심사에서 계속 해야 한다.** 기저를 정상화하면 그 설명 자체가 불필요해진다.
  (슬랙 조정이 막혔을 경우의 대비책으로 기록해 둔다.)
- **(C) 위반을 페널티에서 빼고 후처리 보고 지표로 격하** — ESS가 전압을 **악화**시키는 해
  (대량 충전으로 강하)를 벌하지 못한다. 실제로 일어날 수 있어 방어가 필요하므로 기각.

### ★ 4차 정리 (미결 4건 해소 — 단 λ는 조건부)

**dev 재실행 결과 (슬랙 1.02, n_ess=1, 시드 고정으로 3차와 λ 외 조건 동일):**
```
gbest_f  = −3.05e6  (★ 음수 = J_net 양수 = 경제성 있음)
j_net    = 3.05e6 원/년
b_defer  = 1.145e7 (84%)   b_energy = 2.12e6 (16%)   cost = 1.051e7
v_violation = 0  → penalty = 0
최적해    = b=32/15, S≈0.176 MVA, E≈0.419 MWh, E/S=2.38 (유효 2.14h)
```
- **페널티 지배 해소 확인.** fitness가 순수 `−J_net`이 되어 PSO가 경제성만 최적화한다.
- **미결 4 해소:** `E`가 상한에 붙는 현상이 사라졌다(10.2 → 0.419). 페널티 탓이 맞았으므로
  **6절 E_rated 상한 재검토는 불필요.** E/S=2.38은 6절 손익분기 한계 3.81h 아래로 정합적.
- **미결 1 해소:** 잔여 위반 0.

**미결 2 (λ_V 재조정) → 현 조건에서 조정 불필요.**
> ⚠️ **이것은 "λ_V가 영구 확정됐다"는 뜻이 아니다.** 아래는 **현재 조건**
> (n=1 단일기 / 뼈대 Q=0 / 슬랙 1.02 / dev 탐색 범위)에서의 관측이며,
> 조건이 바뀌면 재검토 대상이다. 재검토 트리거는 이 항목 말미에 열거한다.

`scripts/probe_large_solutions.py` 48조합 그리드 실측:

| 영역 | S 범위 | v_violation | j_net |
|---|---|---|---|
| **경제적 영역** | S < 0.25 | **0** | **> 0** (최대 3.05e6) |
| 위반 영역 | S ≥ 1.5 | 0.0063 ~ 0.536 pu | **≤ −3.86e7** (전부 큰 폭 적자) |

- **두 영역이 완전히 분리되어 있어 λ_V가 최적해 선택에 개입할 여지가 없다.**
  위반이 발생하는 48조합 중 20개는 **전부 j_net이 이미 음수**이고, 가장 덜 나쁜 경우도
  −5.95e7원이다. 페널티가 "양수 j_net을 뒤집는" 상황은 그리드 전체에서 한 번도 없었다.
- **따라서 λ_V는 안전장치로만 기능한다.** 7절이 우려한 두 실패모드(너무 크면 탐색 경직,
  너무 작으면 무력)가 **둘 다 해당 없음**이 실측으로 확인됐다.
- **페널티 계산 경로는 살아 있다**(20조합에서 v_violation > 0, 최대 0.536 pu).
  회귀 방어는 `test_base_violation_at_legacy_slack`(현 부하·구 슬랙 1.0에서
  0.3781163378 pu)이 담당한다.
- ⚠️ **더 촘촘한 그리드로 "경계 영역"을 찾자는 제안은 (현 조건에서) 기각한다.** 한계 j_net이
  이미 S=0.176~0.25에서 음수로 전환되므로(6-A절), S=1.3~2.0 구간은 "j_net이 아직 양수 근처"인
  영역이 아니라 **훨씬 더 적자인** 영역이다. 찾으려는 경계가 이 조건에서는 존재하지 않으므로
  더 촘촘히 재도 같은 결론이 나온다. **단 이는 현재 편익·비용 파라미터에 의존한 관측이지
  모델의 불변 성질이 아니다** — 아래 트리거 중 하나라도 발생하면 이 판단부터 다시 볼 것.

### ★ λ 재검토 트리거 (하나라도 해당하면 위 판단을 재확인할 것)
| 트리거 | 왜 바뀔 수 있는가 |
|---|---|
| **full 실행 (100세대 × 30run)** | dev보다 훨씬 넓게 탐색하므로 경계 근처 해를 만날 확률이 오른다. `runs.csv`의 `v_violation`/`penalty_v` 컬럼이 30 run 내내 0인지 **반드시 확인할 것** |
| **다수기 n≥2** | 위 그리드는 n=1 단일기로만 확인했다. 여러 기가 같은 구간에 몰리면 위반 지형이 다를 수 있다 |
| **하위 QP 편입 (6차 완료)** | Q가 LP 변수가 되어 ESS가 무효전력으로 전압을 지지 → 위반 지형이 바뀐다. 또 **μ(LP 전압 페널티, MU_VOLT)가 λ_V와 갖는 상대 스케일**이 논점 — 구현 시 확인(2-B.3·부록 B) |
| **Phase 2 (태양광 침투)** | 역조류로 **상한(1.05) 위반**이 발생한다. 현재 관측은 전부 하한 위반 영역이며, 상한은 겪어본 적이 없다 |
| **편익·비용 파라미터 변경** (c_cap, Lazard 단가, SMP 등) | 경제적 영역의 S 범위가 이동해 위반 영역과 겹칠 수 있다 |
| 슬랙 전압·계통 설정 변경 | 기저 전압 여유가 바뀌면 위반 시작 지점이 이동한다 |

**미결 3 (λ_line) → 현 조건에서 조정 불필요.** `i_violation = 0`이 1절 예상
(최대 이용률 0.6244)과 일치. 단 **Phase 2 역조류에서는 선로 이용률이 오를 수 있어**
위 트리거가 동일하게 적용된다.

---



## 3-C. 손실 편익의 목적함수 편입 — B_energy·B_defer에의 QP 반영 (6차)


**3절과의 관계.** 3절은 편익의 **정의**(B_energy = 조달비+손실, B_defer = 이연, Cost)를 다룬다. 6차에서 하위 QP가 손실을 목적함수로 보게 되면서(2-B), 그 손실항이 두 편익에 **어떻게 들어가는가**가 새 논점이다. 3-C는 그 편입 방식 — 부호 규약, AVG와 PEAK의 차이, 피크일 pk 제약의 1차항 — 을 확정한다. 편익의 **최종 측정**은 6차에서도 사후 AC 조류계산이며(변경 없음), 3-C가 바꾸는 것은 "LP가 편익을 만드는 스케줄을 어떻게 찾는가"뿐이다.

### 3-C.1 원 설계와의 차이 — "사후 측정"에서 "목적함수 유도 + 사후 측정"으로

3절 원 설계는 *"LP는 조달비 대리로 스케줄을 짜고, 손실 편익은 사후 조류계산으로 측정(결과 추종)"*이었다. 근거는 "손실 편익이 ~1%라 명시적 최적화 이득이 미미"였다. 6차는 이를 **비대칭적으로** 수정한다:

- **여전히 유효한 것:** 편익의 **최종 값**은 사후 AC 조류계산으로 측정한다. LP 목적함수의 손실항은 근사(QP 5계수)이므로, 그 값을 편익으로 쓰지 않는다. B_energy·B_defer는 6차에서도 실제 슬랙 전력으로 계산된다.
- **바뀐 것:** LP가 **손실을 보고 스케줄을 짠다.** 원 설계는 손실을 안 보고 조달비만으로 스케줄을 정했으나, 그러면 Q가 degenerate가 되고(2-B.1), Q의 손실저감 편익을 활용하는 스케줄을 못 찾는다. 6차는 손실을 목적함수에 넣어 **LP가 손실을 줄이는 더 나은 스케줄을 고르도록 유도**한다.

**핵심 구분(반복 강조).** LP 손실항의 목적은 "손실을 정확히 예측하는 것"이 아니라 "실제 편익을 개선하는 스케줄을 고르게 하는 것"이다. LP의 손실 예측이 부정확해도(부록 C의 괴리), LP가 고른 스케줄이 실제 AC에서 더 나은 편익을 내면 그것으로 충분하다. 실측으로 확인됐다: 손실항을 넣은 스케줄이 안 넣은 스케줄보다 실제 AC 슬랙피크를 낮췄다(이연 개선).

### 3-C.2 부호 규약 — 세 번 전파되며 두 번 뒤집힌다 ★

계수 부호가 계수→AVG 목적함수→PEAK 제약으로 전파되며 방향이 바뀐다. **이 지점이 구현 실수를 부른다**(6차 개발 중 실제로 부호 버그가 두 세션을 오염시켰다). 규약을 명시한다.

**계수 (2-B.2):** `L_cost = −ΔL_reduction`. 1차계수 `a_P, a_Q < 0`(손실저감 방향).

**AVG 목적함수:** `L_cost`를 **그대로 더한다**(최소화). `min ( ... + Σ SMP·L_cost·Δt )`. `L_cost`가 손실비용(볼록)이므로 최소화가 손실저감을 추구한다. 별도 부호 반전 없음.

**PEAK pk 제약:** 여기서 **한 번 뒤집힌다.** 손실저감 `ΔL = −(a_P·P + a_Q·Q)`(양수)가 슬랙 피크를 낮추므로:
```
pk ≥ load[t] − P_net[t] − loss_reduction[t],   loss_reduction = −a_Q·Q − a_P·P
```
`a_P, a_Q < 0`이므로 `loss_reduction = −a_Q·Q − a_P·P > 0`(방전 P>0, Q>0에서). 즉 저감분만큼 pk 우변이 작아진다.

**★ 구현 검증 (필수, 7차 정정 — P항만 검증한다).** 부호 버그를 잡는 assert는 **P 손실저감항만** 본다: 방전 시각(`P_net>tol`)에서 `−a_P·P_net > −tol`이어야 한다. 부호가 반대로 들어가면 LP가 "방전하면 피크가 오른다"고 잘못 믿어 방전을 줄이고, 실제 슬랙피크가 **높아져 이연이 악화**된다(6차 부호 버그의 실제 증상).
> **★ 왜 loss_reduction 전체가 아니라 P항만인가 (7차).** 초기엔 "방전 시각에서 loss_reduction 전체 > 0"을 검증하려 했으나 이는 **틀렸다.** Q는 음수(무효전력 흡수)일 수 있고(다각형 PCS가 전 각도 허용, Phase 2 대비), 그러면 `−a_Q·Q`가 음수가 되어 loss_reduction 합이 **정상적으로 음수**가 될 수 있다(그 시각 Q 흡수가 손실을 늘리는 게 물리적으로 옳음). 전체를 검증하면 이 정상 케이스에서 오탐이 난다. 부호 버그가 뒤집는 것은 P 손실저감항이므로 그것만 본다. Q항 계수의 부호 정합성(`a_Q<0`)은 **측정 직후**(loss_coeffs strict assert)가 담당한다. 검증은 P항으로 좁히되, pk 제약 **자체**에는 loss_reduction 전항(Q 포함)이 들어간다 — Q<0이면 그 시각 pk 우변이 커지는 게(피크 저감이 덜 되는 게) 물리적으로 옳다.

### 3-C.3 AVG_DAYS — 손실 2차항 전체

AVG 목적함수는 `L_cost`의 **2차항 전체**를 담는다:
```
min  Σ_t SMP[t]·(P_ch−P_dis)·Δt
   + Σ_t SMP[t]·[ a_P·P_net + a_Q·Q + quad_form([P_net, Q], H_cost) ]·Δt
   + μ·(전압 페널티)
```
- 2차항이 목적함수(최소화)에 있으므로 볼록 유지(H_cost PSD). Q·P의 내부 최적해가 존재한다.
- `SMP[t]·Δt`를 곱하는 이유: 손실저감(MW)을 슬랙에서 조달하니 조달비와 같은 단위(원)로 환산. 3절 B_energy의 "슬랙 유효전력 × SMP" 논리와 정합.
- 이 스케줄이 만드는 실제 손실 편익은 사후 AC로 B_energy에 반영된다(3절 정의 불변).
- 손실면 계수는 2-B.5의 **min7 7점** AC 측정으로 생성한다. 96조건·81점 참값 대조에서 최악 오차 0.054%였고, `LOSS_GRID_DESIGN="full25"`로 종전 그리드를 복원할 수 있다. 교차항 `b_PQ`의 큰 상대오차는 작은 절대크기에 따른 착시이며 손실면 오차 기준으로는 동급이다.
- min7의 정격 경계 축퇴는 **측정중심 pull**(`LOSS_PULL_THRESH=0.95`, `LOSS_PULL_TARGET=0.85`)로 막는다. 이동 대상은 AC 계수 측정 그리드뿐이며 solve·실제 운영점은 불변이다. 180조건 경계 probe에서 7점 비용을 유지하며 최악 오차 약 0.036%였다.

### 3-C.4 PEAK_DAYS — pk 제약에 손실 1차항, 2차는 넣지 않는다

PEAK 시나리오는 목적함수가 `pk`(피크저감)이고, 손실은 **제약**으로 들어간다(3-C.2의 pk 식). 여기서 손실은 **1차항만** 넣는다:

- **왜 1차만인가:** pk 제약은 부등식 `pk ≥ (우변)`이다. 우변에 2차 손실을 넣으면 `pk ≥ (볼록함수)`가 되어 **실행가능영역이 비볼록**이 된다(볼록함수의 상위집합은 비볼록). 1차항은 우변을 선형으로 유지한다.
- **2차 손실 최적화는 AVG 목적함수(3-C.3)에서만** 이뤄진다. PEAK의 비구속 시각(피크가 아닌 21~22시각)의 Q·P는 AVG와 동일한 조달비·손실 목적으로 최적화되고, 구속 시각(피크 2~3개)만 pk 제약의 1차 근사를 받는다.
- **1차 근사 잔차(괴리 C)는 작고 무해하다.** 손실이 슬랙피크의 ~3.5%이고, 그 손실을 1차로 근사하는 2차 잔차는 다시 그 일부다. 그리고 이 잔차는 편익을 오염시키지 않는다 — 이연은 LP의 pk가 아니라 사후 AC 슬랙피크로 계산되기 때문이다(3절·2절 구조적 강제). 계수 재산출(2-B.5)이 운영점에서 1차항을 정확하게 만들어 이 잔차를 더 줄인다.

### 3-C.5 피크일 손실저감의 이중 기여 — 회계 변경 (★ 7차 구현·확정)

**★ 7차에서 구현·검증 완료.** 아래 설계 방향이 그대로 구현됐고, 세부는 이 소절 말미 "7차 확정 사항"에 정리한다.

현 회계는 에너지=AVG_DAYS, 이연=PEAK_DAYS로 분리한다(3절). 그런데 피크시각의 손실저감 `ΔL`은 **두 편익에 동시에 기여**한다:
- 이연: 슬랙 피크를 `ΔL`만큼 낮춤 → `c_cap·ΔL`.
- 에너지: 그 시각 슬랙 조달을 `ΔL`만큼 줄임 → `SMP·ΔL`.

이는 **이중계상이 아니다.** 같은 `ΔL` MW가 서로 다른 두 시장(설비 이연 시장의 연간화 단가 `c_cap`, 전력 도매시장의 `SMP`)에서 각각 값어치를 가진다. 전력 한 덩어리가 "지금 안 사서 좋고(에너지) + 그래서 증설도 미뤄서 좋다(이연)"는 별개 편익이다.

**현 회계는 이 에너지 겹을 누락한다** — 피크일의 에너지 편익 `SMP·ΔL`이 어디서도 계산되지 않는다(PEAK는 이연만, AVG는 피크일을 안 봄). 변경안:
- 피크일이 확정된 계절의 평균일 가중을 1일 줄이고(예: summer 63.6 → 62.6), **그 피크일의 (에너지 편익 + 이연 편익)을 원 단위로 별도 가산**.
- 두 피크 시나리오를 각각 풀어 max 하나로 이연 결정. 목적함수는 시나리오별 독립.
- 피크일 손실저감은 pk 제약(1차항, 3-C.4)을 통해 이연에 기여하고, 그 스케줄의 에너지 편익은 사후 AC로 계상한다.

**N_WEEKDAYS 처리 (헷갈리지 않도록 명시):** `N_WEEKDAYS` 딕셔너리 자체는 **불변**이다(합 247.0/년 유지). 회계 변경은 딕셔너리를 고치는 게 아니라, **최종 편익 계산 단계에서 피크일이 확정된 계절의 가중치를 하루 줄이고 그 피크일의 편익을 별도 가산하는 추가 처리**로 구현한다.

이 변경은 3절의 "PEAK=이연만" 원칙을 개정하는 사안이므로, full 실행 시 (a) 구현 (b) 이연 지배 시 에너지 무시 위험 검증을 거쳐 확정한다.

**★ 7차 확정 사항 (구현 시 정밀화된 결정):**
- **확정 피크일 = ess 슬랙피크가 최대인 PEAK 시나리오.** 그 하루를 통째로 (에너지+이연) 평가하고, 대응 계절(`PEAK_TO_SEASON={'summer_peak':'summer','winter_peak':'winter'}`) 평균일 가중을 −1일. 나머지 피크 시나리오는 버린다(그 계절 가중은 불변). shoulder는 대응 피크일 없어 항상 불변. tie-break는 PEAK_DAYS 순서 앞선 것.
- **★ "(에너지+이연) 별도 가산"의 정확한 뜻:** 이연을 두 번 세지 않는다. `b_defer`는 **변경 없이 통합 max** 유지(`c_cap·(max_PEAK base − max_PEAK ess)`, base/ess max가 다른 피크일에서 나와도 무방 — 이연은 연중 최대 피크량만 중요, 계절·날짜 무관). "확정 피크일" 개념은 **오직 에너지 겹을 어느 하루치로 더할지**에만 쓰인다. 신규 항의 실질은 피크일 에너지 편익 하나이며, 이연은 기존 b_defer가 이미 포착한다.
- **★ b_arb/b_loss 검산 기준 = 총편익 (나) 확정.** b_arb/b_loss의 역할은 검산(`P_slack=ΣLoad+Loss−P_ESS` 항등식 검증)이지 관측 지표가 아니다(관측은 8절 지표). 검산은 커버리지가 넓을수록 엄밀하므로 **`b_arb_total+b_loss_total=b_energy_total`** (AVG 조정가중 + 확정 피크일 1일)로 검산한다. 기존 AVG 기준 `b_arb/b_loss`(=b_energy_avg)도 함수로 보존(회귀·비교용). `evaluate.decomposition_ok`는 total 기준.
- **에너지 무시 위험 실측(공백 4):** dev 예비 관측상 피크일 편익은 이연이 지배(≈99.8%), 에너지 겹은 작고 **음수일 수 있다**(피크일은 이연 목적으로 다른 시각 충전 → 그 하루 에너지 손해). 무시하면 j_net 과대평가라 넣는 게 옳다. 최종 실측은 dev/full로 8차 확정.

### 3-C.6 회귀 기준

6차 편입이 기존 검증 자산(편입 전 LP 결과)을 오염시키지 않았는지 확인하는 회귀 기준은 **부록 A**에 둔다(별도 테스트 파일 `test_qp_regression.py`로 관리). 핵심: `force_q_zero=True`에서 편입 전 대비 P_net 차이가 P 손실 기여분(~2,100~2,600원 규모)으로 한정, Q 손실항 on/off 단조성, 프로토타입 재현. 상세는 부록 A.

---

## 4. 확정 파라미터

### ★ CRF 두 개를 반드시 구분 (합치지 말 것)
| 용도 | CRF | 근거 |
|---|---|---|
| **편익·비용 연간화** (Cost = CRF·CAPEX + OPEX) | **0.076876** (4.5%, **20년**) | ESS 달력수명. Lazard가 열화를 O&M 증강으로 반영 → 사이클수명 아닌 달력수명 20년 지배 |
| **설비 이연 단가 c_cap 산출** | **0.061392** (4.5%, **30년**) | 회피 대상인 **배전설비**의 내용연수 30년. (ESS 20년 아님 — 회피 대상은 ESS가 아니라 배전설비) |
> 두 편익이 서로 다른 시간지평으로 연간화되지만, **연간화 값이라 합산 정당**
> (연간화의 목적이 서로 다른 수명 투자를 연당 등가액으로 환산하는 것). 20년 통일하면 이연이 틀려짐.

### 비용 (Lazard LCOS v10.0, 2025.6, Utility-Scale Standalone 100MW/200MWh, Low)
- 환율 1,405.43 원/$ (2024.7~2025.6 LTM)
- c_kW = $26/kW → **36,541.18 원/kVA**
- c_kWh = $142/kWh × 1.10(오버빌드10%) = $156.2 → **219,528.17 원/kWh**
- O&M = $3.0/kWh-yr, 에스컬레이터 2.5%/yr → 레벨라이즈드 5,195.18 × 1.10 = **5,714.70 원/kWh-yr**
  (등비수열 합 Σq^t, t=1..20, q=1.025/1.045 = 16.4287)
- 보증 = CAPEX의 0.65%/yr(3년차부터) → 레벨라이즈드 **0.5564%/yr**
- 수명 20년, DoD 90%(SOC 5~95%), **RTE 90%(η_c=η_d=0.9487)**, 자기방전 0.067%/day
  - **RTE 90% 근거 (6차):** SUNSYS HES L datasheet(Socomec, AC/AC Round Trip Efficiency 90%, CATL EnerOne LFP·계통급 상용 ESS) + MDPI Energies 논문 *"Grid-Connected PV and Battery Energy Storage Systems: A MILP-Based Economic Sensitivity Analysis for the Education Sector"* (doi:10.3390/en19071803, 실측 왕복효율 90.3%). AC-AC 왕복효율이며 PCS 손실을 포함한다.

**PCS 효율 (6차 신설, 무효전력 경로): `ETA_PCS = 0.975`.**
- **적용 대상:** 무효전력이 PCS를 단방향 통과할 때의 변환손실. `Loss_pcs = (1−ETA_PCS)·(√(P²+Q²) − |P|)` — Q 전용 증분항이다. P 경로 손실은 RTE 90%(AC-AC 왕복)에 이미 포함되므로 이중계상하지 않는다.
- **RTE와의 구분:** `ETA_PCS`(단방향 통과, 0.975)와 충방전 왕복 `η_c·η_d`(RTE 0.90)는 별개 상수다. 코드에서 두 경로는 분리돼 있다.
- **근거:** Power Electronics Freesun HEM datasheet, CEC 효율 97.53% (including MV transformer). 변압기를 포함한 계통주입 효율이며(무효전력 경로가 PCS+변압기를 거쳐 계통에 주입되므로 이 값이 맞다), 97.53%를 보수적으로 0.975로 채택한다. CEC(캘리포니아 가중평균)는 ESS의 상대적 고부하 운전 패턴에 Euroeta(저부하 가중)보다 가깝다.

**★ 무효전력 편익 구조의 견고성.** PCS 효율이 낮아지면 무효전력의 한계비용이 커져 Q 최적값이 줄지만 **붕괴하지 않는다.** 6차의 QP(2차) 정식화 덕분이다 — 옛 LHS 선형 근사에서는 손실저감과 PCS 손실이 둘 다 Q의 1차 항이라, 한계비용이 임계(≈96%)를 넘으면 Q가 코너로 붕괴했다. QP 2차항은 내부 최적해를 보장해, 효율이 바뀌어도 Q가 연속 이동할 뿐이다. 따라서 ETA_PCS=0.975는 구조적으로 안전하다.
- Lazard O&M에 증강(augmentation) 포함 → 연간 용량감소율 별도 모델링 안 함.

### 편익
- **c_cap = 76,082.2 원/kW-yr** (설비 이연, 시스템평균 회피비용).
  산출(정확 입력값, 표시식의 반올림값과 다름): 배전 최대전력관련예산 2,505,145,200,000원
  ÷ 피크증가(28.3 GW / 14년 = 2,021.43 MW/년) × CRF_30(0.061391542475) = 76,082.20317... → **76,082.2**.
  (표시식의 2,505,145 백만원·2,067 MW·CRF는 반올림 표기이고, c_cap 자체는 반올림 없이 산출.)
  근거: 2026년 확정 배전 예산 + 11차 전력수급기본계획 최대전력 증가율(동일 계획체계 대응).
- SMP: 시간별 대표일 프로파일(5절).
- 할인율 4.5%.

### 제약
- 전압 0.95~1.05 pu — **출처 ANSI C84.1 / IEEE 계열**(한국 전기사업법 시행규칙의 표준전압
  허용오차는 저압 수용가 단자전압 기준이라 22.9kV 배전선로에 직접 적용되지 않는다. 1절 참조)
- PCS `sqrt(P²+Q²) ≤ S_rated`
- SOC 5~95%, SOC[24]=SOC[0](에너지중립, 사이클횟수 제한 안 함), SOC[0]=50% 고정

---

## 5. 부하 & SMP 프로파일 (하드코딩)

**5절 변경 최소.** 부하·SMP 프로파일(하드코딩 값)은 6차에서 불변. 스케일 계수만 1절(K_P=2.557201, K_Q=1.357609)을 참조하도록 갱신. `N_WEEKDAYS` 딕셔너리도 **불변**(합 247.0/년); 피크일 회계 변경(3-C.5)은 딕셔너리 수정이 아니라 최종 편익 계산 단계의 추가 처리이므로 5절에 영향 없다.

```
# 출처: KPX 시간별 전국전력수요 & 육지SMP, 2021~2025
# 계절: 여름6-8 / 겨울 당해1,2,12 / 간절기 나머지. 평일만·공휴일 제외(holidays.KR)
# 평균일: 연도별 시간대평균→5년평균 / 최대일: 부하 상위5일→5년평균
# 시각 t=0..23, '24시'=t=0 (np.roll(v,1))
# 부하 정규화 1.0 = 여름최대일 t=18 = 91,957 MW = case33bw 기본부하
# ※ 평일만 사용 근거 (9절 참조): 편익을 낮추려는 게 아니라 "전형적 운영일"을 평가범위로 정의한 것.
#   주말·공휴일은 수요패턴이 질적으로 다른 별개 모드라 평일과 섞으면 어느 쪽도 대표 못 하는 혼합일이 됨.
#   이연은 평일 피크가 지배(주말 불변). Phase 1·2를 동일 평일기준으로 비교하므로 비교 일관성 확보.

LOAD = {
  'summer':      [0.7405,0.6787,0.6522,0.6360,0.6263,0.6256,0.6358,0.6685,0.7203,0.7873,0.8248,0.8366,0.8317,0.8124,0.8438,0.8599,0.8687,0.8842,0.8827,0.8776,0.8713,0.8478,0.8086,0.7732],
  'winter':      [0.7737,0.7217,0.6961,0.6814,0.6754,0.6809,0.7003,0.7414,0.8056,0.8810,0.8863,0.8662,0.8355,0.7899,0.8166,0.8307,0.8455,0.8674,0.8670,0.8552,0.8361,0.8099,0.7828,0.7739],
  'shoulder':    [0.6857,0.6318,0.6094,0.5974,0.5923,0.5967,0.6134,0.6459,0.6868,0.7248,0.7261,0.7147,0.6941,0.6649,0.6959,0.7142,0.7315,0.7552,0.7621,0.7672,0.7617,0.7407,0.7172,0.7016],
  'summer_peak': [0.8091,0.7284,0.6971,0.6775,0.6656,0.6644,0.6763,0.7141,0.7771,0.8593,0.9100,0.9291,0.9303,0.9155,0.9517,0.9706,0.9816,0.9998,1.0000,0.9929,0.9834,0.9506,0.9010,0.8532],
  'winter_peak': [0.8219,0.7697,0.7439,0.7285,0.7203,0.7236,0.7418,0.7829,0.8517,0.9439,0.9725,0.9682,0.9488,0.9055,0.9306,0.9409,0.9489,0.9634,0.9519,0.9255,0.8976,0.8649,0.8329,0.8213],
}
SMP = {  # 원/kWh (절대값, 정규화 안 함)
  'summer':      [130.62,122.52,111.95,107.47,104.36,104.35,107.59,114.98,127.23,134.50,138.16,140.85,140.59,137.70,142.31,145.06,145.39,143.23,141.50,140.33,140.02,137.43,135.17,133.37],
  'winter':      [151.61,143.16,132.98,129.41,128.40,130.18,132.99,140.92,155.88,171.35,177.93,172.76,168.65,160.75,164.01,165.60,167.93,168.53,166.68,164.82,162.31,158.05,152.64,150.50],
  'shoulder':    [142.46,133.73,122.25,116.75,114.38,116.86,124.30,132.21,143.59,153.46,156.95,155.97,152.39,145.46,149.80,152.34,154.47,154.67,154.25,154.81,153.91,150.45,147.35,144.91],
  'summer_peak': [132.98,127.95,117.96,113.83,109.52,108.99,112.96,121.14,131.19,135.09,139.10,141.38,143.69,140.69,146.90,150.81,152.15,147.89,146.66,146.01,144.19,141.65,138.64,133.97],
  'winter_peak': [169.12,154.23,144.06,140.86,140.12,140.76,143.31,152.79,167.44,182.48,195.23,189.12,184.09,176.98,180.11,182.19,187.28,182.43,180.74,177.25,175.11,173.18,167.90,164.63],
}
AVG_DAYS  = ['summer','winter','shoulder']
PEAK_DAYS = ['summer_peak','winter_peak']
ALL_DAYS  = AVG_DAYS + PEAK_DAYS

# N_WEEKDAYS: 가중치 계산 전용, 정확값(반올림 안 함). 합 247.0/년.
N_WEEKDAYS = {'summer': 63.6, 'winter': 60.8, 'shoulder': 122.6}
```
> ※ 따로 첨부됐던 SMP(winter_peak 250~308원 등)는 산출기준이 다른 옛 버전 → 폐기. 위 값이 확정.

---

## 6. 손익분기 부등식 & E_rated 상한 재산정

### 손익분기 부등식 (조달비 포함, 근사식)
설비밀도(용량/전력 = E/S = x)당 이연+조달비 편익이 저장비용을 넘는 조건:
```
c_cap·η_peak + γ·DoD·x  >  (CRF+w)·(c_kWh·x + c_kW) + OM·x
```
- 좌변: 이연(c_cap·η_peak) + 조달비(γ·DoD·x). 우변: 연간화 CAPEX + O&M.
- η_peak=1.0(상한), γ = 왕복 순차익[원/유효kWh-yr] = Σ_s NWD[s]·(SMP_high·η − SMP_low/η).
등호에서 E/S 상한:
```
x_max = [c_cap·η_peak − (CRF+w)·c_kW] / [(CRF+w)·c_kWh + OM − γ·DoD]
```

### x_max 계산 (항별 수치, 1사이클/일·왕복효율 반영)
| 요소 | 값 |
|---|---|
| CRF+w | 0.076876 + 0.005564 = **0.08244** |
| 분자 = c_cap·1.0 − (CRF+w)·c_kW | 76,082.2 − 0.08244×36,541.18 = 76,082.2 − 3,012.45 = **73,069.75** |
| γ (조달비, AVG_DAYS·NWD가중) | summer 1,776.9 + winter 2,034.3 + shoulder 3,473.7 = **7,284.91** 원/유효kWh-yr |
| 분모 = (CRF+w)·c_kWh + OM − γ·DoD | 18,097.90 + 5,714.70 − 6,556.42 = **17,256.18** |
| **x_max** | 73,069.75 / 17,256.18 = **4.2344 kWh/kVA** |
| 손익분기 한계 지속시간 = x_max·DoD | 4.2344 × 0.90 = **3.811 h** |
> γ 각 시나리오: 순차익(원/유효kWh/일) = SMP_high·0.9487 − SMP_low/0.9487.
> summer 27.94(low104.35/high145.39), winter 33.46(128.40/177.93), shoulder 28.33(114.38/156.95).
> (참고) 조달비 제외 시: x_max=3.069, 지속시간 2.76h. 왕복효율 무시 시 x_max=5.2(과대).

### E_rated 상한 결정
- **E_rated 상한 = 10.2 MWh** (S=2.4MVA에서 x=4.25로 x_max 4.234를 살짝 상회 → 경계 잘림 방지).
- 실제 최적 E/S는 시그마 원식으로 PSO가 내부에서 탐색. 상한은 "최적점만 안 자르면 되는" 값.
- 손익분기 부등식 자체는 **근사식**이라 결론에서 비중 낮춤 — 정량 경제성 판정은
  후처리 지표(정확한 J_net·편익 분해)로. 부등식은 정성적 서술용(왜 구조적으로 아슬아슬한가).

### S_rated 상한 (별개 논리 — 충전 여유, DoD 무관)
- **S_rated 상한 = 2.4 MVA.** 상시운전용량(10,000 kVA)은 방향무관 선로한계이고, ESS 충전전력은
  그 시각 부하와 합산되어 초과 불가. 5개 시나리오 최저부하 시각 중 최소 충전여유(겨울 최대부하일
  최저부하 시각) = 2,473 kW → 2.4 MVA. **순시 전력 제약이라 DoD 무관**(DoD는 에너지축 E에만 반영).

---

## 6-A. ★ 최적 규모 결정 메커니즘 (2026-07 4차, 실측 규명)

6절이 "구조적으로 아슬아슬"이라고 정성적으로 예고한 것의 **정량 규명**이다.
근거: `scripts/probe_large_solutions.py` (b=32, E/S=2.38 고정, S 10점 스윕).

> **★ 이 표는 "E/S를 고정한 단면"이다 (2026-07 5차 명시).** 아래 한계 b_defer 곡선과
> 교차점 S=0.176~0.25는 **E/S=2.38로 고정한 1차원 단면**에서의 결론이다. 실제 최적화는
> (S,E) 2차원이므로 E/S도 자유변수인데, 두 결과가 정합하는 근거는 아래 "★ 최적 E/S는
> 위치 무관 상수"다: full 30 run에서 최적 E/S가 2.342±0.0001로 **거의 불변**이라, E/S를
> 최적값에 고정한 단면이 실제 최적경로를 잘 근사한다. 논문 서술 시 "E/S 최적값에 고정한
> 단면에서의 한계분석"임을 명시할 것(임의의 E/S에서 성립하는 결론이 아니다).

### 실측: 한계 b_defer가 상수인 한계 cost를 가로지르는 지점이 최적 S

| S 구간 | 피크저감 Δ/ΔS | 한계 b_defer | 한계 cost | 한계 j_net |
|---|---|---|---|---|
| →0.100 | 0.823 | 62,574,635 | 59,686,448 | **+14,978,230** |
| →0.176 | 0.729 | 55,429,043 | 59,686,448 | **+7,552,589** |
| **→0.250** | 0.484 | 36,816,021 | 59,686,448 | **−11,393,589** ← 부호 전환 |
| →0.500 | 0.417 | 31,751,156 | 59,686,448 | −17,177,216 |
| →1.000 | 0.334 | 25,436,970 | 59,686,448 | −25,705,658 |
| **→1.500** | **0.111** ← 절벽 | 8,457,925 | 59,686,448 | −44,344,294 |
| →2.400 | 0.113 | 8,562,449 | 59,686,448 | −48,460,930 |

- **한계 cost가 전 구간 정확히 59,686,448 원/MVA로 상수다.** CAPEX가
  `219,528×E + 36,541×S`로 선형이고 E/S가 고정이므로 S에 완전 선형이다.
- 따라서 **최적점은 오직 한계 b_defer가 이 상수선을 언제 아래로 지나는가로 결정된다.**
  교차 지점이 S=0.176~0.25 사이이며, **PSO가 찾은 0.1764가 정확하다.**
- S=0.176 구간에서 한계 b_defer(55.4e6)가 이미 한계 cost(59.7e6)보다 낮은데도 한계 j_net이
  양수인 것은 **b_energy의 한계 기여가 그 차이를 메우기 때문이다.** 16%짜리 항이지만
  경계 결정에는 무시할 수 없다.

### 포화 메커니즘: 피크 시각이 옮겨다닌다
```
S=0.05 → t=18 (원래 피크)   S=0.10~0.176 → t=19   S=0.25 → t=16
S=0.50 → t=15               S=0.75 → t=18         S=1.00 → t=21
S≥1.50 → t=23 (고정)
```
- 봉우리를 깎으면 **인접 시각이 새 피크**가 되어 한계 편익이 계단식으로 떨어진다.
  `summer_peak`의 t=16~19가 0.9816/1.0000/0.9929/0.9834로 평평한 것이 원인이며,
  이는 KPX 실측 프로파일에서 온 계통의 실제 성질이다.
- **S≥1.5에서 피크가 t=23에 고정**되고 저감 효율이 1/3로 급락한다(0.334 → 0.111).
  t=23(0.8532)은 저녁 봉우리에서 멀리 떨어진 다른 지형이며, 그것을 더 깎으려면 ESS가
  그 시각에도 방전해야 하는데 **SOC 사이클 등식(SOC[24]=SOC[0])** 때문에 불가능하다.

### 연구 서사 (논문용)
> 설비 이연 편익은 **연중 최대 피크 1점**으로 결정되므로, 그 봉우리를 깎는 순간 인접 시각이
> 새 피크가 되어 한계 편익이 급격히 체감한다. 반면 CAPEX는 용량에 선형이다. 따라서 최적 ESS
> 규모는 **피크 프로파일의 평탄도**에 의해 결정되며, 한국 부하 프로파일처럼 저녁 봉우리가
> 평평한 경우 최적 규모가 매우 작아진다(상한 2.4 MVA 대비 7.3%인 0.176 MVA).

> ⚠️ **기록 정정:** 초기 보고에 "한계 b_defer가 약 1/7로 급락"이라 적혔으나 이는 서로 다른
> 구간의 값(S=0.05→0.10 구간의 6.26e7과 S=1.0→1.5 구간의 8.46e6)을 잘못 짝지은 것이다.
> 실제 S=0.176→0.25 구간의 변화는 **0.664배**다. 결론(최적 S=0.176~0.25)은 동일하나
> 근거 숫자로 1/7을 인용하지 말 것.

### ★ 최적 E/S는 위치 무관 상수다 (2026-07 5차, full 30 run 실측)
full(n=1) 결과에서 서로 다른 버스로 수렴한 run들의 최적 E/S가 **버스와 무관하게 동일**하다:

| 버스 (run 수) | 최적 S | 최적 E | E/S |
|---|---|---|---|
| 15 (18) | 0.17563 | 0.41143 | 2.3426 |
| 16 (3) | 0.17500 | 0.40996 | 2.3427 |
| 31 (1) | 0.17921 | 0.41983 | 2.3427 |
| 32 (8) | 0.17916 | 0.41969 | 2.3425 |

- 네 버스 전부 **소수 넷째 자리까지 일치**(2.3425~2.3427). 위치가 바뀌면 최적 **규모(S)**는
  미세조정되지만(0.1750~0.1792), 저장:출력 **비율(E/S)**은 계통 어디서나 같다.
- 이유: E/S를 결정하는 것은 위 한계분석의 "한계 b_defer 체감 vs 선형 CAPEX 교차"인데,
  이 메커니즘은 **부하·가격 프로파일에만 의존**하고 버스 위치(전압·전류 경로)와 독립이다.
  위치는 손실(b_loss)을 통해 규모에만 영향을 준다.
- **또한 같은 버스로 간 run들끼리는 (S,E)가 std 1e-5 수준으로 완전 수렴** — 각 버스의
  최적 용량이 유일하고 유역이 매끈함을 뜻한다(7절 "전역최적의 강한 증거" 판정 충족).

---

## 7. 구현 요구사항

### 환경·워크플로우 (2026-07 갱신 — 이원화 해소)
- **데스크탑이 주 실행 머신이다.** Windows 보안 설정 완화로 데스크탑에서도 VS Code +
  Claude Code가 동작하게 되어, 기존의 "노트북=작성 / Colab=실행" 이원화는 해소됐다.
  - 데스크탑: **물리 16코어 / 논리 24 프로세서 / 메모리 15.7GB.** 본실험은 여기서 완주한다.
  - 노트북: 코드 작성·검증용 보조. 두 머신 모두 conda env `ess`.
  - **Colab은 백업 경로로 격하.** Colab 무료 티어(~2코어)보다 데스크탑이 압도적이므로
    상시 사용하지 않는다. GitHub 동기화 구조는 그대로 두어 선택지로 남긴다.
- **프로젝트 폴더명은 모든 머신에서 `summer_ess_project`로 통일** (OneDrive 밖 로컬).
- 코드는 **단일 진입점 실행 좋게 모듈화(.py 분리)**. 진입점 `main.py`.
- 설치 완료: pandapower, numpy 2.2.6, scipy, pandas, cvxpy, numba.

**★ 머신 간 재현성 (실측 확인)**
- 두 머신의 **물리량은 일치**한다(당시 `vm_pu=1.0` 기준 손실 310.06 kW / Vmin 0.9407 pu 재현).
  ※ 현행 검증값은 슬랙 1.02 기준으로 바뀌었다(1절 표) — 재현성 결론 자체는 변하지 않는다.
- 단, **잔차 및 잔차 파생값은 미세하게 다르다**(1e-15 스케일). 이는 정상이다 —
  잔차는 `8.81 − (8.50 + 0.31)`처럼 크기가 비슷한 수의 차라 상위 비트가 상쇄되고
  **부동소수점 잡음만 남으므로 구조적으로 재현 불가능**하다. CPU 아키텍처가 다르면
  FMA 사용 여부·벡터화 폭·합산 순서가 달라져 마지막 비트들이 바뀐다.
  → **값이 아니라 크기(1e-15 스케일)만 같으면 정상.**
- 함의: 시드를 고정해도 두 머신의 PSO 경로가 완전히 같지는 않을 수 있다(부동소수점 차이가
  입자 선택을 바꿀 여지). **본실험은 한 머신(데스크탑)에서 완주**하고, 결과와 함께
  머신·패키지 버전을 기록한다.

### ★ 실행 환경 주의 (실측 확인, 상세는 `ENVIRONMENT.md`)
- 이 환경의 numpy는 **MKL 백엔드**다(`np.show_config()`의 `"openblas configuration": "unknown"`).
  따라서 `OPENBLAS_*` 환경변수는 **무효**이며 설정하지 말 것.
- **VS Code 확장 세션에서 MKL 기본 스레딩(Intel OpenMP)이 워커 스레드 풀을 생성하는 시점에
  프로세스가 무증상 종료**된다(stdout/stderr 없이 종료코드만 남음).
  `MKL_NUM_THREADS=1`로는 해결되지 않고 — 스레드 **개수**가 아니라 **생성 자체**가 막히는
  문제이므로 — `MKL_THREADING_LAYER=SEQUENTIAL`로 백엔드를 교체해야 한다.
  (터미널에서 직접 실행한 프로세스에는 이 제약이 없다.)
- **★ 노트북·데스크탑 양쪽 모두 이 설정이 필요하다.** 환경변수는 머신마다 따로 걸어야 한다.
  (데스크탑에서 Windows 보안 완화만으로는 해결되지 않았고, SEQUENTIAL 설정 후 정상 동작 확인.)
- 설정 위치: **conda env config vars** (`conda env config vars set MKL_THREADING_LAYER=SEQUENTIAL -n ess`).
- **★ 이 변수를 코드 파일에 하드코딩하지 말 것.** `os.environ[...]`으로 `main.py` 등에 박으면
  다른 머신·Colab까지 따라가 MKL 멀티스레드를 불필요하게 죽인다. 환경 문제는 환경 설정으로
  해결하고 코드는 건드리지 않는다(관심사 분리).
- 성능 영향: MKL 단일 스레드화. 33버스 희소 조류계산에는 손실이 거의 없고,
  Pool 병렬화 시에는 오히려 워커 간 스레드 경합을 막아 유리하다.
  (단, 워커 스레드 제한은 이와 별개로 **병렬 설계의 일부**로서 코드에 넣어도 된다.)

### 속도 최적화 (안 하면 3배 느려짐)
> 한 입자 평가 = 조류계산 120회(5시나리오×24h). PSO 3,000평가 → 조류계산 36만 회. 개별 속도·병렬이 관건.
1. `pp.runpp(net, numba=True, init='results')` — 24h 순차 시 3.3배(32.7→9.8ms).
   - **numba:** 조류계산 내부 행렬연산을 실행 직전 기계어로 컴파일 → 파이썬 반복을 C 속도로.
   - **init='results':** 조류계산은 뉴턴법(추측 개선 반복). 인접 시각(t,t+1) 계통상태가 비슷하므로
     직전 시각 해를 다음 시각 초기추측으로 재사용(warm start) → 반복 감소·빠른 수렴.
2. 워커당 1회 `build_net`(22.9kV·10MVA 스케일 완료) + BASE_P/BASE_Q 캐싱.
   - 계통 구조는 평가마다 안 바뀜(바뀌는 건 부하·ESS 주입값뿐) → 한 번 만들어 메모리에 두고 재사용.
   - 기저부하 프로파일도 고정 → 한 번 계산해 저장(캐싱). "고정된 건 한 번 만들어 재활용."
3. sgen drop/create 금지, 값만 갱신: `net.sgen.at[i,'p_mw']=...`
   - ESS 객체를 매번 파괴·생성하면 느림 → 이미 붙은 sgen의 숫자값만 바꿔치기(집 새로 짓지 말고 가구만 교체).
- 실측 평가1회(120 PF) = **0.685초** (evaluate.py 구현 후. 초기 참고치 1.178초의 58%).
  - 단축 요인: `init_worker()`가 **기저(ESS 없음) 조류계산 120회를 1회만 계산해 캐싱**하므로
    평가 1회당 실제로 도는 조류계산은 ESS 주입 상태 120회뿐이다(240회가 아님).

### ★ 수치 잡음의 성격 (`scripts/probe_noise.py` 실측, 확정)
동일 상태를 계산해도 **초기값 이력에 따라 `P_slack`이 1e-9~1e-8 MW 수준으로 달라진다.**
원인 규명 결과는 아래와 같다.

| 실험 | 조건 | 결과 |
|---|---|---|
| 1. sgen 유무 (이력을 flat으로 통제) | P=Q=0인 sgen 유무만 차이 | **diff = 0.0 정확히**, 반복횟수 3=3 동일 |
| 2. warm start 이력 (sgen 없음) | flat 직접계산 vs 타 상태 경유 후 warm start | 8.72e-10 MW |
| 3. 실사용 (S=1.0 MVA/E=4 MWh, summer 24h) | flat 독립계산 vs warm start 체인 | 최대 1.20e-8 MW (t=5), 부호 혼재 |
| 4. tolerance_mva 의존성 | 실험2를 1e-8 / 1e-10 / 1e-12로 반복 | 8.72e-10 → 1.03e-12 → 7.46e-14, 시간 3.19~3.23 ms로 사실상 동일 |

- **원인: warm start 이력 차이.** sgen 존재 자체는 **완전히 무해**하다(실험 1에서 diff가
  정확히 0이고 반복횟수도 동일). P=Q=0인 sgen은 노드 전력방정식에 정확히 0을 더하므로
  야코비안도 잔차도 수학적으로 동일하기 때문이다.
  실제 원인은 `init_worker()`의 기저 계산 첫 호출(summer t=0)이 이력 없는 상태에서 출발하는 반면,
  `evaluate_particle`은 같은 net을 이어받아 기저 계산의 마지막 지점(winter_peak t=23)에서
  warm start하므로 **뉴턴법이 tolerance_mva(기본 1e-8) 안의 서로 다른 점에서 멈추기** 때문이다.
- **범위: S=0 케이스 특유가 아니라 모든 평가에 공통이다.** 실제 ESS 스케줄에서 오히려 더 크다.
- **금전 영향: 상한 ~6.91원/년** (실험3 최대 MW차 × 최고 SMP × 최대 N_WD × 24h의 극단 환산.
  실측 0.028원과 정합 — 시각별 부호가 섞여 상쇄되므로 실제로는 더 작다).
  `J_net`이 수천만~수억 원 규모이므로 **완전히 무해**하다.
- **결정: `tolerance_mva` 기본값(1e-8) 유지.** 조여도 시간 비용은 없지만(warm start라 반복이
  이미 최소) 얻을 것도 실질적으로 없다. 잡음이 이미 원 단위 미만이기 때문이다.
  - 참고: 100배 조여도 시간이 안 느는 것은, warm start 초기값이 해에 매우 가까워
    **반복 한 번이면 1e-12에 도달**하고 tolerance가 그 여유 정밀도를 버리고 있었다는 뜻이다.

### 병렬화 (입자 단위)
> CPU 여러 코어(일꾼)에 입자를 나눠 동시 평가. 입자 평가는 서로 독립이라 병렬화 가능(8코어≈8배).
```python
def init_worker():
    global NET, BASE_P, BASE_Q
    NET = build_net(); ...     # 각 일꾼이 시작 시 자기 계통 1회 준비(속도최적화 2)
with Pool(n_cores, initializer=init_worker) as pool:
    fitness = pool.map(evaluate_particle, particles)  # 입자 목록을 일꾼들에 분배·동시 처리
```
- **왜 입자 단위인가 (독립실행 병렬 아님):** 매 반복마다 입자 30개 병렬평가 → 결과 모아 PSO 갱신 →
  다음 반복. 반복마다 수렴상태를 찍어 디버깅 가능. 독립실행(30 runs) 병렬화하면 한 PSO가 통째로
  한 코어에 묶여 중간상태 관찰 불가. → **디버깅 사이클 확보** 위해 입자 단위 선택.

### ★ 워커 수·입자 수 확정 (`scripts/bench_workers.py` 실측, 2026-07-20)
> **확정: `N_WORKERS = 16` (물리 코어 수), `N_PARTICLES = 32`.**

데스크탑에서 그리드 `(10,20) (12,24) (14,28) (16,32) (16,30) (16,36) (16,40) (16,48) (16,64)`를
유효 3회 실행(별도 1회는 시스템 정지로 폐기). 지표는 평가당 시간 = 세대시간 / 입자수.

| 실측 사실 | 함의 |
|---|---|
| **조합 간 차이가 실행 간 재현 노이즈(±15%)에 묻힘.** 세 실행의 최우수 조합이 각각 16/32, 16/64, 12/24로 **전부 다름** | 데이터로 우열을 가릴 수 없다. **추가 실측 무의미** |
| 평가당 시간이 워커 10~16에서 0.09~0.10초로 일정 | 이 구간에서 **병렬 효율 저하 없음** |
| 평가시간 표준편차가 평균의 2배 수준(0.1 ± 0.2초) | 입자별 평가시간이 크게 불균등. `chunksize=1`의 **동적 로드밸런싱이 라운드 정합 효과를 상쇄** → **입자 수를 워커 수의 정수배로 맞출 필요 없음** |
| 워커당 RSS 약 288 MB (10~16워커에서 선형). 16워커 = 4.6 GB | 메모리 여유 있음 |
| **20워커 이상에서 시스템 정지 2회 발생** (화면 정지, python 프로세스 전멸) | RSS 합계는 실제 시스템 압박을 **과소평가**한다. → **20·24워커 사용 금지** |
| 전 조합 `n_diverged = 0` | 탐색공간 무작위 추출 기준 발산은 드물다 |

- **하이퍼스레딩(논리 24)을 쓰지 않는 근거:** BLAS·희소 조류계산은 메모리대역폭 바운드라
  HT 이득이 작고, 실측에서 메모리 한계가 먼저 걸렸다. `os.cpu_count()`를 그대로 쓰지 말 것.
- **`chunksize=1`을 반드시 명시**할 것. `pool.map` 기본값은 `len/(4·workers)` 올림이라
  입자 수를 바꾸면 청크가 커져 로드밸런싱이 죽는다.
- ※ 벤치마크의 4.6 GB는 **상한 추정치**다. 벤치마크는 탐색공간 전체에서 무작위 추출한
  극단 입자를 평가하지만, 실제 PSO는 몇 세대 만에 좋은 영역으로 수렴해 warm start가
  잘 들어 메모리·시간이 더 얌전하다(실제 `main.py` 실행에서 확인). 최악 케이스로 판단해
  20·24워커 금지 결정은 유지한다.

### L-SHADE 하이퍼파라미터 (12차 — PSO에서 교체)
> **정할 파라미터는 사실상 eval_budget 하나다.** F·CR은 실행 중 성공이력으로 자기적응하므로 사람이 안 정한다.
> 나머지는 Tanabe-Fukunaga(2014) 표준값이라 무튜닝(방어 자산 — "왜 그 값?"에 "CEC 우승 계열 표준값"으로 답).
- **정하는 것:** 초기인구 `N_init = 12·n_dims`(차원비례, 위 기수별 입자수 소절), `eval_budget`(프로파일:
  full=100·N_init, dev=30·N_init). 종료는 예산 소진.
- **표준 상수(변경 금지):** `P_BEST_RATE=0.11`(current-to-pbest 상위 풀 비율), `MEMORY_SIZE=6`(F/CR 메모리
  칸수), `MIN_POPULATION=4`(LPSR 하한, current-to-pbest/1이 요구하는 최소), F/CR 초기값 0.5(첫 세대만 영향,
  이후 자기적응이 덮음).
- **F/CR 자기적응:** 개체마다 메모리에서 F(Cauchy(loc,0.1))·CR(정규(loc,0.1)) 샘플 → 성공 개체의 값을
  개선량 가중 Lehmer 평균으로 메모리 갱신. LPSR: 인구 N_init→4 선형감소(예산 비례).
- **`lshade_core.py`는 벤치(twin_valley/rastrigin/rosenbrock + 실제 목적함수)에서 검증 완료.** numpy만 의존,
  objective를 생성자 인자로 받음, `optimize()`가 `{x,f,history,eval_count}` 반환.
- **`_sample_f`의 overflow 경고(무해):** F 샘플의 Cauchy가 `location+0.1·tan(π·(u−0.5))`인데 u가 π/2 특이점
  근처면 tan이 overflow 경고를 낸다(numpy reduce). 그러나 `value>0` 재샘플·`min(,1.0)` 클리핑이 방어해
  F값은 항상 (0,1]로 정상이고 결과 무영향(b=31 완벽 재현이 증명). 미수정(로그 경고뿐). 거슬리면
  `rng.standard_cauchy()`로 교체 가능(동일 분포, 단 난수열이 달라져 회귀 재확인 필요).

### 페널티
- **뼈대: 정적 L1.**
  - 전압 위반량 = `Σ_{s∈ALL,t,bus} [max(0, V−1.05) + max(0, 0.95−V)]` (단위 pu).
  - 선로 위반량 = `Σ_{s∈ALL,t,line} max(0, I/I_rated − 1)` (**과부하 비율**로 정의 → pu와 유사 스케일,
    λ 통일 가능. 전류 A 직접 사용 금지).
- **초기값(첫 실행용, 미확정): `λ_V = 1e10` 원/pu, `λ_line = 1e10` 원/과부하비율.**
  - 근거: 편익·비용 규모 ~2.5e8원(최대설비 Cost). λ_V=1e10이면 위반 0.01pu→벌점 1e8(편익과 비등),
    0.1pu→1e9(편익의 4배로 압도). "가벼운 위반은 편익과 비등·심한 위반은 편익 압도"하는 스케일.
    너무 크면(1e12) 경계에 과도하게 눌어붙어 탐색 경직, 너무 작으면(1e7) 위반이 이득이 되어 무력.
  - 선로는 여유 큼(최대충전 시 이용률 61%)이라 λ_line 사실상 안전장치(거의 미발동). 전압지배.
- **첫 실행 후 조정:** 최적해에 위반 남음 → λ 10배↑ / 위반 없고 편익 눌림 → λ 10배↓ / 위반 거의 없음 → 적절.
- **본실험: 적응형** `λ(t)=λ_0·(1+α·t)` 또는 구간 계단식(초기 탐색 허용→후기 강제).
  적응형 λ_0·α는 뼈대의 정적 관찰값에서 도출.
- 정규화(위반량/한계값)는 "λ 감이 안 잡힐 때"만 도입(전압지배라 당장 불필요).
- **제약 검증은 ALL_DAYS 전부**(에너지·이연은 AVG/PEAK로 나뉘지만 위반만은 최대일 포함 전 시나리오).

### LP 검증 (mean-shift PSO 실증은 폐기)
> 이론적 등가(max_{x,y}=max_x max_y)는 수학적 사실이라 재검증 불필요.
> 단 "LP를 정확히 구현했는가"는 별개 → 아래 3종으로 검증.
1. **해석해 대조 (test_lp.py):** 단순 SMP(예: 12h 저가 100원/12h 고가 200원, 손실무시)에서
   손계산 답과 LP 대조. 효율 90%로 바꿔 재대조. 부호·효율·SOC 제약을 한 방에 검증.
2. **물리 보존 assert (LP 함수 내부):** SOC[24]==SOC[0], SOC∈[0.05·E, 0.95·E] (MWh 규약),
   |P|≤S_rated (원 제약과 동치 — 2절 대수적 항등 참조. Q는 LP 밖이라 여기서 안 봄),
   에너지수지(Σ충전·η − Σ방전/η = 0). 개발 중 켜고 본실험 시 속도 위해 끔.
3. **단조성·극한 점검:** E↑→에너지편익 단조증가, S=0→편익=0, 스프레드=0→차익=0.

#### ★ 테스트 설계 원칙 (실제 구현 중 발견 — 반드시 지킬 것)
**(원칙 1) degenerate 목적함수에서는 '해 분포와 무관한 불변량'으로만 검증한다.**
- `solve_peak`의 목적은 **스칼라 `pk` 하나뿐**이라, 평탄부 제약 `pk ≥ load−P_dis+P_ch`가
  **느슨(inactive)**한 시간대의 충전 분배는 목적값에 영향을 주지 않는다 → **해가 무한히 많다.**
  (실측: S=3,E=100에서 평탄부 충전이 −0.5885~−0.6188로 흩어짐. 총합만 정확히 12.0.)
- 따라서 "충전을 20시간에 균등분산"은 **손계산 편의를 위한 한 예시일 뿐 유일해가 아니다.**
  균등값으로 assert를 걸면 물리가 맞아도 테스트가 실패한다.
- **검증해야 할 불변량:** ① 총충전량(에너지수지), ② `flat_demand ≤ pk_s`(어느 평탄시간도
  피크를 넘지 않음 — 넘으면 그게 새 피크가 되어 모순), ③ `pk_s == max(load − P_net)`(자체 일관성).

**(원칙 2) degeneracy 유무는 '실측(std)'이 아니라 '구조'로 단언한다.** ★
- **금지: `assert flat_demand.std() > 0.01`** 류의 검증. 이는 물리가 아니라 **솔버 내부 동작**을
  검증하는 것이라, cvxpy 버전·기본 솔버(ECOS↔Clarabel) 변경만으로 값이 달라져
  **물리와 무관한 이유로 실패**한다. (실측 S=1,E=12에서 std=0.0164로 임계 0.01에 아슬아슬.)
  더 나쁜 것은, 솔버가 우연히 균등해를 내면 "LP가 틀렸다"는 **잘못된 신호**를 준다는 점
  — 균등해도 엄연히 최적해다.
- **대신 제약의 활성/비활성을 직접 단언한다(솔버 무관, 대칭적):**
  - degenerate 케이스(제약 느슨): `assert flat_demand.max() < pk_s - 0.1`
  - 유일해 케이스(제약 타이트): `assert np.allclose(flat_demand, pk_s)`

**(원칙 3) 무엇이 유일하게 결정되는지 구분한다.**- **방전측 `P_net[20:]`은 항상 등식 검증 가능**: "각 시간이 d 미만이면 그 시간이 새 pk가 되고
  총합은 고정" → 느슨함과 무관한 별개 논거로 유일하게 결정된다.
- **평탄부는 조건부**: 평탄부 수요가 pk에 **등호로 붙을 때만** 유일해가 된다.
  (S=5,E=100: `5+d*/5 = 10−d* = pk*` → 20개 제약이 **동시에 active** → 자유도 소진 → 균등해가 유일.
   실측 편차 3.6e-10. 기하학적으로는 면(face) 위를 미끄러지던 해가 **꼭짓점에 고정**된 것.)
  → 이는 "제약이 타이트해서"라는 일반론이 아니라 **그 점에서만 성립하는 특수 상황**이다.

**(원칙 4) 허용오차는 비교 대상의 성격에 따라 다르게 쓴다. ★**
| 비교 대상 | 규약 | 근거 |
|---|---|---|
| **물리량** (MW, pu) | `atol=1e-7, rtol=0` | 절대 스케일이 고정(슬랙 ~8.8 MW)이라 절대오차가 자연스럽다 |
| **금액** (원), 기대값 ≠ 0 | `rtol=1e-9, atol=0` | ESS 크기·기간에 따라 규모가 변하므로 상대오차라야 규모 무관하게 작동 |
| **금액** (원), 기대값 = 0 | `atol=10.0` (실측 유도) | 기대값이 0이면 상대오차가 정의되지 않는다(`rtol*0 = 0` → `a == 0`을 요구) |

- **`rtol=0` / `atol=0`을 반드시 명시할 것.** `np.allclose`의 기본 `rtol=1e-5`가 남으면
  8.8 MW에서 허용오차가 8.8e-5로 커져 절대기준이 무너지고, `np.isclose`의 기본
  `atol=1e-8`이 남으면 0 근처 비교가 의도치 않게 통과한다.
- **기대값 0인 금액에 `atol=10.0`을 쓰는 근거:** 위 "수치 잡음의 성격"에서 유도된
  잡음 상한 6.91원/년에 안전여유를 붙인 값. **추측이 아니라 실측 유도값이다**(원칙 2와 같은 계보).
  - ★ 이 값은 **현재 조건에 종속**된다: `tolerance_mva` 기본값(1e-8), 시나리오 5개, 현재 N_WD 기준.
    Phase 2에서 시나리오가 10개로 늘면 합산 항이 두 배가 되어 상한도 대략 두 배가 된다.
    **테스트가 깨지면 이 조건을 확인하고 상한을 재유도할 것.** 코드 주석에도 이 조건을 남긴다.
- 이전에 쓰던 `atol=1원`은 근거 없는 어림값이었다. 편익 1,200만 원 규모에서 상대오차 1e-8에
  해당하는데, 규모가 억 단위로 커지면 부동소수점 누적이 이를 넘겨 **물리와 무관하게 깨진다.**

**(원칙 5) 테스트는 완성 후에도 삭제하지 않는다. ★**
- 테스트의 목적은 둘이다: ① "지금 이 코드가 맞는가"(작성 시점 검증),
  ② **"나중에 뭔가 고쳤을 때 이게 깨졌는가"(회귀 방지).** ②가 장기적으로 더 중요하다.
- 이 프로젝트에서 ②가 실제로 걸리는 지점: 6차 QP 편입은 `lower_lp.py`를
  뜯어고치고 `q_ratio`를 없애며 PSO 차원을 4n→3n으로 바꾼다. 그때 `benefits.py`가
  멀쩡한지 확인하는 것이 기존 테스트다 — **회귀 검증(`test_qp_regression.py`,
  force_q_zero 무해성)을 수행하는 주체가 바로 이 테스트들이다.**
  가까운 예로 `params.py`에 MW 상수를 추가했을 때 `test_lp.py` 13개 재실행으로
  회귀 없음을 확인한 것이 있다.
- **장난감 데이터 테스트를 실데이터 테스트로 대체하지 마라. 역할이 다르다.**
  - **장난감 = 정확성 검증.** 2시나리오×3시각·상수 SMP면 손으로 15,000원을 계산할 수 있고,
    코드가 그 값을 내면 Δt 곱셈·N_WD 가중·시나리오 순회가 전부 맞다는 것이 **증명**된다.
  - **실데이터 = 통합 동작 확인.** `b_energy`가 1.37e8원이라는 결과는 **비교 대상이 없어**
    맞는지 알 수 없다. 기껏해야 스냅샷 테스트가 되는데, 그것은 처음부터 틀렸어도 계속 통과한다.
- **실행 비용에 따라 분리한다:** `pytest.ini`에 `slow` 마커를 등록해
  `test_lp.py`·`test_benefits.py`(초 단위, 매번 실행)와 `test_evaluate.py`(조류계산 포함, 무거움)를
  구분한다. 일상 개발은 `pytest -m "not slow"`, 커밋 전·주요 변경 후에는 전체 실행.

#### ★ solve_peak 방전량 d를 결정하는 세 상한 (통합 구조)
모든 peak 테스트 결과는 아래 **하나의 min 구조**로 설명된다:
```
d ≤ S              (PCS 출력 상한)
4d ≤ 0.45·E        (SOC 저장 상한: 0.95E − 0.50E = 0.45E, 평탄부에 쌓아둘 수 있는 양)
d ≤ d*             (충전 되갚음 상한: 부하패턴 의존. 이 테스트 패턴에서 d* = 25/6 ≈ 4.1667)
```
- **셋 중 가장 작은 것이 바인딩된다.** 검증 실측:

  | 케이스 | min(S, 0.45E/4, d\*) | 바인딩 | pk_s |
  |---|---|---|---|
  | E=100, S=2 | min(2, 11.25, 4.167) = 2 | S(출력) | 8.00 |
  | E=100, S=3 | min(3, 11.25, 4.167) = 3 | S(출력) | 7.00 |
  | E=100, S=5 | min(5, 11.25, 4.167) = **4.167** | **되갚음** | 35/6 ≈ 5.8333 |
  | E=12, S=1 | min(1, 1.35, 4.167) = 1 | S(출력) | 9.00 |
  | E=12, S≥2 | min(S, **1.35**, 4.167) = 1.35 | **SOC(저장)** | 8.65 |

- `d*`는 `10−d = 5+d/5` (방전측 감소 vs 충전상승 증가)의 교차점. **부하패턴에만 의존**하고
  설비 파라미터와 무관 — 나머지 두 항과 성격이 다른 이유가 이것.
- **★ SOC 상한 항이 `0.45E`인 이유:** 이 문제에서 배터리는 평탄부에 **먼저 충전**해 쌓아두고
  피크에 방출한다. 따라서 묶이는 것은 방전 여유가 아니라 **"쌓아둘 수 있는 양"**
  `= SOC_MAX_FRAC·E − SOC_INIT_FRAC·E = 0.45E`. **SOC 시작 50% 규약이 이 결과를 만든다**
  (시작이 0.95E였다면 0.9E가 되어 완전히 다른 답).
- **★ 실제 실험에 대한 함의:** 6절 E_rated 상한 10.2 MWh 기준 SOC 항은
  `0.45 × 10.2 / 4 = 1.1475 MW`. S 탐색범위가 이보다 크면 **실제 실험에서는 SOC(저장)가
  지배적 병목**일 가능성이 높다. → "S를 키워도 이연편익이 안 늘어난다"는 결과가 나오면
  그것은 PSO 탐색 실패가 아니라 **E가 묶여 있기 때문**이다. 8절 S\*/E\* 분포 해석 시
  **E/S 비(지속시간)가 어디로 수렴하는지**가 핵심 지표.

---

## 7-A. main.py 설계 확정 사항 (2026-07 3차, 4차 갱신)

`main.py`는 파이프라인의 **단일 진입점**이며 순수 조립자다. 최적화 로직은 `pso_core`,
평가는 `evaluate`에 있고 main은 **조립·실행·기록만** 한다.
(`evaluate.py`/`benefits.py`/`lower_lp.py`/`pso_core.py`는 수정하지 않았다.)

### ★ 실행 단위는 "기수 1개"다
2절이 "1기부터 하나씩 늘리며 순편익이 꺾이는 지점을 탐색"하므로, **꺾임 판단은 사람이 한다.**
- `--n-ess 1`이 기본값(단일 정수). 콤마 리스트도 받지만 명시 지정 시에만.
- **왜 한 번에 다 돌리지 않는가:** n=1 결과를 봐야 λ 조정·입자 수 판단이 서고, 그 판단이
  n=2 실행 설정에 반영되어야 한다. 연달아 돌리면 **잘못된 λ로 전 기수를 태운다.**
- 기수마다 Pool을 새로 만든다(차원이 바뀌므로 필요하고, 앞 기수 워커 메모리 4.6GB가
  확실히 회수되는 이점도 있다).
- 출력 파일명에 기수 포함: `results/generations_n<k>_<timestamp>.csv`, `runs_n<k>_<timestamp>.csv`.

**★ 기수 탐색 규칙 (확정): "꺾인 뒤에도 한 기수 더 본다."**
- 예: n=3에서 꺾여도 n=4까지 확인한다. **기수별 J_net이 단봉이라는 보장이 없기 때문이다.**
  (2기가 서로 간섭해 증분이 작았다가 3기에서 배치가 분산되며 다시 개선될 수 있고,
   PSO 탐색 실패로 중간 기수만 나쁘게 나올 수도 있다.)
- **"꺾였다"의 판정은 단일 최고값이 아니라 run별 gbest의 분포**(중앙값·사분위수, full run 20)로 한다.
  독립실행 분산이 기수 간 차이보다 크면 판정이 성립하지 않는다.

### ★ n=2 통합 실행 결과 (2026-08 12차, PSO 세대50·run10) — 1기 붕괴 재현
통합 solve_peak/solve_avg + 다수기 evaluate 위에서 처음 돈 공정한 n=2다(구 4차 결과 3.08e6·b=15는
분리LP·pf=0.85 시절이라 폐기). 최우수 run: **unit0(b=32, S=0.2099, E=0.4407) + unit1(b=27, S=0, E=0),
j_net≈3.957e6.** 실질 기수 분포 **1:10(전 run 1기 활성)**, 위치조합 (32,)3회/(16,)7회, j_net (32,)3.957e6 >
(16,)3.777e6.

**전 run이 한 기를 S=E=0으로 소멸(= 1기 설치와 동치).** 최적기수 1은 유력하나 **아직 "측정된 결론"이
아니라 "강하게 지지되는 가설"**이다:

1. **통합 LP 정상작동(구조오류 배제).** 탐색실패 0·발산 0·slack balance/SOC/다각형 assert 전부 통과,
   b_defer 붕괴 없음(1기해 b_defer 1.24e7 = n=1급). probe_split 0.53배 붕괴 해소 확인. 대각근사 오차
   n=1급(median 0.03%/0.07%). → 과거 분리LP·d* 중복 붕괴 서명 없음.
2. **1기 최적의 물리 논증.** b_defer가 편익 83.5%이고 이연은 시스템 피크저감의 함수(통합 단일 pk)라
   **집중이 유리**(같은 총 S를 쪼개면 각 기 피크기여 분산돼 시스템 pk 저감효율 하락). 음의 b_defer 369회
   ·최소 −272만원 실재가 방증(2기 활성 시도 시 b_defer 손실을 실제로 봄, arb+loss로 못 메움). E/S=2.10·
   지속시간 1.89h로 용량 여유도 없음.
3. **미측정 지점(솔직히).** "16+32 분산 강제배치 j_net < 1기해" 직접측정은 안 했다(PSO가 그 조합을 안
   가봄). 현 결론은 간접증거(음의 b_defer)에 기댐. **직접측정 + L-SHADE 재실행으로 재확정 대기.**

→ **판정 대기: 최적 기수 1(유력).** L-SHADE 편입 후 n=2 재실행에서 1기붕괴가 재현되면(더 나은 탐색기가
  줬는데도 붕괴) 알고리즘 무관 결론으로 확정된다. 7-A절 규칙("꺾인 뒤 한 기수 더")에 따라 n=3까지 확인.

### ★ 분산 설치의 이득 — 통합 LP에서 측정 가능해짐 (12차)
당초 "상위권 버스 두 곳에 절반씩 나눠 설치하면 손실(I²R)이 줄어드는가"를 물었고, 5차 `probe_split.py`
(분리LP)에서는 **b_defer가 −310만원 무너져 신호가 묻혀 "측정 불가"**였다(원인: d* 중복적용, r=0.5에서
b_defer 0.53배).

- **12차 통합 실측: b_defer 붕괴가 사라졌다.** 통합 solve_peak 위 n=2에서 분산 조합의 b_defer가 정상
  수준(1기해와 동급)이라, probe_split이 경고한 0.53배 붕괴는 통합 구현으로 해소됐다. 즉 이제 분산 설치의
  진짜 이득/손해를 **공정하게 측정할 수 있다.**
- **다만 PSO가 분산해를 거의 안 골랐다**(전 run 1기 붕괴). 그래서 "분산 vs 집중" 직접 대조는 아직 없다 —
  위 n=2 결과의 미측정 지점과 같다. `probe_split.py`를 통합 LP에서 재실행하거나 (16,32) 강제배치를 풀면
  직접 대조가 나온다(미결 ⑤).
- **원 5차 대조군은 "고치기 전"으로 커밋돼 있다.** 통합 재실행 결과가 b_defer 비율 1.0에 붙으면 통합 LP
  검증이 완성된다(n=2 실측이 이미 그 방향).

**★ 다수기 본격 탐색의 시점: L-SHADE 편입 후.**
- 통합 구현·다수기 확장은 완료됐고, PSO n=2 결과는 **L-SHADE 편입 후 재실행으로 폐기·갱신**된다
  (알고리즘 교체로 위치탐색·degeneration 성향이 달라짐). 편입 후 n=1,2,3,4를 L-SHADE로 다시 돌린다.
- n=2 배관(차원 구성·`int_dims` 인덱스·경계 반복)은 12차 통합 실행으로 확인됨.

### ★ 탐색 차원은 3n (b, S, E) — q_ratio를 넣지 않는다 (코드 전면 3n, 12차)
- **★ 코드 전면 3n 기반 (12차 확정).** 종전의 "evaluate 4n 시그니처 유지 + main이 q=0 열 붙이는
  `_expand_to_4n` 어댑터"는 **제거됐다.** evaluate·lower_lp·main 모두 3n `(b,S,E)` 기반이며 4n 경로는
  없다. Q는 하위 QP의 시각별 내부변수라 상위 차원에 등장하지 않는다(2-B).
- (★ 6차: 8절-6 q_ratio 스윕은 폐기됨. 무효전력 가치는 b_loss_q로 측정 — 8절 진단 소절.)
- `int_dims`는 b에 해당하는 `[0, 3, 6, ...]`.

**★ 정수차원(bus) 처리 — L-SHADE `_decode` (12차):**
- **정수 반올림 위치는 평가 직전(`lshade_core._decode`)이 맞다.** 내부 X(모집단·아카이브)는 연속값으로
  유지하고 평가 직전에만 bus를 반올림한다. **이것이 올바른 설계이며 수정하지 말 것** — 반올림값을
  모집단에 저장하면 변이 `X[i]+F·(pbest−X[i])+F·(r1−r2)`가 정수 격자에 눌어붙어 탐색이 고착된다
  (PSO의 pbest 격자 고착과 같은 원리). L-SHADE는 pbest/차분이 전부 연속 X 위에서 계산돼 이 설계가 유지된다.
- `optimize()`가 반환 시 `_decode`를 적용하므로 **최종 해는 정합**하다.
- **단, 8절-5 `b*` 분포 집계는 반드시 `result["x"]`(반올림 적용값)를 쓸 것.**
  내부 연속 좌표를 직접 꺼내면 "최적 버스 = 17.3" 같은 무의미한 값이 나온다.
- **주의(미결 ①):** L-SHADE는 변이가 bus 차원에서도 연속 이동하므로 정수성을 명시적으로 다루지 않는다.
  n=1·n=2에선 지형이 bus 31/32로 강하게 끌어당겨 잘 수렴했으나, 다수기로 정수차원이 늘면 이 부분의 검증이
  필요하다.

### 시드 관리 (12차 SeedSequence 복원 확정)
- **난수는 메인 프로세스에서만 생성**한다. 워커는 순수 평가만 하므로 **실행 순서가 섞여도 결과가 같다.**
- run별 시드는 `np.random.SeedSequence(entropy=base_seed, spawn_key=(n_ess,)).spawn(n_runs)`이며,
  각 자식 SeedSequence를 `lshade_core.LSHADEBench(seed=...)`에 넘긴다(`default_rng`가 SeedSequence를
  직접 수용). `spawn_key=(n_ess,)`로 기수 간 스트림이 독립된다. **`seed=base_seed+run_idx` 같은 저품질
  연속정수 시드 금지.**
  - **★ 12차 사건·복원:** L-SHADE 편입 시 이 부분이 실수로 `run_seed=base_seed+run_idx`(42,43,44)로
    격하됐다가 복원됐다. 격하판은 (a) n_ess 무시(n=1·n=2 run0이 같은 시드 42), (b) 인접정수라 run간
    난수열 상관 위험이 있어, "재시작 분산으로 전역최적 판정"(아래 판정표) 방법론의 전제인 run 독립성을
    훼손한다. 복원 후 dev 3 run에서 1 run이 bus16 지역해로 갈린 것은 **버그가 아니라 시드 독립성이
    살아난 증거**다(격하 연속시드에선 상관돼 우연히 다 b=31로 갔음).
  - `runs.csv`의 seed 컬럼에는 자식 SeedSequence의 정수 지문(`generate_state(1)[0]`, 수십억 규모)을 남긴다.
- `base_seed`는 **CLI 인자(`--seed`, 기본 42)로 노출**한다.
- **비트 단위 완전 재현은 요구하지 않는다.** warm start 이력 차이로 1e-8 MW 수준 잡음이 남지만 금전 영향
  <7원/년이라 무해하다(7절 "수치 잡음의 성격").
- **run 수와 시드:** run 수(full 20)가 뽑는 것은 점 하나가 아니라 N_init×run개 초기점 집합이라 대수의
  법칙이 작동한다. 단 **dev의 3 run 분산은 시드 영향을 받으므로 수렴 판정 근거로 쓰지 말 것.**
  - (선택) full 완주 후 `base_seed`만 바꿔 1회 더 돌려 gbest 분포가 겹치는지 확인하면 시드 무관성을
    실증할 수 있다(심사 질문 대비).

### 중간 저장 — 로그는 남기고 resume은 만들지 않는다
- **resume 불필요:** 본실험 1회가 1시간대라 체크포인트·복원 로직의 구현·검증 비용이
  재실행 비용보다 크다. 대신 **run 단위로 즉시 append·flush**하면 중단 시 남은 run만
  다시 돌리면 되어 resume 효용의 대부분을 얻는다.
- **입자 단위 전체 궤적은 저장하지 않는다**(30×100×30 = 90,000행, 용도 미정).

**로그 스키마 (postprocess.py가 읽는다) — 12차 배치 단위 재정의:**
```
generations_n<k>_<ts>.csv : n_ess, run, gen, batch_size, eval_cumulative,
                            gbest_f, mean_f, std_f, worst_f, elapsed_s,
                            coef_cache_hit_rate_cumulative
runs_n<k>_<ts>.csv        : n_ess, run, seed, gbest_f, x_json, wall_time_s,
                            n_diverged_total, n_negative_bdefer, min_bdefer
```
- **★ 12차 알고리즘 중립 재정의.** L-SHADE는 LPSR로 objective 호출당 배치크기가 가변이라 "1세대=1호출=1행"
  계약(`assert len(history)==len(gen_rows)`)이 성립하지 않는다. 그래서 **행 = objective 호출 1회(= 한 배치)**로
  두고, `gen`은 배치 순번, 신규 `batch_size`(그 호출의 입자 수, L-SHADE 가변·PSO 상수)·`eval_cumulative`
  (누적 평가 수)를 추가했다. PSO는 batch_size가 상수라 같은 스키마에서 완전 호환.
- **`mean/std/worst_f`는 그 배치가 평가한 집단(자식/trials) 기준.** 여전히 기록하되, **std_f 감소를 수렴
  판정에 쓰지 않는다** — L-SHADE 후반은 인구가 4로 줄어(LPSR) std가 표본 탓으로 구조적으로 작아지므로
  "수렴"으로 오독하면 안 된다. `batch_size` 컬럼이 왜 후반 std가 작은지 자체 설명한다. 판정은 재시작
  분산(run간 gbest)으로 한다(아래 기수별 입자수 소절).
- **`gbest_f`는 배치별 best(min fitness)의 누적 최소로 main이 자체 계산**한다(history 배열 비의존 —
  길이불일치 원천 제거). elapsed_s·`coef_cache_hit_rate_cumulative`는 실행 건강성 모니터링용으로 보존
  (캐시 히트율 상승 곡선이 여기 남는다).
- **`n_negative_bdefer`/`min_bdefer`:** 8절이 요구하는 음의 B_defer 카운터.
  **로그를 안 남기면 사후 복원이 불가능하다**(최적해만으로는 탐색 중 발생 여부를 알 수 없음).

### `--diagnose` (페널티 분리 진단)
부록 B의 `LAMBDA_V`/`LAMBDA_LINE`/`PENALTY_DIVERGE`가 전부 잠정값이므로,
**fitness 스칼라 하나만 남기면 조정 근거가 사라진다.**
- `evaluate`의 `return_detail=True`가 주는 `v_violation`/`i_violation`에 λ를 곱해
  **gbest 해 하나에 대해서만** 사후 재계산·출력한다(전 입자 기록은 비용 대비 이득 없음).
- 3-B절의 진단이 이 경로로 얻어졌다.

### 프로파일 (12차 — 예산 기반 재정의)
```
--profile dev  : budget_coef=30,  n_runs=3     (기본값)   → eval_budget = 30·N_init
--profile full : budget_coef=100, n_runs=20               → eval_budget = 100·N_init
```
- **★ 12차 변경.** L-SHADE는 세대수·입자수가 아니라 **평가예산**으로 종료하고, 초기인구는 차원의존
  `N_init=12·n_dims`(실행 시 결정)이라 프로파일에 상수로 못 넣는다. 프로파일은 예산계수와 run수만 정의하고,
  실행부에서 `eval_budget = budget_coef · (12·n_dims)`로 계산한다.
  - n=1(3D): N_init=36, full 예산 3600. n=2(6D): N_init=72, full 예산 7200.
  - **run 20으로 축소(구 30).** L-SHADE 포착률이 PSO보다 높아 다중재시작 부담이 작다(단 실제 포착률은
    full 실측 대기 — 미결 ①). CLI `--budget-coef`로 오버라이드 가능.
- **첫 실행은 반드시 dev.**
- `n_ess`는 프로파일에 포함하지 않는다(별도 인자).
- **★ full 예산은 안전 상한이다(미결 ②).** 100·N_init는 PSO 세대100 등가의 안전 상한이지 L-SHADE에
  필요한 최소가 아니다. 편입 후 50·N_init로 포착률·j_net이 유지되는지 재보고, 유지되면 다수기·Phase2
  부담을 위해 상한을 낮춘다.

### 기수 증가의 계산 비용 (12차 n=2 실측 — 예상 확인됨)
- **기수 n 증가:** sgen이 n개, LP가 n기 SOC·교차항을 갖지만 **조류계산 횟수는 120회로 불변**
  (시나리오 × 시각이지 기수와 무관). **n=2 통합 실측: run당 ~1540s = n=1의 2배 미만** — "평가시간 증가
  미미" 예상이 대체로 확인됐다(2배 미만은 LP 변수증가·교차항이 있으나 active mask로 소멸 기가 solve를
  안 하고 (bus,S) 공유측정 dedup이 작동한 결과).
- **예산 증가:** 평가 횟수가 예산에 비례. 단 LPSR+캐시 완충으로 wall은 예산에 선형 미만(위 기수별 입자수
  소절).
- → 비용을 지배하는 것은 기수가 아니라 **eval_budget × 독립실행 수**이며, 캐시 히트율로 후반 부담이 준다.

### ★ 기수별 입자 수를 차원에 비례시킨다 — `N_init = 12·D` (12차, 원칙 반전)
- **★ 12차 개정 (종전 "사전 비례 금지" 반전).** 종전은 "고정 입자수+사후조정"이었으나, L-SHADE 채택과
  함께 **초기인구를 차원비례 `N_init = 12·n_dims`(D=3n)로 확정**했다(L-SHADE 논문 권장 18·D보다 보수적;
  저차원·좁은골짜기 지형이라 과하지 않게). 워커수·입자수 정수배 무관이 실측 확인돼(bench) 36·72 같은 값도
  병렬 페널티 없다.
- **★ 통제변수는 "동일 예산"이 아니라 "동일 탐색충분성"이다.** 우리가 기수별로 비교하는 것은 j_net 절대값
  (최적기수 곡선)이다. 이 비교에서 중요한 건 각 기수에서 **진짜 최적을 찾았는가**이지 같은 예산을 썼는가가
  아니다 — n=3(9D)을 n=1과 같은 예산으로 묶으면 최적을 못 찾아 "n=3이 나쁘다"가 나오는데 이게 "경제성이
  나빠서"인지 "탐색부족"인지 구분이 안 된다(더 치명적 혼선). 예산·인구를 차원비례로 주고, **각 기수가 충분히
  탐색한 최적끼리 비교**하는 것이 공정하다.
- **탐색 충분성은 사후 검증한다(예산은 결과변수).** 재시작분산(run간 gbest)·탐색실패 카운터(0이어야)·우수
  골짜기 포착률이 "이 기수에서 예산이 충분했나"의 증거다. 깨끗하면 적정, 지저분하면 인구·예산 증액. 예산은
  고정상수가 아니라 이 지표가 통과할 때까지의 결과변수다.
- **L-SHADE 시간이 폭발하지 않는 이유:** 총 평가횟수 = eval_budget이지 "인구×세대"가 아니다(LPSR로 인구가
  N_init→4 선형감소, 평균인구 ≈ N_init/2). N_init을 키워도 총 평가횟수는 예산으로 고정되고, 인구는 평가의
  분포만 바꾼다. 시간을 늘리는 건 인구가 아니라 예산 자체인데, 후반 평가는 인구 작고 캐시 뜨거워(96s→14s
  실측) 거의 공짜라 wall이 예산에 선형으로 안 는다. n=2 통합 run당 ~1540s로 n=1의 2배 미만이 이를 방증.
- **판정 기준 (8절-5의 run 독립실행에서 공짜로 얻어진다):**
  | 관측 | 해석 |
  |---|---|
  | gbest_f 분산 小 + gbest_x 뭉침 | 어디서 출발해도 같은 곳 도착 → **전역최적의 강한 증거** |
  | gbest_f 분산 大 | **탐색 부족** → 인구·예산 증설 |
  | f는 뭉치는데 x는 흩어짐 | 명세가 예상한 **위치 무차별성**(8절-5). f도 흩어지면 무차별이 아니라 실패 |
- **왜 수렴곡선이 아니라 재시작 분산인가 (중요, 불변):** "수렴곡선 평탄화"는 최적화기의 소진수렴(PSO는 w
  감소, L-SHADE는 LPSR 인구감소)일 뿐 전역최적 도달을 함의하지 않는다. 비볼록 문제(b 이산, 전압페널티
  조각적)에서 국소해도 곡선이 똑같이 평탄하다. 판정은 반드시 run간 gbest 분포로 한다.

### 구현 주의
- Windows `spawn` 방식이므로 Pool 생성은 반드시 `if __name__ == "__main__":` 가드 안에서.
- Pool은 한 기수 안에서 **재사용**한다(run마다 새로 만들면 `init_worker`의 기저 조류계산
  120회 캐싱 비용을 매번 치른다).
- `MKL_THREADING_LAYER`를 `os.environ`으로 **설정하지 말 것**(7절). 읽어서 경고만.

---



## 8. 후처리 지표 (뼈대부터 포함, 최적화에 개입 안 함)

### 편익 분해 (목적함수는 통합, 후처리는 분리)
- **B_energy** (통합): 손실+조달비.
- **B_arb** (조달비 분리): `Σ_s N_WD[s] Σ_t (P_dis[t]−P_ch[t])·SMP[s][t]·Δt` — ESS 순방전×SMP.
- **B_loss_avg** (AVG_DAYS 손실 분리): 기존 정의 유지, **음수 가능** 주석 유지. **★ 12차 명칭:** 본문 분해
  라벨을 `b_loss`→`b_loss_avg`로(AVG만이라 "전체 손실"로 오해방지). 단 **편익 비중 섹션의 분모는 총편익**이라
  거기선 `b_loss_total`·`b_arb_total`(peak일 포함)을 쓴다 — avg만 넣으면 peak분이 빠져 share 합이 100%가 안
  된다. CSV 컬럼명(`b_loss`)·benefits 함수명은 불변, postprocess 표시 계층만 변경. `b_loss_q`/`b_loss_p`/
  `b_loss_total`은 별개 개념이라 명칭 불변.
  - **★ P/Q 분해 (6차 신설, 지표 2+5).** `p_slack_ess`와 `p_slack_ess_qzero`(둘 다 이미 `schedule` CSV에 존재)의 차이가 **무효전력 순가치**, 나머지가 유효전력 성분. → `runs` CSV에 `b_loss_q`, `b_loss_p` 신규 컬럼, `postprocess` CSV에 `b_loss_q_share_of_gross_pct` 신규. B_loss_Q>0이면 Q가 손실을 통해 순편익을 만든다는 실측 증거.
  - **★ 음수 B_loss 추적 (6차, 지표 6).** 심야 대량충전 I²R로 `b_loss_p`가 음수가 되는 현상(5차 발견)이 새 파라미터에서도 나타나는지 확인. 위 P/Q 분해로 "음수는 P 성분, Q 성분은 양수"임을 분리해 보인다. → `postprocess` CSV에 `b_loss_p_negative`(bool), `b_loss_p_min` 신규.
- **B_defer** (이연): 위 3절.
- **★ 음의 B_defer 발생 카운터.** 최적화 중 `B_defer < 0`인 입자가 실제로 나왔는지 기록
  (3절 클리핑 금지 결정의 사후 확인). 발생 횟수·최소값을 남긴다. **★ 12차: n=1에서도 실질 음편익이
  나온다**(탐색경로의 열등 배치가 피크를 키움 — 3절 정정표). `min_bdefer`는 전 run 합산 최솟값이라
  지역해를 거친 run이 있으면 백만원 단위 음수가 남는다. "n=1은 잡음" 서술은 폐기(3절).
- **검산:** `B_arb + B_loss ≈ B_energy` (항등식. 안 맞으면 버그).

- **★ 편익 비중(share)의 분모는 총편익이다 (2026-07 5차 정정).** 각 편익의 비중은
  **총편익 `B_gross = B_defer + B_arb + B_loss`(= `B_defer + B_energy`)로 나눈다.**
  순편익(J_net)으로 나눈 비율은 물리적 의미가 없다(J_net = 편익−비용이라 편익항을 J_net으로
  나누면 합이 100%를 넘는다 — 초기 구현이 이 오류로 세 share 합이 444%였다).
  - **full(n=1) 실측 (총편익 대비):** `B_defer 84.4% / B_arb 14.2% / B_loss 1.42%`.
    참고 `cost/B_gross ≈ 77.0%`, 따라서 순편익률 `J_net/B_gross ≈ 23%`.
  - ⚠️ **문구 정정:** 이전 서술의 "손실 편익 6%"는 **순편익 대비**로 잘못 계산한 값이었다.
    총편익 대비로는 **1.42%**다. b_loss가 작다는 결론은 같지만 인용 숫자를 6%로 쓰지 말 것.

### 비용·순편익
- CAPEX, OPEX, Cost(=CRF_20·CAPEX+OPEX), **J_net**(=B_energy+B_defer−Cost).
- **★ cost 3분해 (12차 신설):** `capex_power_annual`(=CRF_20·C_KW·S, 출력분) / `capex_energy_annual`
  (=CRF_20·C_KWH·E, 용량분) / `opex`(=benefits.opex(S,E), warranty 포함). **검산: 세 항 합 = cost**
  (CRF_20·(power+energy)=CRF_20·capex이므로 정확 성립, rtol=1e-9). n=1 실측: capex_energy 7.44e6 ≫
  capex_power 5.92e5(12.6배) — E 중심 자산임을 정량화. postprocess (1)절 cost 아래 출력.
- 각 편익 크기 비교 출력(예상·실측 확인: B_defer >> B_arb_total > B_loss_total).
- **후처리 요약은 median을 주값으로 쓰되 mean을 병기**한다. PSO 재시작 분포는 최적권 내
  열등 버스·(다수기의) 탐색실패 run 때문에 heavy-tail이라 mean이 아래로 끌린다
  (full n=1 실측: median 3.0887e6 > mean 3.0787e6). median≫mean이면 아래꼬리 신호.
- **★ 탐색실패 run 카운터:** `j_net < 1e3`인 run(전 기 S=E=0 소멸)을 별도 집계하고
  중앙값·비중 계산에서 **제외**한다(제외값 기본, `_all` 접미사로 전체 병기). n≥3 dev에서
  실제 1건 발생했다(warm-start 잡음만 남아 j_net≈0.03원). n=1 full에서는 0건.
- **★ 경계 근접은 플래그가 아니라 실제 비율로 기록**: `s_boundary_frac`,`e_boundary_frac`
  = `(값−lo)/(hi−lo)`. full 실측 E=0.040, S=0.073 → 둘 다 경계 무관(E 상한 10.2 유지의 방어 근거).

### 운영·설비 지표 (프롬프트 6절)
1. **η_peak** = ΔP_peak / S_rated (상한 1.0).
2. **지속시간** = E_rated·DoD / S_rated (손익분기 한계 **3.81h** 참고, 조달비 포함 기준).
3. **E/S비** (kWh/kVA, x_max≈4.234 참고).
4. **EFC** (등가 전 사이클) = `Σ|P_net|·Δt / (2·E_usable)`, E_usable=E_rated·DoD.
   - **★ 연간 EFC 회계정합 (12차):** 종전 "AVG 247일 순가중 + peak 별도보고"를 회계(`_accounting_values`)와
     동일하게 정정. `benefits.select_peak_day`가 고른 **단일 peak 계절**만 N_WEEKDAYS에서 −1일 하고 그 1일을
     peak EFC로 대체하며, **선택 안 된 다른 계절 peak은 연간합에서 제외**(회계에서도 버려지므로). peak_day는
     회계와 100% 정합 위해 저장된 값을 재사용. n=1 실측 298.55회/년(summer_peak 선택). 두 peak EFC는 참고로
     병기하되 연간합엔 선택된 것만 반영. shoulder는 peak 없어 그대로.
   - **활용도:** Lazard 350회/년과 비교(과소=활용저조 → 경제성 악화 원인, 과다=수명위협). n=1 실측 298.55로
     과소 = **활용 저조, "ESS가 arbitrage 머신이 아니라 이연 자산으로 작동"의 정량 근거**(b_defer 지배와 정합).
   - **20년 달력수명 위협:** 연간 EFC가 Lazard 수명가정(350회/년=7,000회/20년)을 넘는지.
     ※ 평일 247일 기준이라 과소평가(주말 제외). 실제 365일은 더 많되 주말은 운영강도 약해 평일비례만은 아님.
     → **판정:** 247일 주값이 이미 350 밑돌면 실제도 안전. **보조 상한점검:** 최악가정(주말=평일) 365/247
       스케일해도 350 미만인지 병기(n=1 실측 441로 이 최악상한은 초과 — 주값 298이 판정값).
5. **b\* 분포** (30회 독립실행). **★ "최적권이 넓다"의 근거는 스윕이 아니라 PSO 결과 내부다
   (2026-07 5차 근거 교체).**

   #### full(n=1, 30 run) 실측 — 위치는 유의하지만 최적권이 넓다
   30 run이 **4개 버스**로 갈렸고, 그 네 해의 j_net이 **1.08% 이내**에 모여 있다:

   | 버스 | run 수 | j_net (자기 최적 용량) |
   |---|---|---|
   | 15 | 18 | 3,088,697 (최고) |
   | 16 | 3 | 3,087,884 (−0.026%) |
   | 31 | 1 | 3,057,695 |
   | 32 | 8 | 3,055,217 (−1.08%) |

   - **서로 다른 시드가 서로 다른 버스로 수렴했는데 j_net이 1.08% 이내** → 여러 골짜기가
     거의 같은 깊이. 이것이 "최적권이 넓다"의 **직접 증거이며, PSO 내부 데이터만으로 성립**한다
     (외부 스윕에 의존하지 않는다). 후처리 지표 `bstar_jnet_spread_pct`(버스 그룹별 median 간
     spread)와 `bstar_distinct_count`가 이 값을 산출한다. dev(3 run)에서는 표본 부족이라
     `bstar_sample_warning`이 뜨고, full(30 run)에서 비로소 의미를 갖는다.
   - **15와 16은 실질 동률**(0.026% 차이). 어느 쪽을 골라도 무방. 31/32(다른 피더)는 약 1% 열등.

   #### ★ PSO가 전역최적을 찾았는가 — 두 스윕으로 확증
   `scripts/probe_bus_sweep_full.py`: **버스 15의 최적 용량(S=0.17563, E=0.41143)을 전 버스에
   고정**하고 32버스 전수 평가.
   - **버스 15가 32개 중 1위** (2위 b=14, 3위 b=16; 페널티는 전 버스 0).
   - full에서 각 버스가 **자기 최적 용량**을 찾았을 때도 15 > 16 > 31 > 32로 15가 1위였다.
     → **자기최적·고정용량 양쪽 모두 15가 최적**이므로 PSO의 위치 탐색이 옳았다(놓친 우월 버스 없음).
   - ⚠️ **고정용량 스윕은 "편향된 단면"임을 유의**: 각 버스의 진짜 최적 용량은 서로 다르므로
     (버스별 최적 S가 0.1750~0.1792), 15가 아닌 버스의 j_net은 그 버스의 잠재력을 **과소평가**한다.
     따라서 이 스윕은 **버스 간 최종 순위표가 아니라 "놓친 우월 버스가 있는가"의 스크리닝**이다.
     (dev 시절 이 스윕에서 보였던 "다른 버스가 미세하게 15를 이김"은 용량고정의 인위적 산물이었고,
     각 버스가 자기 용량을 찾는 full에서는 사라졌다.)

   #### 위치 의존성의 메커니즘 (dev 스윕 — 참고자료로 격하)
   > **아래 dev 스윕(`scripts/probe_bus_sweep.py`)은 "왜 위치가 중요한가"의 메커니즘 규명용
   > 참고자료다.** 최적해 (S,E)를 고정하고 b=1~32 전수 평가한 것으로, **PSO 결과 품질 평가에
   > 쓰면 순환논리**가 되므로(최적해를 가정하고 시작하므로) 그 용도로는 쓰지 않는다. b* 분포의
   > 주 근거는 위 full 30 run이다.

   - **의존성의 근원은 이연이 아니라 손실(b_loss)이다** (당초 "손실이 작아 위치 무차별" 예상과 정반대).
     b_defer(편익의 84%)의 위치 폭은 6.8%로 거의 무관 — 슬랙에서 본 순주입은 위치와 거의 무관하고
     차이는 손실분뿐. b_loss는 작으면서도 위치 의존성을 거의 전적으로 만든다.
   - **소용량과 대용량의 최적 위치가 역전**(스피어만 −0.379): small은 b_loss가 전 버스 양수라
     말단 유리, large는 b_loss가 대부분 음수(심야 대량충전 전류가 긴 선로에서 I²R 급증)라 말단 불리.
     "최적 ESS 위치는 용량에 의존한다"는 실무적 발표 재료.

   > ⚠️ **부록 C.3-5의 MISOCP 기각 근거 중 "위치 무차별성을 분포로 보이는 결과"라는 서술은 무효다.**
   > 다만 기각 결론 자체는 유지된다 — b* 분포는 여전히 주요 결과이며("무차별"이 아니라 "최적권이
   > 넓다"는 형태로, full 30 run이 근거), 확률적 탐색이라야 분포를 얻는다는 논리도 그대로다.
6. **무효전력 가치** (Q가 배전망에서 값어치를 갖는가 — 핵심 연구질문).
   - **★ q_ratio 스윕은 6차에서 폐기됐다.** q_ratio 변수가 사라지고(2절·2-B) Q가 하위 QP의
     시각별 결정변수가 되었으므로, "q_ratio를 0→1로 스윕"하는 진단은 성립하지 않는다.
   - **대체: Q≡0 강제 vs Q 자유 비교.** 최적해에서 Q=0 강제한 j_net과 Q 자유 j_net의 차이가
     무효전력 순가치다. 이는 **8절 신규 소절의 `b_loss_q`**(schedule의 `p_slack_ess` vs
     `p_slack_ess_qzero`)로 이미 측정된다. 스윕 곡선 대신 이 단일 비교값이 "Q 자유도의 가치"를 준다.
   - **★ 11·12차 확정 — b_loss_q 양수, Q는 손실 채널에서도 실질 가치.** PCS 무효전력 변환손실을 목적함수에
     편입(11차)하자 `b_loss_q`가 **−444,114 → +474,078원(부호 반전)**. 12차 n=1 실측 `b_loss_q=+474,482`,
     `b_loss_p=+242,607`(둘 다 양수) — **무효전력 손실저감이 유효전력의 약 2배 기여.** `q_to_s_ratio≈0.70`,
     `poly_binding_frac≈0.53`, `s_util_median≈1.0`으로 PCS 용량 한계까지 Q를 밀어쓴다. Q가 순수하게 만든
     슬랙 감소 +0.109 MW(양수).
   - **★ Q 가치 확정 (11차 boundary 실험).** Q 순가치 dj_net = +743,004원 = j_net의 18.78%. s_util_cap을
     1.0→0.80으로 낮추면 j_net이 단조·매끄럽게 감소(무차별 구간 없음) → **Q를 PCS 한계까지 공급하는 것이
     인위적 안착이 아니라 진짜 최적.** b_defer는 Q와 무관(이연은 순수 P 채널), Q는 arb+loss 에너지 채널.
     envelope theorem(Q 미세배분 무차별, s_util_median<1이라 일부 시각만 boundary)과 화해: "Q 총량은
     많을수록 이롭고, 그 시각 간 배분은 평평해 무차별".
   - **★ 서사 확정:** **"무효전력은 배전망에서 실질 가치를 가지며 한계까지 공급이 진짜 최적"** — 핵심
     연구질문에 명확한 긍정. 9차의 "Q 가치가 손실 아닌 전압·용량 채널" 해석은 **PCS 손실 미편입의 인공물이라
     폐기**(11차). 옛 표현("무효전력 순가치 없음"/"손실 아닌 전압채널")이 부록 등에 남아 있으면 이 확정으로 교체.

### 추가 지표
7. **평균일/최대일 스케줄 유사도.** 계절 **내부**에서만 비교(summer avg↔summer_peak,
   winter avg↔winter_peak). 계절을 섞으면 "평균/최대일 차이"인지 "여름/겨울 차이"인지 구분 불가.
   - **정량화:** 피어슨 상관 + S로 정규화한 L1(`Σ|P̂_avg−P̂_peak|/24`) + **방전 봉우리 시각 집합**.
   - **★ 방전 시각은 `dis_peak_hours`(최댓값 90%↑ 시각 전부)로만 본다 (2026-07 5차 확정).**
     argmax 단일시각과 SMP 가중 무게중심은 **폐기**했다 — 방전이 이중·삼중봉이면 존재하지 않는
     중점을 반환해 오도한다(실측: summer_avg 이중봉 `0;16` → 무게중심 t=8은 방전이 0인 시각).
     봉우리 집합과 SMP(schedule CSV에 이미 있음)를 놓고 **사후 분석으로 직접 해석**한다.
   - **★ 유인충돌 판정(자동):** corr≥0.7 "aligned" / 0.3≤corr<0.7 "partial" / corr<0.3 "conflict".
     대표 기는 첫 `is_active` 유닛 기준.
   - **full(n=1) 실측:** summer=**"conflict"**(corr≈0.009 — 거의 무상관), winter="partial"(corr≈0.47).
     여름 평균일은 이중봉(t=0·16, SMP 추종)이고 최대일은 단봉(t=18, 부하 추종)이라 어긋난다.
     이는 6-A절 유인충돌의 정량 확정이며, **실패가 아니라 발견**이다("같은 설비가 그날 조건에 따라
     차익거래/피크저감으로 모드 전환" — 9절 서사의 정량 근거).
   - ※ 유사한 경우의 근거는 "같은 정책"이 아니라 **SMP-부하 高상관(full 실측: summer 0.971,
     winter 0.984)**으로 서술할 것. 낮으면 그 상관이 그날 조건에서 깨진 것이다.
   - **기수별/기별 지표는 units 파일로 분리**한다: `postprocess_n{k}_{ts}.csv`(run 수준, 기수당 1행)와
     `postprocess_units_n{k}_{ts}.csv`(기 수준, 최우수 run의 기마다 1행 — 소멸 기는 NaN·is_active=False).
     `effective_n_units`(active 기 수)의 분포로 "다수기가 단일기로 붕괴"를 통계 확인한다
     (full n=1은 자명; n≥2에서 1로 수렴하면 최적기수 1의 통계 확증).

> 지표 목록은 결과 분석하며 확장 가능한 구조로 둘 것.

---

### 무효전력·손실·QP 진단 (6차 신설)

QP 손실 편입·계수 재산출·LinDistFlow 전압(2-B, 3-C)의 사후 검증 지표. 대부분 기존 `schedule` CSV 컬럼에서 집계되어 추가 비용이 거의 없다. 전부 최적해 기준. 각 지표의 저장 위치·컬럼명을 명시한다(신규 컬럼명은 6차에서 정한다).

| 지표 | 내용 | CSV 파일 | 컬럼명 |
|---|---|---|---|
| **무효전력 활용** (1+3) | 총 ΣQ, 비영 시각 수, 중앙값, 최대, Q/S 비 | postprocess | `q_sum_mvar`, `q_nonzero_hours`, `q_median_mvar`, `q_max_mvar`, `q_to_s_ratio` |
| — 시나리오별 Q | AVG(계절별)·PEAK별 Q 기여 (피크일 가설 검증) | postprocess | `q_contrib_summer`, `q_contrib_winter`, `q_contrib_shoulder`, `q_contrib_peak` |
| — 원 시계열 | 시각별 Q (기존) | schedule | `q_mvar` (기존) |
| **연간 PCS 손실** (A) | `(1−ETA_PCS)(√(P²+Q²)−|P|)` 연간 집계(원) | postprocess | `pcs_loss_annual_won` |
| — 원 시계열 | 시각별 PCS 손실 (기존) | schedule | `loss_pcs_mw` (기존) |
| **다각형 바인딩** (B) | PCS 한계 구속 시각 비율 | postprocess | `poly_binding_frac` |
| — 원 시계열 | 시각별 바인딩 여부 (기존) | schedule | `poly_binding` (기존) |
| **겉보기전력 이용률** (C) | 중앙값·최대·고이용 시각수 | postprocess | `s_util_median`, `s_util_max`, `s_util_high_hours` |
| — 원 시계열 | 시각별 이용률 (기존) | schedule | `s_utilization` (기존) |
| **Q 방향** (D, Phase2 대비) | Q 부호 범위 | postprocess | `q_min_mvar`, `q_max_mvar` |
| **QP 손실모형 정확도** (7) | LP 예측 손실저감 vs AC 실제의 괴리 | postprocess | `qp_loss_model_err_max`, `qp_loss_model_err_mean` |
| **재산출 효과+수렴** (8+E) | P=0계수 해 vs 재산출 해 j_net 차이, Δ(2−1)/Δ(1−0) | postprocess | `recompute_jnet_delta`, `recompute_convergence_ratio` |
| **캐시 통계** (9) | 히트율, 고유(b,S)수, 계수측정 조류계산 총수·벽시계 | postprocess | `coef_cache_hit_rate`, `coef_unique_bs_count`, `coef_runpp_total`, `coef_measure_wall_s` |
| **LinDistFlow vs AC 전압오차** (11) | 근사 전압 vs AC 전압 최대차 | schedule / postprocess | schedule `v_lindistflow`(신규) / postprocess `max_lindistflow_ac_voltage_err` |
| **μ 전압 페널티 활성** (10) | 발동 여부·시각·버스 | runs / schedule | runs `penalty_v`(기존, 세분화) / schedule `mu_penalty_active`(신규) |
| **피크일 이중 기여** (12) | 피크일 (에너지+이연) 각 성분 | postprocess | `peak_day_energy_benefit`, `peak_day_defer_benefit` |

**주요 지표 해설:**
- **무효전력 활용·PCS 손실**: 핵심 연구질문("ESS 무효전력이 배전망에서 가치를 갖는가")에 답한다. 시나리오별 Q 분해로 설계 가설(**피크일 Q 손실저감이 최대**)을 실측 확인한다.
- **다각형 바인딩**: ESS가 S 제약(용량 한계)에서 운전하는지 E 제약(에너지)에서 운전하는지 구분 — Q가 남는 용량을 쓰는지의 근거. POLY_N=128 결정과 연결.
- **QP 손실모형 정확도·재산출 효과·캐시 통계**: 방법론 방어. 프로토타입 정확도(0.05%)와 C0/X0 대비 C1/X1 영향이 실제 최적해에서도 미미한지 진단 경로로 계속 확인한다. `coef_cache_hit_rate`는 p_center=0 C0 공유의 실효성을 보여준다.
- **LinDistFlow vs AC 전압오차**: `max_lindistflow_ac_voltage_err`를 게이트(0.005 pu)와 비교해 "sweep wide, judge narrow"가 최적해에서 통과하는지 판정.
- **피크일 이중 기여**: 3-C.5 회계 변경(피크일 에너지+이연 동시 계상)의 각 성분을 분리 기록한다.

**Q 한계가치 / envelope (지표 4, 미구현·예정):** 최적해에서 Q를 ±x% 섭동해 j_net 변화를 재는 지표. **사후 CSV로는 계산 불가**(섭동점의 P·SOC 재최적화 + AC 재평가가 필요하며 저장돼 있지 않음). 프로토타입에서 확인된 "Q 민감도 낮음(포락선)"을 최종해에서 재확인할 필요가 있을 때 별도 섭동 실행으로 측정한다.

---

## 9. 연구 방향성 (합의)

3층 결론구조:
1. **손익분기 부등식** (구조적 조건, 정성적 서술용): η상한 1.0 → 최대지속 **3.81h**(조달비 포함).
   `c_cap·η_peak + γ·DoD·(E/S) > (CRF+w)(c_kWh·E/S + c_kW) + OM·(E/S)` (6절 참조, 근사).
2. **PSO로 달성 가능한 최대 η 도출 → 경제성 판정** (정량, 후처리 지표로).
3. **Phase 2가 격차 메우는 조건** (PV 침투율, 조달비).

**우선순위: Phase 1 vs Phase 2 비교가 우선.** Phase 2에서 양수 결론을 지향하되,
Phase 1의 부호 자체를 목표로 삼지 않음. 두 Phase를 **동일 기준**으로 평가해 공정 비교하는 것이 핵심.
(※ 편익을 인위적으로 낮추거나 높이는 선택 금지 — 비교를 왜곡함.)

### 운영 모드 전환의 정합성 (완전예지 상한의 방어)
운영자는 평범한 날엔 조달비 편익 최대화(저가충전-고가방전), 연중 최대부하 예상일엔 피크저감 최대화.
이 모드 전환이 정당한 이유: **최대부하일 예측이 실제로 잘 맞음**(폭염·한파 예보·요일·계절로 며칠 전 예측).
피크일 판별은 예측 정확도가 높아 완전예지와 실제의 괴리가 작음. 최대일은 특정 하루가 아닌
**연중 상위 5일 평균**이라 "이런 극단일이 오면 이렇게 대응한다"는 대표적 피크대응(운영지침 아님).
같은 설비를 그날 조건에 따라 다른 정책으로 운영 — 설비는 하나, 운영정책만 전환.

### 배전망을 택한 근거
편익 귀속 주체 명확(단일 배전사업자), 배치(b) 변수가 의미를 갖는 유일한 층위,
R/X 高 배전망 고유 물리(유효전력↔전압·손실 직결, 무효전력 전압개선 미미),
분산전원 통합 이슈의 현장(Phase 2 정합), 정책적 시의성(11차 계획으로 배전 투자 확대),
IEEE 33-bus 표준 벤치마크.

### 평일만 사용한 근거 (층위별)
편익을 낮추려는 게 아니라 **평가 범위를 "전형적 운영일"로 정의**한 것.
1. **정의:** 평일은 산업·상업 정상가동일로 배전 계획·운영의 기준 조건. 주말·공휴일은 수요패턴이
   질적으로 다른 별개 모드 → 하나의 대표일로 평균 내면 두 모드가 뭉개진 혼합일(어느 쪽도 대표 못 함).
   즉 "대표성 포기"가 아니라 "이질적 모드를 안 섞으려는 분리". (필요시 주말 대표일 별도 확장 가능.)
2. **이연 물리 (견고):** 설비 이연은 연중 최대 피크가 결정 → 거의 평일 발생(산업수요). 주말은 피크 안 만듦.
   주말 포함 여부와 이연 편익 무관(인과 깨끗함).
3. **비교 일관성:** 주말 포함 시 일평균 편익↓·일수↑라 연간 총합 방향 불분명하나, 에너지 편익이 총편익에서
   작고(이연 지배) 이연은 평일 불변 → 결론(Phase 1 vs 2 비교) 안 바뀜. **Phase 1·2를 동일 평일기준으로
   평가하는 한 비교는 공정.**
4. (보조) 전력분석에서 평일/주말/공휴일 분리는 요일·공휴일 효과로 인한 표준 관행.
> 심사 대비: "주말 편익은?" → "평일 기준 편익을 평가했고 주말·공휴일은 수요·이연 기여가 작아 제외.
> 연간 총편익 완전산정엔 주말 대표일 추가가 후속 과제." (음수/양수 어느 쪽으로도 편향 없는 서술.)

---

## 10. 미결/보류 (나중에 다룸) — 12차 갱신

**★ 12차 즉시 미결 (다음 세션 로드맵):**
- ① **L-SHADE 실제 포착률 (full):** SeedSequence 복원 후 dev 3run 중 1개가 bus16 지역해로 갈림(probe의
  100%는 저품질 연속시드 덕이었을 가능성). full run20으로 실측 필요. 실패율 유의하면 인구(12·D→더 크게)·
  예산 증액 검토.
- ② **예산 절반 실험:** full 예산 100·N_init(PSO등가 안전상한)를 50·N_init로 낮춰도 포착률·j_net 유지되는지.
  유지되면 다수기·Phase2 부담 절감.
- ③ **n=2 최적기수 L-SHADE 재확정:** 편입 후 n=2 재실행. 1기붕괴 재현되면(더 나은 탐색기서도 붕괴)
  알고리즘 무관 결론 확정. n=3까지 확인(꺾인 뒤 한 기수 더).
- ④ **테스트 전면 갱신:** test_qp_regression·test_evaluate·test_lp·test_benefits를 최신 최적해(b=31/32·
  통합solve·L-SHADE·3n) 기준으로 일괄 손봄. 시뮬레이션은 정상작동하나 테스트가 낡음.
- ⑤ **"16+32 분산 강제배치" 직접측정:** 1기최적을 "가설"에서 "측정된 결론"으로. bus 고정·S,E만 최적화해
  j_net<1기해 확인. 통합 LP라 이제 공정 측정 가능.
- ⑥ **그룹C 결측:** `qp_loss_model_err`(LP 시각별 예측손실 미저장) 계측 추가.
- ⑦ **중간발표 PPT:** 손익분기 부등식 / Q 실질가치·한계공급 최적 / PV 전후 비교 서사.

**설계·구현 완료(참고):**
- ~~다수기 통합 solve_peak·solve_avg + 교차항~~ → **구현 완료(12차).** 조인트 (n,T) QP, 대각근사 채택
  (교차항 위상의존 같은분기 8.2%/다른분기 0.1~0.2%). 최적 기수는 L-SHADE 다수기 실험이 확정(위 ③⑤).
- ~~시변 Q LinDistFlow 편입~~ → 6차 완료. μ(MU_VOLT) 4e7.
- ~~피크일 이중 기여 회계(3-C.5)~~ → 7차 구현·확정. EFC도 12차 회계정합(8절-4).
- **역률 민감도 스윕 (1절):** 0.92/0.95/0.97 강건성. Phase 2 이후.
- **Phase 2 (PV 추가):** clear/cloudy 시나리오 분리, Q<0 자동지원. Phase 1 확정 후.


## 부록 A. 모듈 구조 (제안)

폴더: `summer_ess_project/`. 분리 원칙 = **"바뀌는 이유가 같은 것끼리 묶고 다른 것끼리 분리"**
(파라미터 수정은 params.py만, LP 수정은 lower_lp.py만 건드리게).

```
main.py            # 진입점(오케스트레이터). 기수 루프(1기→2기→…) → L-SHADE 실행(병렬) → 후처리 → 저장.
                   #   ★ 12차: 알고리즘 중립 리팩터(gen 계약 해제, 배치단위 generations.csv, 예산 기반 종료).
lshade_core.py     # ★ 12차 신설. L-SHADE 엔진(numpy만 의존). objective 생성자 인자, {x,f,history,eval_count} 반환.
pso_core.py        # (구 최적화기, 12차 대체) L-SHADE로 교체됨. 회귀 대조용으로 남겨둠(삭제 안 함).
build_net.py       # 계통 준비: case33bw 로드 + 22.9kV/10MVA 스케일 + 선로정격 주입. 워커가 시작 시 1회 호출.
lower_lp.py        # 하위 분리 LP. solve_avg(조달비 min), solve_peak(피크 min). 입력(부하곡선,S,E,q)→스케줄 P[t]. cvxpy.
evaluate.py        # 평가 지휘자. evaluate_particle(x): 하위LP → 24h 조류계산 → 결과수집 → benefits → fitness. PSO↔나머지 다리.
benefits.py        # 편익·비용 공식(순수함수): B_energy/B_arb/B_loss/B_defer/CAPEX/OPEX/Cost.
                   #   ※ pandapower·cvxpy import 금지. MW/MWh 단일 단위(3절 단위 규약).
                   #   ※ b_defer는 pk_s를 인자로 받지 않는다(2절 구조적 강제).
postprocess.py     # 후처리 지표(8절). 최적해 받아 계산·출력. 최적화 비개입 사후분석이라 격리.
params.py          # 모든 상수 창고: CRF·비용·c_cap·LOAD/SMP·경계·PSO초기값. 나머지 전부 여기서 가져옴. "숫자는 전부 여기".
                   #   ※ kW/kWh 출처값과 MW/MWh 파생값을 함께 보관(3절 단위 규약).
test_lp.py         # LP 해석해 대조 검증(7절). 단순 SMP 장난감케이스로 lower_lp 정답 확인.
test_benefits.py   # benefits 손계산 대조 검증. 순수함수라 숫자 in/out만으로 검증 가능.
test_evaluate.py   # 통합 검증(불변량·극한). 조류계산 포함이라 무겁다 → slow 마커.
pytest.ini         # slow 마커 등록. `pytest -m "not slow"`로 무거운 테스트 제외.
scripts/           # 일회성 확인 스크립트(본 파이프라인 비포함). check_balance.py, probe_noise.py.
```

**데이터 흐름:** params(상수) → build_net(계통) → [main이 PSO 시작] → PSO가 x=(b,S,E,q) 제안 →
evaluate가 lower_lp(스케줄)+조류계산+benefits(편익)로 fitness 반환 → PSO가 x 갱신 반복 →
최적해 → postprocess(지표). test_lp는 곁가지로 lower_lp 검증.

**분리 이유:** (1) 관심사 분리(계통·LP·편익·최적화·후처리가 각각 다른 이유로 바뀜),
(2) 검증 격리(pso_core=검증완료, test_lp=검증도구), (3) Phase 2 확장(params 딕셔너리만 5→10 확장,
로직은 리스트 순회라 불변). Colab 실행은 `!python main.py` 단일 진입점(파일 수와 무관).

**구현 순서 (의존성 아래층부터):**
`lower_lp.py`(+`test_lp.py` 동시) → `benefits.py` → `evaluate.py` → `main.py` → `postprocess.py`.
- **lower_lp+test_lp 먼저:** params만 의존, 조류계산 무관한 최하층. test_lp(해석해 대조)를 같이 짜
  "결과 맞는지 모른 채 위층 쌓기"를 방지. 부호·효율·SOC를 장난감 SMP로 검증 후 진행.
- **benefits:** 순수함수(숫자 in → 원 out)라 손계산 숫자로 공식 검증 가능. B_arb+B_loss≈B_energy 검산도 여기서.
- **evaluate:** build_net+lower_lp+benefits+페널티를 엮는 통합점. 세 조각 검증 후 붙여야 버그층 좁혀짐.
  24h 조류계산 루프(numba, init='results', sgen 값갱신)도 여기.
- **main:** evaluate가 fitness 스칼라 반환 확인 후 pso_core에 꽂아 병렬 PSO. 기수 루프도 여기.
- **postprocess:** 최적해 형식 정해진 뒤 붙여야 두 번 안 고침. 최적화 비개입이라 마지막.

### 진행상황 (2026-07 갱신)

**완료·검증됨**
- `params.py` — 상수. **SOC는 `_FRAC` 접미사**(`SOC_INIT_FRAC/MIN_FRAC/MAX_FRAC`)로 보관, MWh 변환은 LP 내부.
- `build_net.py` — **0번 계통검증 완료.** ★ 2026-07 3차에서 슬랙 전압이 1.0 → 1.02로 바뀌어
  **검증값이 전면 재산출됐다(1절 표).** 현행 기준: 손실 296.86 kW / Vmin 0.9620 pu(bus 17) /
  line0 255.99 A / 기저 전압위반 0.0 pu. 구 기준(`vm_pu=1.0`: 310.06 kW / 0.9407 / 261.51 A)은
  회귀 검증용 참고값으로 남긴다.
- `pso_core.py` — 벤치마크 검증 완료.
- `lower_lp.py` + `test_lp.py` — **✅ 완료. 13개 테스트 전부 통과.**

**lower_lp 확정 사항 (4건 반영 완료)**
1. **SOC MWh 규약 전환 완료.** `_soc_constraints`/`_assert_physics`에서 `/E_mwh` 나눗셈 제거,
   `SOC_*_FRAC × E_mwh`로 절대량 변환. (0除 위험 근본 제거.)
2. **`solve_avg` degeneracy 안전장치 추가.** 목적함수에 `EPS_REG·Σ(P_ch+P_dis)`, `EPS_REG=1e-6`.
   (명세의 "η<1이라 동시충방전 자동배제"는 **SMP>0 & η<1일 때만** 참이므로 안전장치 필요.)
3. **`solve_peak` docstring 경고 추가** — 반환 `pk_s`를 B_defer에 직접 쓰지 말 것(2절 주의 참조).
4. **peak 해석해 검증 확장** — 7절 "테스트 설계 원칙" 및 "세 상한 min 구조" 참조.
   검증 과정에서 나온 결과들이 그 절에 전부 반영되어 있다(S=2/3/5 × E=100/12 스윕).

**테스트 구성 (13개) — 7절 LP검증 3항목의 분해**
| 7절 항목 | 대응 테스트 |
|---|---|
| #1 해석해 대조 | `avg_lossless`, `avg_eta90`, `peak_shaving`(S=2), `peak_S3`, `peak_S5`, E=12 스윕군 |
| #2 물리 보존 assert | 함수 아님 — `lower_lp._assert_physics`가 **모든** 테스트에 자동 삽입 |
| #3 경계 sanity | `avg_zero_spread`, `avg_zero_power`, `avg_energy_monotonic`, `peak_zero_power` |
※ 7절이 `solve_avg` 위주로 쓰여 **`solve_peak` 해석해 검증이 누락**돼 있었다 → 채워 넣은 것.

**`scripts/check_balance.py` — ✅ 실측 완료 (3절 "슬랙 수지 검증" 표 참조)**
- shunt·컨덕턴스 성분 없음 확인 → 수지식 추가 항 없음.
- 잔차 machine epsilon 수준, ESS 주입 크기 무관 → **atol=1e-7, rtol=0 확정.**
- 이 스크립트는 확인 전용이며 본 파이프라인에 포함되지 않는다.

**`scripts/probe_noise.py` — ✅ 실측 완료 (7절 "수치 잡음의 성격" 표 참조)**
- 잡음 원인이 sgen 존재가 아니라 **warm start 이력**임을 규명. 금전 영향 <7원/년.
- `tolerance_mva` 기본값 유지 결정. 원칙 4의 `atol=10.0` 근거를 제공.

**`benefits.py` + `test_benefits.py` — ✅ 완료. 15개 테스트 전부 통과.**
- 순수함수 모듈. pandapower·cvxpy를 import하지 않는다(숫자 in → 원 out).
- `params.py`에 MW 기준 상수 5개 추가(기존 kW/kWh 상수는 출처 추적용으로 보존).
- 구현 함수: `assert_slack_balance`, `b_energy`, `b_defer`, `capex`, `opex`, `total_cost`,
  `j_net`, `b_arb`, `b_loss`, 검산 함수.
- 교차검증(독립 산술 확인): `b_defer` 피크감소 3 MW → 76,082.2×1000×3 = **228,246,600원**,
  `capex(S=1, E=4)` → 36,541,180 + 878,112,680 = **914,653,860원**. 둘 다 보고값과 일치
  → **MW 단위 상수가 의도대로 작동함이 독립적으로 확인됨.**
- 음의 `B_defer` 테스트가 **정확히 대칭값(∓228,246,600원)** 으로 나와 클리핑 없음이 값으로 증명됨.

**`evaluate.py` + `test_evaluate.py` — ✅ 완료. 8개 테스트 전부 통과.**
- 설계 확정 사항은 **3-A절** 참조.
- 워커 전역 `_NET/_BASE_P/_BASE_Q/_BASE_FLOW` 패턴. `init_worker()`가 기저 조류계산 120회를
  **1회만** 계산해 캐싱 → 평가당 실제 조류계산은 ESS 주입 상태 120회뿐.
- **실측 평가 1회(n=1, 120 PF) = 0.685초** (초기 참고치 1.178초의 58%).
- 기저 재현 확인: 슬랙 1.02 기준으로 **재작성·재검증 완료(2026-07 5차)**. Claude Code가
  새 코드로 테스트했고 전부 통과. (구 `vm_pu=1.0` 기준값 310.06 kW/0.9407/261.51은 회귀 참고용.)
- **실데이터 수지 검증 통과** — 매 평가마다 sgen에 실제 주입한 배열로 `assert_slack_balance` 수행.
- `b_arb + b_loss ≈ b_energy` 실데이터 검산 통과.
- `params.py`에 `PENALTY_DIVERGE = 1e15` 추가, `pytest.ini` 신규(slow 마커).
- 전체 테스트 33개 통과 (`-m "not slow"` 25개 1.98초 / `-m slow` 8개 16.04초).

**`main.py` — ✅ 완료 (2026-07 3차). 설계 확정 사항은 7-A절 참조.**
- 착수 전 미정사항 3건(워커 수 / 시드 관리 / 중간 저장) **전부 해소** — 7-A절에 확정 기록.
- `evaluate.py`/`benefits.py`/`lower_lp.py`/`pso_core.py` 무수정. main은 순수 조립자.
- PSO 3n(b,S,E) 탐색 + `_expand_to_4n` 어댑터로 evaluate의 4n 시그니처에 접속.
- 첫 dev 실행(슬랙 1.0 시점)에서 **전압 페널티 지배 문제**가 드러남 → **3-B절** 참조.
  1차 조치로 슬랙 전압 1.02 확정(1절). ★ 4차에서 **λ는 현 조건에서 조정 불필요**로 정리
  (3-B절. 영구 확정 아님 — 재검토 트리거 표 참조).

**`scripts/bench_workers.py` — ✅ 실측 완료 (7절 "워커 수·입자 수 확정" 표 참조)**
- `N_WORKERS=16` / `N_PARTICLES=32` 확정. 20·24워커 사용 금지(시스템 정지 실측).
- 조합 간 차이가 재현 노이즈에 묻혀 **추가 실측은 무의미**하다는 것이 결론.

**`scripts/probe_voltage.py` — ✅ 실측 완료 (1절 "슬랙 전압 1.02" 표 참조)**
- 기저 전압위반 0.9169 pu 실측 → 슬랙 1.02에서 0으로 해소됨을 확인.
- ESS 최적해가 기저 위반의 **63.6%를 저감**하고 있었음도 이 값으로 확인(3-B절).

**`main.py` CSV 확장 (2026-07 4차)**
`runs.csv`에 편익 분해·페널티 11개 컬럼을 추가했다:
`j_net, b_energy, b_defer, b_arb, b_loss, cost, v_violation, i_violation,
penalty_v, penalty_line, decomposition_ok`
- **`--diagnose` 여부와 무관하게 항상 기록한다.** full 30 run에서 stdout으로는 분석이
  불가능하고, 깜빡 끄면 재실행 비용이 1시간이다. run당 gbest 재평가 0.7초 × 30 = 21초로
  비용은 무시할 만하다.
- 검산(`−j_net + penalty_v + penalty_line` vs `gbest_f`, atol=10원)은 **경고만 하고 죽지 않는다**
  (본실험 중간에 죽으면 손해가 크다).
- `--diagnose`의 역할은 "화면에서도 보기"로 축소됐다(CSV 값을 그대로 출력, 중복 평가 없음).

**진단·실험 스크립트 5종 — ✅ 완료**
| 스크립트 | 확정한 것 | 반영 절 |
|---|---|---|
| `probe_voltage.py` | 기저 위반 0.9169 pu → 슬랙 1.02에서 0 | 1절, 3-B절 |
| `probe_large_solutions.py` | 최적 S=0.176~0.25(한계 j_net 부호 전환), 피크시각 이동 포화, λ_V 무관 | 6-A절, 3-B절 |
| `probe_bus_sweep.py` | 위치 의존성 메커니즘(근원은 b_loss, 소/대용량 선호 역전) — **참고자료로 격하** | 8절-5 |
| `probe_split.py` (5차) | 분산 이득 "측정 불가"(현 분리LP의 d* 중복적용으로 b_defer 붕괴) | 2절, 7-A절 |
| `probe_bus_sweep_full.py` (5차) | 버스15 최적용량 고정 전수스윕서 **15가 32버스 중 1위** — PSO 위치탐색 확증 | 8절-5 |

**`main.py` full 실행 — ✅ 완료 (2026-07 5차, n=1, 100세대·30 run)**
- 실행: 데스크탑 **터미널 직접**(VS Code+Claude Code 경유 시 메모리 부족으로 다운 — 주의).
  파라미터 n_particles=32, n_iters=100, n_runs=30. wall_time 평균 308초/run, 총 154분.
- **★ 3-B 재검토 트리거 통과:** `v_violation`/`penalty_v`/`i_violation`/`penalty_line`이
  **30 run 전부 정확히 0**. 슬랙 1.02 결정이 full 규모에서 검증됨(λ 재조정 불필요 재확인).
- 검산 전부 통과: 편익분해 30/30, decomposition_ok 30/30, 발산 0, 탐색실패 0.
- **최적해:** b=15, S≈0.176, E≈0.420(E/S=2.36), **j_net median ≈ 3.089e6 원/년**.
  편익구조(총편익 대비) b_defer 84.4%/b_arb 14.2%/b_loss 1.42%. 결과는 6-A·8절에 반영.

**`postprocess.py` — ✅ 완료 (2026-07 5차). 설계는 8절 참조.**
- 사후분석 전용(최적화 경로 무수정). 출력: run 수준 `postprocess_n{k}` + 기 수준
  `postprocess_units_n{k}` + 원자료 `schedule_n{k}`(long format, p_slack_base/ess 포함).
- 게이트 검증 통과: 완주 / 탐색실패 카운트(n=3서 1건 포착) / `b_arb+b_loss≈b_energy` /
  cost 재계산 / 구 스키마 결측 컬럼 보고. share 합=100%±0.01 hard assert.
- 편익 share 분모=총편익, median+mean 병기, degenerate 제외 기본(+`_all`), 경계 실비율,
  유인충돌 라벨, `effective_n_units` 분포. **optimal_region(스윕 기반)은 제거** — 8절-5 참조.

**본코드 구현 — ✅ 7차 완료 (2026-07, dev 실행 직전)**
6차 확정 설계(2-B, 3-C)가 전 모듈에 구현되고 파이프라인 정합성이 검증됐다(dev 미실행). 모듈별:
- **`lower_lp.py`:** LP→QP 재작성. 손실 5계수 블록대각 quad_form(교차항 0, 다수기 대비 통합형), LinDistFlow μ(2-B.3), 다각형 POLY_N=128, 계수 SHA-256 해시 Problem 캐시. `return_voltage` 플래그로 v_lindistflow_sq(33행 슬랙포함 pu²) 반환. is_dcp(dpp=True) 확인. solve_peak 입구 `a_P<0,a_Q<0` assert + 방전시각 `−a_P·P>0` assert. **8차: `_PROBLEM_CACHE`를 OrderedDict LRU `PROBLEM_CACHE_MAXSIZE=16`(params)으로 — 무한 누적 MemoryError 방지.**
- **`loss_coeffs.py` (신설):** AC 조류계산으로 5계수 측정. 단위 L_cost[MW]/(P[MW],Q[Mvar]) — lower_lp의 SMP_PER_MWH 환산과 정합(direct-model 9.6e-7 일치 실측). 절편 없는 OLS + 열 정규화(작은 S 조건수 개선, 계수 불변). (b,S,s,t,p_center) 캐시 — **8차: S를 `S_CACHE_GRID_MVA=0.001` 격자에 반올림해 조회(방식 B, solve엔 연속 S), OrderedDict LRU `MEASURED_CACHE_MAXSIZE=8192`. 9차: 96조건 조밀격자 검증을 거쳐 기본 측정 그리드를 full25→min7(`LOSS_GRID_DESIGN`)로 교체(최악 손실면 오차 0.054%, full25 복원 가능).** strict 부호·PSD assert. 1080케이스 전수 무위반 확인. cache_info() 노출.
- **`evaluate.py`:** 기본 최적화는 `RECOMPUTE_ENABLED=False`로 1차 grid 공유 C0→X0만 사용해 2차 측정·재solve를 제거. 진단 경로는 X0/X1/X2·C0/C1/C2·시각별 재산출 지표를 유지하고, True로 종전 X1 최적화를 복원한다. S/E 비활성 조기분기(1e-6), `_run_schedule_ac`/`_accounting_values` 분리, decomposition_ok=**total 기준**.
- **`benefits.py`:** select_peak_day, b_energy_adjusted, b_energy_peak_day, b_arb_total, b_loss_total 신설(기존 함수 보존). PEAK_TO_SEASON 매핑.
- **`postprocess.py`:** 8절 지표 그룹 A/B/C. b_loss_q(동일 P·Q=0 재조류계산으로 순수 Q 기여 분리), 전압오차·μ 활성.
- **`main.py`:** 멀티코어 캐시 집계(반환값 편승+pid baseline 차분), 세대별 히트율 generations.csv, RUN_FIELDS에 recompute·캐시 컬럼.
- **테스트:** test_lp(coeffs 인자, Q 부호양방향), test_benefits(신규 함수+total 검산), test_qp_regression(부록 A 층1·2·부호). 전부 통과.
- **프로토타입 검증(6차 완료):** QP 손실근사 0.05%, PSD 전 케이스, 부호 규약, 괴리 B/C 1회 수렴, 캐시 E무관, POLY_N 스윕.
- **5차 full 결과(j_net 3.089e6 등)는 참고용** — Q 미반영 뼈대라 7차 dev/full 재실행 시 갱신.

**dev 완주 + 측정 가속 — ✅ 9차 완료 (2026-08)**
- **메모리:** 8차 LRU+양자화 후에도 8·4워커 크래시 → 워커당 피크 실측(약 650 MiB, 기저 ~150 MiB) → **8워커 완주 확정**(16은 메모리 위험, 12·10은 32입자에 안 나눠떨어져 idle 발생, 8이 최적).
- **min7 그리드(9-A):** `probe_coef_grid_reduction.py` 96조건 검증 → full25→min7 교체. dev서 최적해·편익구조 불변, run2 2.15배 단축.
- **경계 pull(9-B):** `probe_boundary_degeneracy.py` 180조건 → 정격경계 축퇴를 측정중심 pull로 해소(7점 비용 유지, 경계오차 0.036%).
- **재산출 완전 제거(9-C):** dev 후처리로 재산출 j_net 영향 연 100원(0.003%) 확인 → 최적화 경로에서 제거(`RECOMPUTE_ENABLED=False`), 진단 경로 유지. run2 1.90배 추가 단축.
- **누적 속도:** 활성 run **4,780초 → 1,169초(약 4.1배)**. min7 2.15배 × 재산출제거 1.90배.
- **★ 최적해 갱신(9-E, 이후 이 값을 기준):** **b=16, S=0.15018212, E=0.43580173, j_net=2,991,905원/년.**
  편익: b_energy=1,583,700 / b_defer=12,238,171 / b_arb=1,803,400 / b_loss=−202,950 / cost=10,830,000.
  구조: b_defer 88.5% / b_arb 12.9% / b_loss −1.5%. v_violation=penalty_v=0.
  b_loss_q=−444,114(음수 — Q 해석은 8절-6 정정 참조: 손실 아닌 전압·용량 채널 가치).
  pcs_loss_annual=2,492,264. q_to_s_ratio=0.787, poly_binding_frac=0.633, s_util_median≈1.0.
  eta_peak=1.071, 지속시간 2.612h(<손익분기 3.811h), E/S=2.902.
- **★ degeneration 문제 노출(9-D, 다음 세션 최우선):** dev 3 run 중 **2개(run0,1)가 S=E=0 비활성 평지에
  빠짐** — PSO가 "ESS 미설치(j_net≈0)" 국소최적을 탈출 못함. run2만 경제적 해로 수렴. postprocess가
  "유효표본 n=1<10" 경고. **full 30 run의 재시작 분산 판정이 무력화되므로, full 전에 탈출이 선결.**
- **후보4 기각(9-D):** 히트율 3.5%로 중복 원천 부족 → 캐시 공유 이득 6.5% vs 3모듈 수정, 회수 나빠 취소.
- **overflow 경고:** dev 로그에 `RuntimeWarning: overflow encountered in reduce`(초기 세대 발산입자
  fitness 집계). gbest 무해(diverged=0)하나 다음 세션서 원인 확인 대상.

### ★ 구현 세부 결정 (Q1~Q4, 7차 확정 — 본문 승격 전 기록)
6차 설계엔 없던, 본코드 구현에서 확정한 판단들. 다음 구현·수정 시 이 결정을 따른다.
- **Q1 (3층 분업):** loss_coeffs=측정 / lower_lp=solve(계수를 인자로 받음) / evaluate=재산출 조율. lower_lp·benefits는 pandapower를 조류계산 용도로 import하지 않는다.
- **Q2 (계수 상수 굽기):** 손실계수는 cvxpy Parameter로 두면 교차항 bilinear로 DPP가 깨진다 → **float 상수로 굽고** SHA-256 해시를 Problem 캐시 키에. S/E/load는 Parameter 유지. ~~**S 양자화는 미도입**~~ → **[8차 개정] S 양자화 도입 확정.** 계수를 상수로 구우면 계수가 바뀔 때마다 새 Problem이 생기는데, S 연속이면 매 평가마다 새 Problem이 무한 누적돼 **dev 첫 실행이 MemoryError로 크래시**했다. 해결: (1) Problem·계수 캐시를 **LRU 상한**(`PROBLEM_CACHE_MAXSIZE=16`, `MEASURED_CACHE_MAXSIZE=8192`, 둘 다 params) — 메모리 안전판. (2) **S 양자화(방식 B)** — 계수 캐시 키의 S만 `S_CACHE_GRID_MVA=0.001` 격자에 반올림하고 **solve·PSO는 연속 S 유지**(계수만 격자에서 측정·재사용). 검증: j_net 영향 0.02원, 인접 S 고유 키 85.7%↓, 0.0001 격자는 감소 0%라 기각. → 8차 이력 참조.
- **Q3 (재산출 제거·진단 보존):** 최적화는 p_center=0의 1차 계수 C0를 measure_coeffs_grid로 (b,S) 공유하고 X0를 최종 해로 사용한다. 운영점 C1과 X1, C2/X2는 run당 gbest 진단에서만 시나리오별로 계산한다. `RECOMPUTE_ENABLED=True`가 종전 경로의 회귀 스위치다.
- **Q4 (assert P항 한정):** pk 제약엔 loss_reduction 전항(`−a_P·P−a_Q·Q`) 들어가나, **검증 assert는 P항만**(`−a_P·P>−tol`, 방전시각). Q<0 흡수 시 loss_reduction이 정상적으로 음수가 될 수 있어 전체 검증은 오탐. Q항 부호 정합성은 측정 직후 `a_Q<0`(loss_coeffs strict)이 담당. → 3-C.2 갱신됨.

**다음 작업 (2026-08 9차 갱신 — dev 완주·측정 4배 가속 완료, degeneration 탈출 국면)**
※ 완료: min7(9-A)·경계 pull(9-B)·재산출 완전제거(9-C)·dev 무해성 검증(9-E). 이하가 남은 것.
1. **★★ PSO degeneration 탈출 (최우선, full 선결조건).** dev 3 run 중 2가 S=E=0 비활성 평지에 빠짐(9-D).
   근원: 탐색공간에서 "j_net≈0인 비활성 평지"가 압도적으로 넓고 "j_net>0 경제적 섬"이 좁다. 전 군집이
   평지에 앉으면 velocity가 죽어 좁은 섬을 못 찾는다. 후보 대책: **초기화를 S,E 양수영역에 편향 샘플링 /
   비활성 해에 작은 페널티로 평지 기울이기 / velocity 재점화**. 탈출 실험 자체가 시뮬레이션이므로
   9차 속도개선(4배)이 이 실험의 전제. 탈출 확인 후에야 full의 재시작 분산 판정이 유효.
2. **test_evaluate·test_qp_regression 확정.** 이제 재산출 제거 후 최적해가 나왔으므로 이 값으로 박는다:
   gbest x=[16, 0.15018212110054674, 0.4358017280776617], j_net=2,991,905, b_energy=1,583,700,
   b_defer=12,238,171, v_violation=0. test_qp_regression 층1 원단위(2,100~2,600원)도 이 해 기준. (전압위반
   기대값 0.3781163378은 이미 갱신됨.)
3. **S 양자화·캐시 제거 검토.** 히트율 3.5%면 캐시가 속도에 거의 무의미 → 양자화(나아가 캐시 전체) 제거로
   메모리·복잡성 절감 가능(크래시 원인이 캐시 증식이었으니 메모리에 이롭다). **단 degeneration 수정 후
   히트율 재측정하고 판정** — 탈출로 입자가 뭉치면 히트율이 오를 수 있어 현 3.5%가 곧 바뀔 값이다.
4. **overflow 경고 원인 확인.** 초기 세대 발산입자 fitness 집계 오버플로. gbest 무해하나 탈출 실험서 입자
   분포 바꿀 때 재확인.
5. **후처리 지표 실측 검증.** dev 실제 결과로 postprocess 지표가 물리적으로 타당한지.
6. **n=1 full 실행 + 분석.** 탈출·테스트 확정 후. 무효전력 가치(전압·용량 채널)가 핵심 연구질문의 답.
7. **다수기 확장** — 통합 solve_peak·solve_avg, 교차항 B행렬(C.6, blockdiag→비대각). 계수 측정층도 교차항
   위해 재설계 필요(버스쌍 측정 or 해석적 B∝DᵀRD, 단순 행렬 채우기 아님). 최적 기수는 이 실험이 결정.
8. **Phase 2** (PV 추가, clear/cloudy 분기, Q<0 자동 지원).
- ※ 문서 분리(RATIONALE.md)는 보류 — 부록이 회고록으로 정리돼 분리 실익이 줄었다.


### ★ QP 편입 회귀 기준 (6차, `test_qp_regression.py`)
6차 편입이 기존 검증 자산(편입 전 LP 결과)을 오염시키지 않았는지 확인한다:
- **층 1 (하드 게이트):** `force_q_zero=True`(Q=0 강제)에서 편입 전 대비 P_net 차이가 **P 손실항 기여분(~2,100~2,600원 규모)으로 한정**. 초과 시 정식화 오류. (완전 (B) 정식화라 Q=0이어도 P 손실항이 남으므로 "완전 일치"가 아니라 "P 손실 기여분만큼 차이"가 기준.)
- **층 2:** Q 손실항 on일 때 j_net ≥ off일 때(Q=0이 항상 실행가능하므로 최적해가 그보다 나쁠 수 없음). 위반 시 부호 오류.
- **층 3 (참고):** 프로토타입(고정 통제점, P 고정)의 j_net을 통합 LP에서 같은 P 강제 시 재현. P 고정이 통합 LP엔 부자연스러워 하드 기준 아님.
- **부호 assert:** 방전 시각 `−a_P·P_net > 0` (P항만, 3-C.2 7차 정정 — Q<0 흡수 시 loss_reduction 전체는 정상 음수 가능). 계수 부호 전파 검증. Q항 부호는 측정 직후 `a_Q<0`이 담당.

### ★ scripts/ 규약 (확정)
- **커밋한다.** `probe_noise.py`가 `BALANCE_ATOL`과 원칙 4의 `atol=10.0`을 떠받치듯,
  스크립트는 확정 상수의 **근거**이며 재현 가능성 자체가 자산이다.
- **실측 결과 CSV도 함께 커밋한다.** 벤치마크는 머신 종속이라 재실행으로 복원되지 않는다.
  파일명에 머신 태그를 포함할 것(`bench_workers_<hostname>_<timestamp>.csv`).
- **pytest 수집 대상이 아니다** (`pytest.ini`의 `norecursedirs = scripts`).
  일회성 실측이라 조건이 바뀌면 자연히 깨지는데, 이를 테스트로 취급하면
  **"물리와 무관한 이유로 실패"하는 원칙 2의 함정**에 그대로 빠진다.
- **각 스크립트 상단에 "무엇을 확정했는가" 한두 줄을 남긴다.** 실행 일시·머신은
  CSV 파일명과 Git 이력에 이미 있으므로 중복이라 불필요하다. 남겨야 할 것은 **결론**이다
  (Claude Code는 대화 기록을 읽을 수 없으므로 파일 자체에 근거가 있어야 한다).


## 부록 B. 확정 상수 요약 (코드 붙여넣기용)

```python
# CRF (두 개 구분!)
CRF_20 = 0.076876   # 편익·비용 연간화 (4.5%, 20년, ESS 달력수명)
CRF_30 = 0.061392   # c_cap 산출용 (4.5%, 30년, 배전설비 내용연수). 정밀값 0.061391542475

# 비용
c_kWh_capex = 219528.17   # 원/kWh
c_kW_capex  = 36541.18    # 원/kVA
c_kWh_opex  = 5714.70     # 원/kWh-yr
warranty_rate = 0.005564  # CAPEX 대비/yr (레벨라이즈드)

# 편익
c_cap = 76082.2           # 원/kW-yr (이연, 반올림 없는 산출값의 소수1자리)

# ★ MW/MWh 파생 상수 (3절 단위 규약). benefits.py는 아래만 사용한다.
#   반드시 위 출처값을 곱해서 정의할 것 — 숫자를 다시 타이핑하면 두 값이 갈린다.
C_CAP_PER_MW_YR       = c_cap       * 1000   # 원/MW-yr
C_KWH_CAPEX_PER_MWH   = c_kWh_capex * 1000   # 원/MWh
C_KW_CAPEX_PER_MVA    = c_kW_capex  * 1000   # 원/MVA
C_KWH_OPEX_PER_MWH_YR = c_kWh_opex  * 1000   # 원/MWh-yr
SMP_PER_MWH = {s: np.array(v) * 1000 for s, v in SMP.items()}   # 원/MWh

# 수지 검증 허용오차 (3절 실측 유도, 확정)
BALANCE_ATOL = 1e-7        # MW. np.allclose(..., atol=BALANCE_ATOL, rtol=0) — rtol=0 필수

# 발산 페널티 (3-A절, 잠정값 — 첫 실행 로그 보고 조정)
PENALTY_DIVERGE = 1e15     # 원. 도달 가능 최대 페널티(~1.9e13)의 50배 이상. np.inf 금지
                           # ★ 12차: overflow 경고의 원인 아님(1e15는 std 제곱해도 1e30<<float 한계).
                           #   경고 원인은 lshade_core._sample_f의 tan(π/2±ε) 특이점 — 무해(재샘플·클리핑 방어)

# 물리
DoD = 0.90
eta_c = eta_d = 0.9487     # RTE 0.90 (충방전 왕복). 무효전력 경로와 별개
ETA_PCS = 0.975            # ★ 6차 신설. 무효전력 단방향 통과 효율(변압기 포함).
                           #   Loss_pcs=(1-ETA_PCS)*(hypot(P,Q)-abs(P)). RTE와 이중계상 안 함. 근거: Power Electronics HEM CEC 97.53% (4절)
self_discharge_daily = 0.00067                          # /day
SELF_DISCHARGE_HOURLY = 1 - (1 - 0.00067)**(1/24)       # = 2.7925633002e-05 (복리 정확값)
V_min, V_max = 0.95, 1.05  # pu — 출처: ANSI C84.1 / IEEE 계열 (한국 전기사업법 아님, 1절 참조)

# SOC 규약: MWh(에너지 절대량). 아래는 '비율'이며 LP 내부에서 E_rated를 곱해 쓴다.
SOC_INIT_FRAC = 0.50       # SOC[0] = SOC[24] = 0.50 * E_rated  [MWh]
SOC_MIN_FRAC  = 0.05       # 0.05 * E_rated  [MWh]
SOC_MAX_FRAC  = 0.95       # 0.95 * E_rated  [MWh]  (DoD 90%)

# 스케일 (★ 6차: S=10MVA 고정 + 역률 0.95. P·Q 각각 다른 계수. 구 K_scale=2.288670 폐기)
K_P = 2.557201             # 9.500 MW / 원본 P합 3.715
K_Q = 1.357609             # 3.122499 Mvar / 원본 Q합 2.300  → P합9.500, Q합3.122499, S=10.0, 역률0.95 (1절)
PF_TARGET = 0.95
VN_KV = 22.9
SLACK_VM_PU = 1.02         # 3차 변경(구 1.0). 6차 재확인: 새 부하에서도 기저 전압위반 0. 1절 참조

# 경계
B_BOUNDS = (1, 32)         # 정수
S_BOUNDS = (0.0, 2.4)      # MVA (충전여유 2,473kW → 2.4, DoD 무관)
E_BOUNDS = (0.0, 10.2)     # MWh (x_max=4.234·S 살짝 상회, 조달비 포함 재산정)
# ★ 6차: Q_RATIO_BOUNDS 삭제. Q는 하위 QP의 시각별 결정변수(2절·2-B). 8절-6 q_ratio 스윕도 폐기.

# ★ 6차 신설: 하위 QP 파라미터 (2-B 참조)
POLY_N = 128               # PCS 다각형 면수. 반경오차 1-cos(pi/128)=0.030%, PCS 과소과금 0.27%
MU_VOLT = 4.0e7            # LinDistFlow 전압 페널티 μ. 초기추정 = LAMBDA_V/247 (일 단위 등가). 유도항이라 정확값 덜 중요 — 구현 시 실측 조정
# 손실계수(a_P,a_Q,b_PP,b_QQ,b_PQ)는 (b,S,t,s)별 조류계산 산출. 최적화는 p_center=0 C0/X0, 재산출은 gbest 진단 전용(2-B.5). H_cost PSD(quad_form 직접)
LOSS_COEF_P_GRID = [-0.25, -0.125, 0, 0.125, 0.25]   # 중심 근방 P격자(×S), Q는 적응형 5점
LOSS_GRID_DESIGN = "min7"   # 기본 7점; 회귀 비교 시 "full25" 복원
LOSS_PULL_THRESH = 0.95      # min7 측정중심 pull 발동 비율(|p_center|/S)
LOSS_PULL_TARGET = 0.85      # pull 후 측정중심 비율(solve·운영점은 불변)
RECOMPUTE_ENABLED = False    # 최적화는 X0 사용; True면 종전 C1/X1 복원

# 페널티 (★ 4차: 현 조건에서 재조정 불필요 — 경제 영역과 위반 영역이 분리.
#          영구 확정 아님. full 실행·다수기·Phase 2 진입 시 재검토. 3-B절 트리거 표 참조)
LAMBDA_V = 1e10            # 원/pu (전압위반. 실측상 안전장치로만 기능 — 최적해 선택에 미개입)
LAMBDA_LINE = 1e10         # 원/과부하비율 (선로위반. 실측 i_violation=0 → 조정 불필요)
# 위반량: L1, ALL_DAYS 전부 합산

# ★ 상위 최적화기 = L-SHADE (12차 — PSO 상수 폐기. lshade_core.py)
#   정할 파라미터는 사실상 eval_budget 하나. F/CR은 자기적응. 나머지는 Tanabe-Fukunaga 2014 표준값.
N_INIT_COEF = 12           # 초기인구 N_init = 12 * n_dims (n_dims=3*n_ess). n=1→36, n=2→72
P_BEST_RATE = 0.11         # current-to-pbest 상위 풀 비율 (표준)
MEMORY_SIZE = 6            # F/CR 성공이력 메모리 칸수 (표준)
MIN_POPULATION = 4         # LPSR 인구 하한 (표준)
# 프로파일: eval_budget = budget_coef * N_init
#   개발(dev):  budget_coef=30,  n_runs=3   (n=1 예산 1080)
#   본실험(full): budget_coef=100, n_runs=20 (n=1 예산 3600, n=2 예산 7200)
#   ★ full 예산은 PSO등가 안전상한 — 50·N_init로 포착률 유지되면 낮춤(미결 ②)
N_WORKERS = 8              # ★ 12차: 통합 QP 다수기서 메모리 안전 상한 8(구 16은 다수기 QP 메모리 초과)
                           #   (probe/벤치는 8워커. 단일기 뼈대 시절 16은 구값)

# gbest 재현 검산 허용오차 (7-A --diagnose)
RUN_CONSISTENCY_ATOL_WON = 15.0   # ★ 12차: 10→15. 우수해서 Q가 PCS한계 binding(poly_binding~0.53)일 때
                                  #   QP solver 재현오차 10.07원으로 임계 미세초과. 물리량 아닌 solver
                                  #   재현잔차라 버그(수백~수천원)와 4~5자릿수 차이 — 탐지력 무손상
# (구 PSO 상수 W_MAX/W_MIN/C1/C2/N_PARTICLES는 12차에서 폐기)
```

---

## 부록 C. 하위 QP + LinDistFlow 편입 — 결정 여정 (6차 완료 기록)

> **성격 전환 (6차).** 이 부록은 원래 *"시변 Q를 LinDistFlow로 편입하는 계획서"*였다. 6차에서 편입이 **완료**되며, 부록의 성격이 "계획"에서 "결정 여정 회고록"으로 바뀐다. 확정된 설계는 본문(2-B, 3-C, 1·4·8절)으로 승격됐다. 이 부록은 **왜 그렇게 결정했는가**의 근거·기각 이력·시행착오만 간결히 남긴다.

### C.1 계획이 실측으로 바뀐 지점 — "손실 무시"에서 "손실 QP 편입"으로 ★

원 계획(5차까지)은 **"전압만 LP가 보게 하고 손실은 계속 버린다(2차 효과라 무시)"**였다. 근거는 "손실 편익이 총편익의 ~1%라 명시적 최적화 이득이 미미"였다.

**이 전제가 20여 세션의 프로토타입 검증으로 뒤집혔다:**
- "손실 1%"는 **무효전력을 못 보던 시절의 값**이었다. Q를 최적화하면 손실저감 편익이 유의미해진다. 그리고 Q는 손실을 통해서만 편익을 만들므로, 손실을 목적함수에서 버리면 Q를 최적화할 구동력 자체가 사라진다(해가 degenerate).
- 비선형 손실 `I²R`을 **2차식(QP)으로 근사**하면 실측 대비 0.02~0.07% 정확도로 재현됨이 확인됐다. "LP에 못 넣는다"는 전제가 "QP로 넣을 수 있다"로 바뀌었다.
- → **결론: 전압(LinDistFlow)과 손실(QP 5계수)을 모두 하위 문제에 편입한다.** 확정 설계는 2-B절.

### C.2 방식 선택 — PWL vs QP (QP 채택)

손실 2차 곡면을 목적함수에 넣는 두 방식을 프로토타입으로 비교했다:
- **PWL(조각선형):** 세그먼트 경계에서만 정확하고 경계 사이에서 최대 −32.5% 오차. 정확도가 세그먼트 수(하이퍼파라미터)에 종속. 계단 고정, 솔버 불안정(OPTIMAL_INACCURATE).
- **QP(2차):** 하이퍼파라미터 없음, 전 구간 균일 정확도(−0.7%→보정 후 0.05%), 속도 6~7배, 수치조건 우수, Phase 2 Q<0 자동 지원, 다수기 교차항 표현 가능.
- → **QP 채택.** j_net 차이 자체는 작으나(순효과의 1~2%), 견고성(하이퍼파라미터 부재·속도·확장성)에서 QP가 전 축 우세. 상세 프로토타입 이력은 세션 기록 참조.

### C.3 기각된 대안들 (간결)

- **MISOCP (혼합정수 SOCP):** 정수변수로 상보성·on-off를 엄밀히 표현하나, (a) 계산시간 폭증 (b) 이 문제에 정수 의사결정이 실제로 필요 없음(효율 손실이 상보성을 자동 보장) → 기각. 상위호환이 아니라 잃는 것(속도)이 있다.
- **SOCP (D2, PCS/손실 원뿔):** 다각형/QP 대비 solve 3~5배 느림(full 환산 2h→7~10h), 대가는 반경오차 0.48%뿐 → 기각. 다각형 POLY_N=128로 충분.
- **P̄ 기반 PWL PCS (D3):** 기준점 P̄가 하이퍼파라미터이자 운영점 의존이라, PWL을 버린 이유를 PCS에 다시 들여옴. 게다가 P̄는 최적 스케줄을 알아야 정해져 순환 → 기각.
- **α(P) 축차선형화·상시 재산출:** 운영점마다 손실계수를 갱신해 얻는 연간 j_net 이득이 dev gbest에서 약 100원(0.003%)뿐이라 최적화 경로에서는 제거. X1/X2 계산은 gbest 진단에만 유지한다(2-B.5).

### C.4 전압 = LinDistFlow μ 페널티 (유지된 설계)

- LinDistFlow 재귀식으로 전압을 P·Q의 affine 표현으로 얻고, 하한·상한 초과분을 **목적함수 페널티 μ**로 넣는다(하드 제약 아님). 근거: 근사오차로 인한 infeasible 위험 회피, 근사오차가 가부를 뒤집지 않음. 최종 전압 판정은 사후 AC + 7절 λ_V.
- **정확도 게이트 통과:** LinDistFlow 근사 vs AC 최대 전압오차 0.001421 pu < 임계 0.005 pu (방문 영역 내). "sweep wide, judge narrow" — 넓게 탐색하되 최적권에서만 정확도를 요구. 임계 0.005의 근거는 전압 여유(슬랙 1.02의 상한 여유)와 게이트의 오염 방지 역할에서 나온다.
- **μ 페널티는 부분선형(볼록)이라 문제를 QP 수준으로 유지**한다(2차로 안 만듦). 하위 문제 = 2차 목적(손실) + 부분선형 페널티(전압) + 다각형·선형 제약.

### C.5 시행착오 교훈 — 부호 버그와 괴리

- **★ 부호 버그 (두 세션 오염).** 계수 규약 `L_cost = −ΔL`에서 1차계수가 음수인데, pk 제약에 그대로 넣어 손실저감을 손실증가로 취급했다. LP가 방전을 줄여 실제 슬랙피크가 **높아지고 이연이 악화**됐다. 부호를 바로잡으니(3-C.2) 손실 1차항이 이연을 개선. 교훈: 부호가 계수→목적함수→제약으로 전파되며 두 번 뒤집히므로, 방전 시각 `loss_reduction>0` assert로 강제.
- **괴리 A/B/C.** (A) LP pk(부하단, LinDistFlow 조류) vs AC 슬랙 괴리 (B) 계수 stale — 측정점≠해점 (C) pk 1차 근사 잔차. B·C의 운영점 재산출 효과는 dev gbest 연간 약 100원으로 실질 무해해 최적화에서는 C0/X0를 사용한다. C1/X1/X2 진단은 이 결론이 유지되는지 감시한다.
  - **★ 괴리 A의 뿌리는 LinDistFlow 선형화다.** 재산출·손실근사는 손실 모델(B·C)을 고치지만, 괴리 A의 대부분은 **LP가 쓰는 조류 모델(LinDistFlow, 선형)과 편익을 재는 조류 모델(AC, 비선형)의 구조적 격차**에서 온다. 이는 손실 계수를 아무리 정확히 재도 안 사라진다(재산출 전후 괴리량이 거의 동일한 이유). LinDistFlow를 AC로 바꾸면 LP가 비선형이 되어 못 풀기에, "LP는 근사 조류로 스케줄, 편익은 AC로 측정"하는 이중 구조의 불가피한 산물이다.
  - **★ 괴리는 무해하지 않다 — 정확한 성격.** 괴리는 **QP 지형을 왜곡**해 전역최적 탐색을 방해할 수 있다(무해하지 않음). 다만 **최종 편익 값 자체는 그 스케줄을 사후 AC에 넣어 얻은 물리적 값**이므로, 괴리가 편익 숫자를 틀리게 만들지는 않는다. C.4 게이트(전압오차 0.0014 < 0.005)가 보장하는 것은 "이 지형 왜곡이 최적 스케줄 선택을 뒤집을 만큼 크지는 않다"는 것이다.

### C.6 다수기 교차항 — 미래 안건 (경로 기록)

다수기(n≥2) 손실은 `Q^T B Q`의 **비대각 교차항**(공유 상류 선로의 `2·r·I_i·I_j`)을 갖는다. 이는 solve_avg의 기별 분리가능성을 깨고(2절), PWL로는 원리적으로 표현 불가능하나 **QP는 B 행렬 비대각화로 정확히 표현**한다.
- **해석적 경로:** B 행렬은 실측(버스쌍 조합폭발) 없이 **계통 R 행렬에서 유도** 가능하다 — `B ∝ D^T R D`(D는 상하류 0/1 행렬), 구성상 자동 PSD. 조류계산 없이 pandapower 망 데이터에서 계산되며 볼록성이 공짜로 보장된다.
- **당면 안건 아님:** 현 로드맵은 n=1을 먼저 돌린다. 다수기 확장 시 통합 solve_peak·solve_avg와 함께 처리한다. (최적 기수는 다수기 실험이 결정할 대상 — 과거 "기수 1"은 분리 LP의 산물이라 근거로 쓰지 않음.)

### C.7 POLY_N 상향 (12→128)

PCS 다각형 면수를 스윕한 결과: 오차가 `1−cos(π/N) ∝ 1/N²`을 따르며, N=64에서 j_net 참값 대비 <500원, N=128에서 완전 수렴. 16코어 full 비용은 N=32 대비 +12초로 무시할 만함. **POLY_N=128 채택**(방어력 우선). 전 각도 N면 생성이라 Phase 2 Q<0에 그대로 전이.
